# 真实预约提交 QA 复测 - 2026-06-21

message_type: handoff
request_id: REQ-20260621-真实预约提交QA复测
from_lane: qa
to_lane: coordinator
created_at: 2026-06-21T10:45:29+08:00
recorded_in: 当前对话 / docs/agent-dispatch-state.md / docs/worklog.md / docs/testing/真实预约提交QA复测-20260621.md

## QA 结论

阻塞，真实页面预约提交未完成复测。

本轮已复核后端与真实云端前置证据：`api` 云函数已部署，远端包包含 `customer_profile` 缺集合兼容逻辑；真实云数据库已创建空集合 `customer_profile`，并通过 `listCollections` 与 `count n=0` 只读验证。

但本轮未能可靠操作微信开发者工具真实业务页，未完成“选择可约桌台 -> 提交预约 -> 查看我的预约/预约详情 -> 到店提交”的真实页面链路。因此不能写成真实预约提交通过，也不能确认 `DATABASE_COLLECTION_NOT_EXIST customer_profile` 在用户页面上已经消失。

## 测试环境

- 项目路径：`C:\Users\宋\Documents\桌球小程序`
- AppID：`wx6a196f6ebfb27596`
- 云环境：`cloud1-d2gp2ayiwab8f8a94`
- 目标云函数：`api`
- 目标集合：`customer_profile`
- 复测方式：优先尝试微信开发者工具业务页；失败后降级为真实云端修复证据复核与手动复测清单。

## 测试步骤

1. 复核 `docs/agent-dispatch-state.md`，确认当前 active request 指向 QA 复测。
2. 复核 `docs/backend/真实预约customer_profile集合缺失-20260621.md`，确认后端部署与集合创建记录。
3. 复核 `docs/worklog.md` 最新记录，确认 `customer_profile` 集合创建和只读验证已完成。
4. 尝试使用 WeChat DevTools MCP 捕获当前开发者工具窗口截图，准备判断是否能继续真实页面操作。
5. 因工具返回 `WeChat DevTools window not found`，停止 GUI 自动化，不继续冒充页面点击结果。

## 真实页面/真实云端证据

### 已确认的真实云端证据

- `api` 云函数部署成功，后端记录显示：
  - `success=true`
  - `filesCount=11`
  - `packSize=48.2 KB`
  - 云函数状态 `Active`
  - 运行时 `Nodejs16.13`
- 部署后远端包核对通过：
  - 下载包 `handlers.js` 与本地 SHA256 一致。
  - 远端包包含 `isCollectionNotFoundError`、`findOneOptionalCollection`、`customer_profile` 缺集合兼容逻辑。
- `customer_profile` 集合真实创建成功：
  - 创建返回 `ok=1.0`，requestId `6e567302-d7ea-460e-abd5-6fa65be7111c`。
  - `listCollections` 返回 `name=customer_profile`、`type=collection`，requestId `5fceb7eb-41de-4593-a9cb-14d835081738`。
  - `count customer_profile` 返回 `n=0`、`ok=1.0`，requestId `5e327c15-bd75-40f1-96a5-f0e244f1e93a`。

### 未获得的真实页面证据

- 未获得预约页提交成功截图。
- 未获得提交失败/成功 toast。
- 未获得“我的预约”中新预约记录截图。
- 未获得预约详情状态截图。
- 未获得到店提交后状态截图。
- 未获得控制台中 `createReservation` 的真实返回体。

## 通过项

- 后端真实部署与远端包核对证据已存在。
- `customer_profile` 空集合真实存在且为空，缺集合根因已按最小授权补齐。
- 本轮未执行删除、清空、迁移、批量造数据或创建其它集合。
- `git diff --check -- docs/testing/真实预约提交QA复测-20260621.md docs/worklog.md` 通过，无输出。

## 失败项 / 阻塞项

### B-01 无法可靠操作当前微信开发者工具业务页

严重等级：阻塞真实页面验收。

MCP 截图结果：

```text
ok=false
stderr=WeChat DevTools window not found
```

影响：

- 无法确认当前开发者工具是否仍打开正确项目业务页。
- 无法执行真实页面提交预约。
- 无法观察是否仍出现 `DATABASE_COLLECTION_NOT_EXIST customer_profile`。
- 无法确认预约是否进入“我的预约/到店”链路。

## 是否仍有 customer_profile 报错

真实页面层：未确认。

云端前置条件层：`customer_profile` 集合已存在，且 `api` 云函数已部署包含缺集合兼容逻辑，因此此前 `DATABASE_COLLECTION_NOT_EXIST customer_profile` 的直接根因已处理。但由于本轮未完成真实页面提交，不能宣称用户页面不再报错。

## 未实测范围

- 未在微信开发者工具真实页面点击提交预约。
- 未写入真实 `reservation` 测试预约记录。
- 未验证“我的预约”出现新预约。
- 未验证预约详情页。
- 未验证“我已到店”提交。
- 未真机。
- 未体验版。
- 未多账号/多角色。
- 未真实手机号授权。
- 未测试支付、储值、优惠券、订单。
- 未做 UI 风格验收。

## 用户最小复测与回传要求

需要用户手动操作或重新提供截图/控制台文本：

1. 打开微信开发者工具中的正确项目。
2. 清缓存并重新编译。
3. 进入预约页，选择一张可约桌台。
4. 点击提交预约。
5. 回传以下任一结果：
   - 成功：预约成功 toast、跳转到“我的预约”或预约详情截图、新预约记录截图。
   - 失败：完整控制台错误文本、toast 文案、云函数返回体。
6. 若成功进入我的预约，继续点击预约详情并尝试“我已到店”，回传状态是否变为“待核实”。
7. 失败回传字段建议包含：
   - `errCode`
   - `errMsg`
   - `code`
   - `message`
   - 是否仍包含 `DATABASE_COLLECTION_NOT_EXIST customer_profile`
   - `createReservation` 返回的 `ok/data/code/message`

## 建议下一步负责 agent

下一步负责 agent：测试工程师 Agent / 后端工程师 Agent  
lane：qa / backend

- 用户完成真实页面复测并回传截图/错误后，由测试工程师 Agent 记录真实页面结果。
- 若仍出现 `customer_profile` 缺集合错误，回派后端工程师 Agent 核对调用环境、云函数版本和真实 DB 环境是否一致。
- 若出现其它集合缺失或新后端错误，回派后端工程师 Agent 按最小授权处理。
- 若提交成功并进入我的预约/到店链路，再交验收官 Agent 做阶段收口。

是否触发暂停条件：是。当前需要用户手动微信开发者工具操作/截图；QA 无法可靠操作真实业务页。
