# P0 前端预约功能补齐报告 - 2026-06-20

message_type: handoff
request_id: REQ-20260620-P0前端功能补齐实施
from_lane: frontend
to_lane: coordinator
created_at: 2026-06-20T12:04:47+08:00
recorded_in: 当前对话 / docs/frontend/P0前端预约功能补齐报告-20260620.md / miniprogram/

## 完成内容

- 完成预约页 P0 功能补齐：日期选择、日期+时间+时长生成起止时间、按时段和桌型刷新桌台、不可用桌台禁选、预计价格摘要、备注输入。
- 完成我的预约 P0 功能补齐：状态筛选 tabs、详情入口、统一状态文案、待到店差异化“我已到店”按钮、其他状态只读动作提示。
- 补必要的预约详情状态文案兼容和 mock/service 返回兼容。
- 根据后端 P0 回传，补小程序 API 错误码中文文案、分页字段兼容，以及 `loginByWechat`、`authorizePhone`、`getCurrentRole`、`reviewCampaignRegistration` 的本地 mock action。
- 本轮未改后端、未改 Web 后台、未部署、未写真实数据库、未做 UI 风格重做、未写 `docs/worklog.md`。

## 改动文件

- `miniprogram/pages/reservation/index.js`
- `miniprogram/pages/reservation/index.wxml`
- `miniprogram/pages/reservation/index.wxss`
- `miniprogram/pages/my-reservations/index.js`
- `miniprogram/pages/my-reservations/index.wxml`
- `miniprogram/pages/my-reservations/index.wxss`
- `miniprogram/pages/reservation-detail/index.js`
- `miniprogram/pages/reservation-detail/index.wxml`
- `miniprogram/services/api.js`
- `miniprogram/utils/mock.js`
- `docs/frontend/P0前端预约功能补齐报告-20260620.md`

## 页面/交互变化

- 预约页增加 7 天日期条，切换日期后重新请求可用桌台。
- 预约页保留时间滚轮，选中时间和时长变化后统一生成 `startAt/endAt`，并刷新桌台可用性。
- 预约页增加桌型筛选，传 `tableType/table_type` 到 `getAvailableTables`。
- 桌台卡统一展示可预约、已预约、使用中、维修中、停用等状态；不可用桌台点击只提示，不会选中。
- 预约页增加预计价格摘要，按桌台小时价、助教小时价和时长合计；仍明确为到店核实结算。
- 我的预约增加 tabs：全部、待到店、待核实、进行中、历史。
- 我的预约每张卡增加“查看详情”；待到店展示“我已到店”，其他状态展示只读状态动作。
- 预约详情页状态流转改为按状态枚举匹配，展示统一中文状态。

## 接口调用变化

- `getAvailableTables` 调用补充 `storeId/store_id`、`startTime/start_time`、`endTime/end_time`、`tableType/table_type`。
- `getAvailableTables` 前端消费兼容 `list/items/tables`。
- `createReservation` 提交补充 camelCase 和 snake_case 双口径字段，包括时间、桌台、助教、人数、时长、预计金额。
- `getMyReservations` 调用补充 `status/page/pageSize/page_size`，前端仍保留客户端筛选兜底。
- `getReservationDetail` 和 `submitArrival` 补充 camelCase 与 snake_case 双口径字段。
- 本地 mock 的 `getAvailableTables` 支持 `list/items/tables` 返回和桌型筛选；`getMyReservations` 支持状态筛选。
- `getMyReservations`、`getCampaignRegistrations`、`getCustomers`、`getNoShowRecords` 本地 mock 补 `page/page_size/total` 分页字段。
- API 错误态补充 `TABLE_HAS_ACTIVE_RESERVATION`、`CAMPAIGN_FULL`、`DUPLICATE_REGISTRATION`、`LOCK_BUSY`、`UNAUTHORIZED`、`FORBIDDEN`、`RESOURCE_UNAVAILABLE`、`REASON_REQUIRED` 等中文提示。
- 新增后端 P0 action 的本地 mock 兼容：`loginByWechat`、`authorizePhone`、`getCurrentRole`、`reviewCampaignRegistration`。

## 验证结果

- 小程序 JS `node --check`：通过，覆盖 `miniprogram/` 下全部 `.js`。
- JSON 解析：通过，`miniprogram/` 下全部 `.json` 可解析。
- 相关 WXML 标签检查：通过，覆盖 `reservation`、`my-reservations`、`reservation-detail` 三个页面。
- mock action 检查：通过，验证 `getAvailableTables` 的 `list/items/tables` 返回、桌型筛选、`getMyReservations` 状态筛选与分页、`getReservationDetail`、新增登录/手机号/角色/活动审核 mock action。
- app.json 页面入口检查：通过，`reservation`、`my-reservations`、`reservation-detail` 均已登记且页面文件齐全。
- `git diff --check -- miniprogram docs/frontend`：通过，退出码 0；仅有工作区已有 LF/CRLF 换行提示。

## 未实测范围

- 未用微信开发者工具/真机/体验版截图验证。
- 未连接真实云函数和真实数据库。
- 未替换真实价格、真实桌台编号、真实助教资料。
- 未进行 UI 样式模板调整。

## 剩余问题

- 真实接口字段若后端继续调整，需要中枢合并后端 P0 回传后再做一次联调校准。
- 预计价格当前按前端可见小时价估算，真实优惠、会员价、跨时段分段计费仍需后端价格规则最终兜底。
