# 我的个人中心改造 QA 复验报告 - 2026-06-14

## 任务信息

- request_id: REQ-20260614-我的个人中心改造QA
- from_lane: qa
- to_lane: coordinator
- 范围：`我的` Tab、个人中心页面、功能入口、原我的预约入口、储值/优惠券/订单展示链路
- 方式：只读静态检查 + JS/JSON 语法检查 + 后端单测

## 通过项

1. Tab 路由符合选择
   - `miniprogram/app.json` 中底部 Tab 第三项为 `pages/mine/index`，文本为“我的”。
   - 底部 Tab 第四项仍为 `pages/assistant-list/index`，文本为“助教”。
   - `pages/my-reservations/index` 保留在 `pages` 路由中，但不再作为底部 Tab 直接入口。

2. 个人中心页面结构符合第一版目标
   - `miniprogram/pages/mine/index.wxml` 已包含用户卡结构、头像、昵称、会员等级、手机号授权按钮。
   - `miniprogram/pages/mine/index.js` 已通过 `getMineCenter` 加载 profile、wallet、coupons、orders、noShowLocks。
   - 快捷指标包含“优惠券”“储值余额”。

3. 核心入口覆盖符合要求
   - `我的订单`：跳转 `/pages/mine-feature/index?type=orders&title=我的订单`
   - `我的预约`：跳转 `/pages/my-reservations/index`
   - `优惠券`：跳转 `/pages/mine-feature/index?type=coupons&title=优惠券`
   - `会员中心`：跳转 `/pages/mine-feature/index?type=member&title=会员中心`
   - `储值记录`：跳转 `/pages/mine-feature/index?type=wallet&title=储值记录`
   - 另有助教预约、活动报名、客服与门店入口。

4. 原 `my-reservations` 链路保留
   - `miniprogram/pages/my-reservations/index.js` 仍通过 `getMyReservations` 加载预约列表。
   - 个人中心“我的预约”入口指向该页面。

5. 储值/优惠券/订单为可查看、未接支付
   - `mine-feature` 对 `orders/coupons/member/wallet` 均有展示映射。
   - 储值页文案明确“第一期只展示后台配置金额，不接支付充值”。
   - 空态文案说明优惠券、会员权益可由商家后台配置。

6. Mock 与后端接口覆盖
   - `miniprogram/services/api.js` mock `getMineCenter` 返回 profile、wallet、coupons、orders、reservations、assistantReservations、campaignRegistrations、noShowLocks。
   - `cloudfunctions/api/src/handlers.js` 已实现 `getMineCenter` 与 `getMyReservations`。
   - `cloudfunctions/api/test/handler-flow.test.js` 包含 `mine center returns profile wallet coupons orders and user records`。

## 验证结果

- `node --check miniprogram/pages/mine/index.js`：通过
- `node --check miniprogram/pages/mine-feature/index.js`：通过
- `node --check miniprogram/pages/my-reservations/index.js`：通过
- `node --check miniprogram/services/api.js`：通过
- `node --check miniprogram/utils/mock.js`：通过
- `miniprogram/app.json` 与 mine 相关页面 JSON 解析：通过
- `cloudfunctions/api npm test`：24/24 通过
- `$wechat-devtools-qa` 静态检查：`ok: true`，18 个页面均无阻塞失败

## 失败项

- 未发现阻塞级功能失败。

## 剩余风险

1. 真实微信开发者工具截图未执行
   - 当前线程不能直接操作微信开发者工具 GUI。
   - 需要用户或可操作 GUI 的 QA 打开开发者工具，清缓存/重新编译后截图确认。

2. `mine/index.wxss` 存在兼容性警告
   - 静态检查提示 `radial-gradient`、`display: grid`、`gap`。
   - 这些不一定导致失败，但项目此前出现过 WXSS 真实渲染不生效问题，因此需要开发者工具截图确认“我的”页没有裸文本、布局堆叠或按钮不可识别。

3. 手机号授权真实弹窗未验证
   - 代码层存在 `open-type="getPhoneNumber"` 和回调处理。
   - 真机/开发者工具授权弹窗、拒绝授权、授权后手机号回填仍需实机确认。

4. 储值/支付相关真实链路不在本轮范围
   - 当前符合“不接支付，仅查看”的选择。
   - 真实充值、支付、退款、核销未测试，也不应在本轮测试。

## 需要微信开发者工具截图确认的点

- 底部 Tab 点击“我的”是否进入个人中心，而不是直接进入“我的预约”。
- 个人中心是否显示用户卡、会员等级、手机号授权按钮。
- 优惠券、储值余额、订单、我的预约、会员中心、储值记录入口是否可见且可点击。
- 点击“我的预约”是否进入原 `my-reservations` 列表。
- 点击“储值记录/优惠券/我的订单/会员中心”是否进入 `mine-feature` 对应标题页。
- 页面是否无裸文本、无白底样式丢失、无布局重叠。

## QA 结论

- 静态复验通过：个人中心改造符合用户选择。
- 后端与 mock 数据链路通过：`getMineCenter` 覆盖 profile、wallet、coupons、orders、reservations 等核心字段。
- 不需要打回前端/后端做功能修复。
- 进入验收官前建议补一张微信开发者工具“我的”页截图，重点排除 WXSS 兼容导致的视觉 P0。

## 用户截图补充复验

- 用户 23:39 微信开发者工具截图显示“我的”页结构和样式已渲染，但页面出现 `ACTION_NOT_FOUND: 未知接口：getMineCenter`。
- 本地代码确认 `cloudfunctions/api/src/handlers.js` 已实现并导出 `getMineCenter`，后端单测 `mine center returns profile wallet coupons orders and user records` 通过。
- 根因判断：开发者工具当前连接的云函数 `api` 版本未包含 `getMineCenter`，属于云函数未部署或部署版本过旧；不是前端页面结构缺失。
- 已补前端只读兜底：`miniprogram/services/api.js` 在 `getMineCenter` 遇到 `ACTION_NOT_FOUND` 时降级读取本地 mock，避免个人中心演示红屏；未对预约、报名、后台配置等写操作放开降级。
- 复验结果：`node --check miniprogram/services/api.js` 通过；`cloudfunctions/api npm test` 24/24 通过；小程序静态检查 `ok: true`。
- 剩余真实联调要求：如需验证真实后台可配置数据，仍需部署最新版 `cloudfunctions/api` 到当前云环境后重新编译小程序。
