# Web 商家后台 QA 实测报告 - 2026-06-15

## 任务信息

- request_id: REQ-20260615-000003-Web商家后台QA实测 / REQ-20260615-000004-Web后台QA无审批收敛 / REQ-20260615-000005-Web后台Playwright补证
- from_lane: qa
- to_lane: coordinator
- 范围：Web 商家后台 HTTP、构建、源码页签覆盖、mock action 链路、核心保存/发布 mock 链路、Playwright 浏览器补证
- 限制：未接真实云端；本轮只验证本地 mock 后台

## 自动通过项

1. HTTP 可访问
   - `http://127.0.0.1:5173/` 返回 HTTP 200。
   - 首页 HTML 可正常返回。

2. 生产构建通过
   - `web-admin npm run build` 通过。
   - 输出：
     - `dist/index.html`
     - `dist/assets/index-wJf4YGBd.css`
     - `dist/assets/index-BI7bntwg.js`

3. 页面/页签源码覆盖完整
   - 经营：经营看板、预约履约、客户沉淀
   - 配置：门店配置、桌台配置、价格规则、助教配置、活动配置
   - 增长：报名名单、爽约限制、素材库、首页装修器、报价与营销、操作记录
   - 顶部状态：云环境、操作人、同步中状态

4. mock action 读取链路通过
   - `getAdminDashboard`
   - `getStoreDetail`
   - `manageTables`
   - `getPriceRules`
   - `getAssistants`
   - `getCampaigns`
   - `getAdminReservations`
   - `getCustomers`
   - `getCampaignRegistrations`
   - `getNoShowRecords`
   - `getAuditLogs`
   - `getAssets`
   - `getPageConfig`

5. mock 数据覆盖通过
   - 看板 metrics：预约 2、待核实 1、客户 2、活动报名 1、爽约限制 1、空桌 2、空闲助教 2。
   - 桌台：4 张
   - 价格规则：3 条
   - 助教：3 个
   - 活动：2 个
   - 预约：2 条
   - 客户：2 条
   - 报名：1 条
   - 爽约限制：1 条
   - 素材：4 个
   - 首页装修模块：4 个

6. 核心保存/新增/发布 mock 链路通过
   - `manageStore`
   - `manageTables`
   - `managePriceRules`
   - `manageAssistants`
   - `manageCampaigns`
   - `manageAssets`
   - `managePageConfig`
   - 写操作后审计日志新增 7 条，最近记录包含 `managePageConfig:publish`、`manageAssets:upsert`、`manageCampaigns:publish`。

7. 文本/样式静态可用性
   - 源码中存在关键按钮：进入装修器、新增桌台、新增价格规则、新增助教、发布活动、新增素材、发布首页配置。
   - CSS 存在侧边栏、顶部栏、按钮、表格横向滚动、卡片网格、装修器预览等样式。

8. Playwright 浏览器补证通过
   - `open http://127.0.0.1:5173` 成功。
   - `snapshot` 成功，页面标题为“台球馆商家管理后台”。
   - 初始看板可读：今日预约 2、当前空桌 2、新增客户 2、活动报名 1。
   - 中文显示正常，侧边栏正常，无白屏。
   - 截图已保存：`output/playwright/web-admin-dashboard-20260615.png`。
   - 点击 `预约履约` 成功，列表显示 `A01 · 普台`、`B01 · 乔氏银腿`。
   - 点击 `素材库` 成功，素材列表可见。
   - 点击 `首页装修器` 成功，模块列表和小程序首页预览可见。
   - 点击 `报价与营销` 成功，三个商业化套餐可见。
   - 点击 `发布首页配置` 成功，页面出现“首页装修已发布”。
   - 截图已保存：`output/playwright/web-admin-decorator-after-publish-20260615.png`。
   - 控制台仅 React DevTools 信息和 `favicon.ico` 404，未发现业务 JS 错误。

## 自动失败项

- 未发现 HTTP、构建、源码页签覆盖、mock action 或核心 mock 写链路的自动失败项。

## 未覆盖项

1. 肉眼设计判断未覆盖
   - 无法在本轮自动确认是否“好看”、是否符合用户商业化展示审美。
   - Playwright 已证明主要页面可读可操作；最终审美取舍仍可由用户选择性确认。

2. 真实云端未覆盖
   - 本轮只验证 mock action，不依赖真实云端。
   - 未验证真实数据库、真实素材上传、真实部署。

## 严重等级判断

- P0：未发现。
- P1：未发现自动可证实问题。
- P2：未发现。
- P3：当前 mock 数据只有 3 个助教，商业演示若要求展示“网红级”等更多档位，可后续补演示数据。
- 非阻塞：`favicon.ico` 404，可后续补图标，不影响后台业务演示。

## 复现与验证步骤

1. HTTP：
   - `Invoke-WebRequest -Uri http://127.0.0.1:5173/ -UseBasicParsing`
   - 结果：HTTP 200。

2. 构建：
   - 在 `web-admin/` 执行 `npm run build`
   - 结果：构建通过。

3. mock action smoke：
   - 直接导入 `web-admin/src/api.js`，依次调用读取 action。
   - 结果：全部返回可用数据。

4. mock 写链路 smoke：
   - 直接调用 `manageStore/manageTables/managePriceRules/manageAssistants/manageCampaigns/manageAssets/managePageConfig`。
   - 结果：全部成功，并写入 mock 审计日志。

5. Playwright 浏览器补证：
   - 打开 `http://127.0.0.1:5173`。
   - 截图看板。
   - 点击预约履约、素材库、首页装修器、报价与营销。
   - 点击发布首页配置。
   - 结果：页面可读、交互可用、发布提示出现，无业务 JS 错误。

## 是否需要用户亲自检查

- 不强制需要。
- Playwright 已覆盖主要页面可读性、侧边栏跳转、装修器发布 mock 链路和截图留证。
- 如用户要判断商业化展示审美或报价文案是否符合销售预期，可选择亲自打开 `http://127.0.0.1:5173/` 检查。

## QA 结论

- Web 商家后台 QA 实测通过。
- HTTP、构建、源码覆盖、mock action、核心 mock 保存链路、Playwright 浏览器补证均通过。
- 未发现阻塞交付问题，可进入验收官阶段。
