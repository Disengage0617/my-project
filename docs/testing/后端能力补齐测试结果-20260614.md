# 后端能力补齐测试结果 - 2026-06-14

## 执行命令

```bash
cd cloudfunctions/api
npm test
```

## 结果

- tests: 21
- pass: 21
- fail: 0

## 覆盖重点

- 权限：普通用户无法执行员工动作；跨门店员工无法操作。
- 门店隔离：资源和操作均校验 `store_id`。
- 状态机：预约到店、核销、开台、关台、爽约、释放；桌台和助教状态切换。
- 冲突：桌台预约冲突、短锁、助教预约确认冲突。
- 限额：活动报名满额拒绝，成功后递增 `quota_used`。
- 手机号脱敏：员工查看活动名单/客户列表返回脱敏手机号，店长返回完整手机号。
- 审计日志：商家配置、助教状态、预约关键状态、爽约解除写入审计。
- 测试隔离：`bootstrapMvp` 和烟测 action 明确标记为 test-only。
