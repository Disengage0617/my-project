# 部署 api 云函数并只读 smoke - 2026-06-20

message_type: handoff
request_id: REQ-20260620-部署api云函数并只读smoke
from_lane: backend
to_lane: coordinator
created_at: 2026-06-20T当前时间+08:00
recorded_in: 当前对话 / docs/backend/部署api云函数并只读smoke-20260620.md / docs/worklog.md

消息内容：
- 部署命令/结果：
  - 部署前本地检查：
    - `node --check cloudfunctions\api\src\handlers.js`：通过，无输出。
    - `node --check cloudfunctions\api\index.js`：通过，无输出。
    - `cd cloudfunctions/api && npm test`：通过，29/29。
  - 部署命令：

```text
& 'E:\微信web开发者工具\cli.bat' cloud functions deploy --project 'C:\Users\宋\Documents\桌球小程序' --appid wx6a196f6ebfb27596 --env cloud1-d2gp2ayiwab8f8a94 --paths 'C:\Users\宋\Documents\桌球小程序\cloudfunctions\api' --remote-npm-install --lang zh
```

  - 部署结果：成功。CLI 返回 `api success=true`、`filesCount=11`、`packSize='30.5 KB'`，并提示线上已存在云函数 `api`，本次执行更新云函数。
- 部署后核对：
  - `cloud functions info --env cloud1-d2gp2ayiwab8f8a94 --names api`：`api` 状态为 `Active`，运行时 `Nodejs16.13`，超时 `3` 秒。
  - 已将部署后的云端 `api` 隔离下载到 `.tmp_cloud_download_after_deploy_api_20260620/`，未覆盖本地 `cloudfunctions/api/`。
  - 下载后 `src/handlers.js` 关键字核对：
    - 包含 `TABLE_TYPES = ["standard", "silver", "duya", "gold", "rose_gold"]`。
    - `bootstrapMvp` 包含 A/B/C/D/E 五组 40 张桌台，其中 C 组 `count: 8`、`table_type: "duya"`、`table_type_name: "独牙"`。
    - `getAvailableTables` 读取 `payload.table_type || payload.tableType`，并按 `table.table_type` 做服务端过滤，返回 `{ list, tables, items, total }`。
  - `node --check .tmp_cloud_download_after_deploy_api_20260620\src\handlers.js`：通过，无输出。
- 只读 smoke 结果：
  - 已完成云函数元信息 smoke：`api` 为 `Active`。
  - 已完成云端源码 smoke：隔离下载后的云端源码已是本地新版，包含 40 桌初始化和独牙桌型过滤逻辑。
  - 未执行 `getAvailableTables({ table_type: "duya" })` 真实 action 调用：当前微信开发者工具 CLI 的 `cloud functions` 命令只暴露 `list/info/deploy/inc-deploy/download`，未提供直接只读 invoke/callFunction 入口。
  - 未查询真实数据库：本轮禁止写库，且当前 CLI 未提供数据库集合只读查询入口。
- 是否仍需 DB 只读授权：
  - 是。虽然云端 `api` 代码已更新，但真实 `billiard_table` 是否已有 C01-C08、`table_type=duya`、启用/空闲状态仍未确认。
  - 下一步建议只读核对 `store_demo_001` 下 `billiard_table` 总数、C01-C08 是否存在、`table_type` 是否为 `duya`、`enabled` 是否为 `true`、`status` 是否为后端可用状态。
- 是否需要 DB 写入/初始化授权：
  - 暂不直接执行。若 DB 只读确认真实库仍只有 A01/A02/B01，或缺 C01-C08/`table_type=duya` 字段，则需要单独申请 DB 写入/初始化/迁移授权，受控补齐 40 张桌台。
  - 本轮未执行 `bootstrapMvp`、`seed.js`、`manageTables` 或任何写数据库 action。
- 改动文件：
  - `docs/backend/部署api云函数并只读smoke-20260620.md`
  - `docs/worklog.md`
  - 新增部署后隔离下载目录：`.tmp_cloud_download_after_deploy_api_20260620/`
- 验证结果：
  - 本地语法检查通过：`cloudfunctions/api/src/handlers.js`、`cloudfunctions/api/index.js`。
  - 本地测试通过：`cloudfunctions/api npm test`，29/29。
  - 云函数部署通过：`api success=true`。
  - 部署后元信息查询通过：`api Active / Nodejs16.13 / timeout 3`。
  - 部署后隔离下载通过：`.tmp_cloud_download_after_deploy_api_20260620/`。
  - 部署后源码核对通过：云端下载版已包含 `duya`、C 组 8 张、`table_type/tableType` 过滤。
  - 待执行：`git diff --check -- docs/backend/部署api云函数并只读smoke-20260620.md docs/worklog.md`。
- 未实测范围：
  - 未查真实云数据库。
  - 未调用真实 `getAvailableTables` action。
  - 未写入或迁移 40 张桌台数据。
  - 未跑微信开发者工具 MCP、未截图、未真机、未验证用户页面是否已显示 C01-C08。
- 建议下一步负责 agent：
  - Codex 中枢：申请或确认 DB 只读检查入口，优先确认真实 `billiard_table` 数据。
  - 后端工程师 Agent：执行 DB 只读核对；若缺 C01-C08，再输出最小数据迁移/初始化方案并等待写入授权。
  - 测试工程师 Agent：在后端确认数据也齐备后，复测 DevTools 预约页“独牙”筛选。
- 是否触发暂停条件：
  - 是。云函数部署已完成；继续排查“独牙仍 0 张”需要 DB 只读检查。若确认数据缺失，补齐数据属于真实数据库写入/初始化/迁移，必须另行授权后才能执行。

## Handoff 回传

message_type: handoff
request_id: REQ-20260620-部署api云函数并只读smoke
from_lane: backend
to_lane: coordinator
created_at: 2026-06-20T当前时间+08:00
recorded_in: 当前对话 / docs/backend/部署api云函数并只读smoke-20260620.md / docs/worklog.md

消息内容：
- 部署命令/结果：已执行 `cloud functions deploy --env cloud1-d2gp2ayiwab8f8a94 --paths ...\cloudfunctions\api --remote-npm-install`，结果成功，`api success=true`、`filesCount=11`、`packSize='30.5 KB'`。
- 部署后核对：`api` 元信息为 `Active / Nodejs16.13 / timeout 3`；部署后隔离下载源码已包含 40 桌、C 组 `duya`、`getAvailableTables` 的 `table_type/tableType` 过滤。
- 只读 smoke 结果：云函数代码层 smoke 通过；CLI 无直接 action invoke/DB query 入口，因此未调用真实 `getAvailableTables`，未查真实 DB。
- 是否仍需 DB 只读授权：是，需要确认真实 `billiard_table` 是否已有 C01-C08 和 `table_type=duya`。
- 是否需要 DB 写入/初始化授权：待 DB 只读结果决定；如真实数据缺 C01-C08，则需要单独授权写入/初始化/迁移。
- 改动文件：`docs/backend/部署api云函数并只读smoke-20260620.md`、`docs/worklog.md`、`.tmp_cloud_download_after_deploy_api_20260620/`。
- 验证结果：`node --check` 通过；`npm test` 29/29 通过；部署成功；部署后 `info` 和隔离下载核对通过；`git diff --check` 待最终命令确认。
- 未实测范围：未查 DB、未调用 action、未写数据、未跑 DevTools 页面/真机。
- 建议下一步负责 agent：后端工程师 Agent 执行 DB 只读核对；若数据齐备再交测试工程师 Agent 复测页面；若数据缺失则由中枢申请 DB 写入授权。
- 是否触发暂停条件：是。继续到真实数据修复需要 DB 只读或写入/初始化授权。
