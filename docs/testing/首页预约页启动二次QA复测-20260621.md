# 首页预约页启动二次 QA 复测 - 2026-06-21

message_type: handoff
request_id: REQ-20260621-首页预约页启动二次QA复测-新线程
from_lane: qa
to_lane: coordinator
created_at: 2026-06-21T11:31:00+08:00
recorded_in: 当前对话 / docs/agent-dispatch-state.md / docs/worklog.md / docs/testing/首页预约页启动二次QA复测-20260621.md

## 测试环境

- 项目路径：`C:\Users\宋\Documents\桌球小程序`
- AppID：`wx6a196f6ebfb27596`
- 微信开发者工具 CLI：`E:\微信web开发者工具\cli.bat`
- IDE server：`http://127.0.0.1:18799`
- 当前 DevTools 窗口标题：`billiards-private-domain-mvp`
- 本轮边界：只做本地静态检查、本地 DevTools 清编译缓存、打开项目、截图复核；未上传、未预览、未部署、未写真实云数据库。

## 输入材料

- `docs/frontend/首页services-api路径二次修复-20260621.md`
- `docs/testing/首页预约页启动QA复测-20260621.md`
- `miniprogram/pages/home/index.js`
- `miniprogram/pages/home/services/api.js`
- `miniprogram/pages/reservation/index.js`
- `miniprogram/pages/reservation/services/api.js`
- `miniprogram/pages/reservation/utils/mock.js`
- `miniprogram/pages/reservation/utils/constants.js`
- `miniprogram/app.json`
- 中枢补充的当前已打开窗口截图：`docs/testing/screenshots/current-open-devtools-20260621.png`

## 测试步骤

1. 复核前端二次修复报告和上一轮 QA 失败报告。
2. 检查首页/预约页顶层 require 是否已改为页面本地中转模块。
3. 运行小程序静态结构检查：`check-miniprogram-static.mjs --project C:\Users\宋\Documents\桌球小程序`。
4. 运行 JS 语法检查，覆盖首页、预约页、中转模块和公共模块。
5. 定向检查页面注册、本地中转模块和公共目标文件存在。
6. 使用 DevTools CLI 执行 `cache --clean compile --project C:\Users\宋\Documents\桌球小程序`。
7. 使用 DevTools CLI 执行 `open --project C:\Users\宋\Documents\桌球小程序`。
8. 尝试使用 DevTools 自动化/GUI 截图分别打开首页和预约页。
9. 纳入中枢补充的当前已打开 DevTools 主窗口截图作为首页启动层证据。

## 通过项

- 本地结构检查通过：`pagesChecked=18`、`ok=true`、`failures=[]`。
- 首页和预约页均已在 `miniprogram/app.json` 注册，页面 `.js/.wxml/.wxss/.json` 文件齐全。
- 首页顶层 require 为 `require("./services/api.js")`。
- 预约页顶层 require 为 `require("./services/api.js")`、`require("./utils/mock.js")`、`require("./utils/constants.js")`。
- 页面本地中转文件和公共目标文件存在。
- JS 语法检查通过，无输出。
- 定向中转目标检查输出：`STARTUP_BRIDGE_REQUIRE_TARGETS_OK pages=2 bridgeTargets=4 publicTargets=3`。
- DevTools CLI 登录正常，`islogin` 返回 `{"login":true}`。
- DevTools CLI 清编译缓存成功，返回 `cleancache`。
- DevTools CLI 打开项目成功，返回 `open`。
- 当前已打开 DevTools 主窗口截图显示首页真实渲染，不再是空白页。
- 当前已打开 DevTools 主窗口截图未见上一轮的 `module 'services/api.js' is not defined`。
- 当前已打开 DevTools 主窗口截图未见上一轮的 `Page "pages/reservation/index" has not been registered yet`。

## 失败项或未通过项

- DevTools 自动化页面路由仍不可靠，`pageRoutingOk=false`。
- QA 自己的 GUI 截图虽然成功产出 `01-home.png` 和 `02-reservation.png`，但两张截图底部实际页面路径均为 `pages/home/index`，不能证明预约页已打开。
- 当前截图中 Console 仍有 `SystemError (appServiceSDKScriptError) timeout`，与本轮 `services/api.js` 解析错误不同；本轮不把它判定为首页启动阻塞，但建议后续继续追踪。

## 截图和证据路径

- 当前已打开窗口截图：`docs/testing/screenshots/current-open-devtools-20260621.png`
- QA GUI 首页截图：`docs/testing/screenshots/首页预约页启动二次QA复测-20260621-retry/01-home.png`
- QA GUI 预约用例截图：`docs/testing/screenshots/首页预约页启动二次QA复测-20260621-retry/02-reservation.png`

截图可信度说明：

- `current-open-devtools-20260621.png` 可作为首页启动层真实 DevTools 证据：页面路径为 `pages/home/index`，首页已渲染。
- `02-reservation.png` 文件名来自预约用例，但截图实际仍停在 `pages/home/index`，不能作为预约页通过证据。

## 是否仍有目标报错

- `services/api.js` 解析失败：当前截图未见。
- `Page "pages/reservation/index" has not been registered yet`：当前截图未见。
- 首页空白：当前截图未见，首页已渲染。
- 预约页无法路由：未能确认；工具路由不可靠，当前没有预约页真实渲染截图。

## 未实测范围

- 未真实确认预约页渲染成功。
- 未验证真实预约提交链路、我的预约、预约详情、我已到店。
- 未真机、体验版、多账号、真实手机号授权验证。
- 未上传、未预览、未部署、未写真实云数据库。

## 结论

首页启动层二次复测通过：真实 DevTools 当前窗口已显示 `pages/home/index` 首页内容，不再空白，也未见 `services/api.js` 模块解析失败和预约页未注册报错。

预约页启动仍未通过 QA 确认：当前证据只有首页截图，自动化路由和 GUI 截图都没能可靠切到 `pages/reservation/index`。

## 建议下一步

- 下一步负责 agent：测试工程师 Agent / 用户配合
- lane：qa
- 建议任务：由用户在当前微信开发者工具里点击底部“预约”tab 后再截图 Console 和页面路径；或由中枢继续寻找可靠 GUI 路由方式。
- 是否建议进入验收官：否。首页启动问题可以阶段通过，但“首页/预约页启动”作为组合任务仍缺预约页真实截图证据。
- 是否触发暂停条件：是。预约页真实渲染需要用户手动切到预约页后截图，或需要更可靠的 DevTools GUI 路由能力。
