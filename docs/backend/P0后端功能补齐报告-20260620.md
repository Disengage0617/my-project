# P0 后端功能补齐报告 - 2026-06-20

message_type: handoff
request_id: REQ-20260620-P0后端功能模块实施
from_lane: backend
to_lane: coordinator
created_at: 2026-06-20T当前时间+08:00
recorded_in: 当前对话 / docs/backend/ / cloudfunctions/api/

消息内容：
- 完成内容：完成 P0 后端功能补齐最小收口：统一接口协议文档，补登录/手机号授权/角色 action，补活动报名审核 action，补活动报名短锁与名额占用标记，补配置类字段级校验，补 `manageTables:disable` 有效预约保护，补后台列表分页，补本地测试。
- 改动文件：`cloudfunctions/api/src/handlers.js`、`cloudfunctions/api/test/handler-flow.test.js`、`docs/backend/api.md`、`docs/backend/schema.md`、`docs/backend/P0后端功能补齐报告-20260620.md`。
- 接口变化：
  - 新增 `loginByWechat`：基于云函数上下文 `OPENID` 创建/更新 `user`，本地测试可用 payload `openid` seam。
  - 新增 `authorizePhone`：本地可测手机号写入 `user`，传入 `store_id` 时同步 `customer_profile`；真实微信解密未在本轮冒充完成。
  - 新增 `getCurrentRole`：返回当前用户在门店的 `role`、`permissions`、`staff_user`。
  - 新增 `reviewCampaignRegistration`：支持 `approve/reject`，记录审核人、审核时间、拒绝原因，写 `audit_log`；拒绝已占名额报名时回退 `quota_used/registered_count`。
  - 加强 `createCampaignRegistration`：使用 `reservation_lock` 的 campaign 资源锁保护名额扣减和重复提交，并写 `quota_reserved`。
  - 加强 `manageStore/manageTables/managePriceRules/manageAssistants/manageCampaigns`：增加字段级校验。
  - 加强 `getAdminReservations/getCampaignRegistrations/getCustomers/getNoShowRecords/getAuditLogs/getCampaigns`：返回基础分页字段。
- 数据模型/字段变化：`campaign_registration` 增加 `reject_reason`、`quota_reserved` 字段说明。
- 权限/状态机变化：
  - `reviewCampaignRegistration` 要求 `staff` 及以上，只允许 `pending_review -> approved/rejected`。
  - `manageTables:disable` 与 `updateTableStatus` 一致，有有效预约时返回 `TABLE_HAS_ACTIVE_RESERVATION`。
  - 配置类写入继续要求 `manager` 及以上，并新增枚举、金额整数、布尔、时间范围等校验。
- 验证结果：
  - `node --check src/handlers.js` 通过。
  - `node --check test/handler-flow.test.js` 通过。
  - `npm test` 通过，29/29。
- 未实测范围：未部署云函数，未连接真实云环境，未写真实云数据库，未做微信开发者工具/真机/多账号实测，未接真实微信手机号解密。
- 剩余问题：活动锁为当前云数据库实现下的短锁保护，真实高并发仍需云端联调确认；手机号授权目前是本地可测 seam；测试辅助 action 的生产隔离策略仍需后续部署前收口。
- 建议下一步负责 agent：测试工程师 Agent 对 P0 后端做回归测试；随后前端工程师 Agent 按新增 action 和分页字段接入活动审核、手机号授权、角色判断与错误态。
- 是否触发暂停条件：否。本轮未部署、未写真实云端数据、未要求账号密钥。
