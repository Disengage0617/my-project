# 预计到店时间与素材接管 QA 复验报告

日期：2026-06-14
复验时间：2026-06-14T22:42:54+08:00

## 本轮执行 agent

- 测试工程师 Agent

## 复验范围

- `miniprogram/pages/reservation/index.js`
- `miniprogram/pages/reservation/index.wxml`
- `miniprogram/pages/home/index.wxml`
- `miniprogram/pages/assistant-list/index.js`
- `miniprogram/utils/mock.js`
- `miniprogram/assets/generated/`

## 复验通过项

| 验证项 | 结果 | 说明 |
|---|---|---|
| 预约页文案 | 通过 | 已显示“预计到店时间” |
| 预约摘要 | 通过 | 已显示“预计 {{startTime}} 到店” |
| 预约页助教兜底合并 | 通过 | 真实接口返回少量助教时调用 `mergeAssistantFallbacks` 合并 mock |
| 助教列表兜底合并 | 通过 | `assistant-list` 同样调用 `mergeAssistantFallbacks` |
| 最多 8 个助教 | 通过 | 合并逻辑在 `merged.length >= 8` 时停止 |
| 助教去重 | 通过 | 以 `id/_id` 建立 `usedIds`，避免重复追加 |
| 合并行为模拟 | 通过 | 接口返回 2 条且含重复 mock id 时，最终合并为 8 条且无重复 id |
| 首页 Hero 图 | 通过 | 使用 `/assets/generated/billiards-bg-option-1.png` |
| 店内活动图 | 通过 | 使用 `/assets/generated/billiards-bg-option-2.png` |
| 本店赛事图 | 通过 | 使用 `/assets/generated/billiards-bg-option-4.png` |
| 助教图 | 通过 | 使用 `/assets/generated/assistant-coach-card.jpg` |
| 素材文件存在 | 通过 | 目标图片均存在于 `miniprogram/assets/generated/` |
| JS/JSON 静态解析 | 通过 | 未发现解析失败 |
| WXML 标签结构 | 通过 | 目标 WXML `<view>` 开闭匹配 |
| WXSS 大括号检查 | 通过 | 目标 WXSS `{}` 匹配 |
| 后端基线测试 | 通过 | `cloudfunctions/api npm test`，21/21 通过 |
| `git diff --check` | 通过 | 仅 CRLF 提示，无空白错误 |

## 失败项

- 无。

## 剩余 GUI 风险

- 当前 QA 线程无法操作微信开发者工具 GUI，未做真实截图复验。
- 需要确认首页图片在模拟器中实际加载、裁切和层级是否正确。
- 需要确认预计到店时间滚轮在真实开发者工具中交互顺滑，且文案没有换行挤压。
- 需要确认助教补齐后列表滚动、图片加载和 8 条上限展示正常。

## 自动检查可信度

- 自动检查覆盖关键字符串、素材路径、素材存在性、兜底合并行为模拟、JS/JSON 解析、WXML 标签、WXSS 大括号、`git diff --check` 和后端单测。
- 中文关键字检查已使用 Unicode 转义重跑，避免控制台编码导致误判。
- 结论可用于静态验收，但不能替代微信开发者工具截图验收。

## 结论

- 本轮修复静态复验通过。
- 不需要打回前端。
- 可继续流转，但最终验收前仍需补微信开发者工具截图。
