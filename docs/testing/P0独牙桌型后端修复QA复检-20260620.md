# P0 独牙桌型后端修复 QA 复检报告

- request_id：REQ-20260620-40张桌台初始化QA复检
- 执行 agent：测试工程师 Agent
- lane：qa
- 执行时间：2026-06-20
- 复检范围：后端 `bootstrapMvp` 40 张桌台初始化、`getAvailableTables` 独牙筛选、前端字段兼容口径。
- 边界：不跑微信开发者工具 MCP，不截图，不部署，不写真实数据库，不改业务代码。

## 1. 测试结论

本地代码与自动化测试层通过。

上一轮定位的根因“后端 `bootstrapMvp` 只初始化 A01/A02/B01，缺 C01-C08”已在本地代码层修复：当前 `bootstrapMvp` 定义 A/B/C/D/E 五组共 40 张桌台，其中 C01-C08 为 `table_type="duya"`、`table_type_name="独牙"`；`getAvailableTables` 已支持 `table_type/tableType` 服务端筛选并返回 `list/tables/items/total`。

不能写成微信开发者工具或真机已通过。用户此前看到独牙 0 张的实机场景仍需在清缓存、重新编译或连接更新后的后端数据后手动复核。

## 2. 复检步骤

1. 执行后端语法检查：`node --check cloudfunctions/api/src/handlers.js`。
2. 执行测试文件语法检查：`node --check cloudfunctions/api/test/handler-flow.test.js`。
3. 执行后端测试：在 `cloudfunctions/api` 下运行 `npm test`。
4. 静态核对 `bootstrapMvp` 五组桌台数量、桌型字段和 C01-C08 独牙字段。
5. 静态核对 `getAvailableTables` 对 `payload.table_type || payload.tableType` 的过滤，以及 `list/tables/items/total` 返回。
6. 静态核对前端 API 和预约页仍同时传递/兼容 `tableType` 与 `table_type`，不会与后端字段口径冲突。

## 3. 实际结果

- `node --check cloudfunctions/api/src/handlers.js`：通过。
- `node --check cloudfunctions/api/test/handler-flow.test.js`：通过。
- `npm test`：通过，29/29。
- `bootstrapMvp` 桌台初始化：
  - A01-A12：`table_type="standard"`，`table_type_name="普台"`，`area_name="普台区"`。
  - B01-B10：`table_type="silver"`，`table_type_name="乔氏银腿"`，`area_name="银腿区"`。
  - C01-C08：`table_type="duya"`，`table_type_name="独牙"`，`area_name="独牙区"`。
  - D01-D06：`table_type="gold"`，`table_type_name="乔氏金腿"`，`area_name="金腿区"`。
  - E01-E04：`table_type="rose_gold"`，`table_type_name="乔氏玫瑰金"`，`area_name="玫瑰金区"`。
- `getAvailableTables`：读取 `payload.table_type || payload.tableType`；按 `table.table_type === tableType` 过滤；返回 `{ list, tables, items, total }`。
- 后端测试断言已覆盖 `bootstrapMvp` 后 `billiard_table` 总数 40，且 `getAvailableTables({ table_type: "duya" })` 返回 C01-C08。
- 前端字段口径：`miniprogram/pages/reservation/index.js` 请求同时传 `tableType` 和 `table_type`；`miniprogram/services/api.js` 本地 mock 也兼容 `payload.tableType || payload.table_type`，并归一 `duya/独牙/Cxx`。

## 4. 通过证据

- 后端测试输出：`tests 29`、`pass 29`、`fail 0`。
- 静态断言输出：`STATIC_ASSERT_OK tableGroups=40 duya=C01-C08 filter=table_type/tableType frontendCompat=ok`。
- 代码旁证：
  - `cloudfunctions/api/src/handlers.js`：五组桌台定义包含 C 组 8 张 `duya/独牙`。
  - `cloudfunctions/api/test/handler-flow.test.js`：断言 40 张桌台和 C01-C08 独牙过滤结果。
  - `miniprogram/pages/reservation/index.js` 与 `miniprogram/services/api.js`：前端仍传递并兼容 `tableType/table_type`。

## 5. 未实测范围

- 未跑微信开发者工具 MCP。
- 未截图。
- 未真机扫码。
- 未真实手机号授权。
- 未部署云函数。
- 未写真实云数据库。
- 未验证用户 DevTools 当前连接的是否仍为旧云端数据或旧缓存。
- 未做最终上线验收。

## 6. 后续最小手动复核步骤

1. 用户确认本地代码已更新到包含 40 张桌台初始化修复的版本。
2. 在微信开发者工具中清缓存并重新编译小程序。
3. 从首页进入预约页。
4. 选择桌型“独牙”。
5. 预期显示 C01-C08，数量为 8 张，且不混入普台 A 组或银腿 B 组。
6. 如果仍显示 0 张，回传当前接口数据来源、控制台请求返回、是否走真实云端、是否已重新部署/初始化后端数据。

## 7. 建议下一步负责 agent

下一步负责 agent：测试工程师 Agent / Codex 中枢  
lane：qa / coordinator

建议：由中枢安排用户按最小步骤做微信开发者工具手动复核；若复核仍为 0 张，优先交后端工程师 Agent 检查真实云端是否部署新云函数、是否仍使用旧 `billiard_table` 数据或旧初始化结果。

是否触发暂停条件：否。本轮只做本地复检，不需要用户即时操作。

