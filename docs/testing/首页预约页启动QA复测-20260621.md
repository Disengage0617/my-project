# 首页预约页启动 QA 复测 - 2026-06-21

message_type: handoff
request_id: REQ-20260621-首页预约页启动QA复测-新线程
from_lane: qa
to_lane: coordinator
created_at: 2026-06-21T11:00:00+08:00
recorded_in: 当前对话 / docs/agent-dispatch-state.md / docs/worklog.md / docs/testing/首页预约页启动QA复测-20260621.md

## 测试环境

- 项目路径：`C:\Users\宋\Documents\桌球小程序`
- AppID：`wx6a196f6ebfb27596`
- 微信开发者工具 CLI：`E:\微信web开发者工具\cli.bat`
- IDE server：`http://127.0.0.1:18799`
- 微信开发者工具窗口：存在，标题为 `billiards-private-domain-mvp`
- 本轮边界：只做本地静态检查、本地 DevTools 打开/清编译缓存/编译截图；未上传、未预览、未部署、未写真实云数据库。

## 测试步骤

1. 复核前端修复报告 `docs/frontend/首页services-api路径错误修复-20260621.md`、调度状态和 `docs/worklog.md`。
2. 运行小程序静态结构检查：`check-miniprogram-static.mjs --project C:\Users\宋\Documents\桌球小程序`。
3. 复核 `project.config.json` 与 `miniprogram/app.json`，确认 `miniprogramRoot=miniprogram/`，首页和预约页均在 `pages` 注册。
4. 运行 JS 语法检查：`node --check` 覆盖首页、预约页、`services/api.js`、`utils/mock.js`、`utils/constants.js`。
5. 定向检查首页/预约页绝对 require 目标：`/services/api.js`、`/utils/mock.js`、`/utils/constants.js` 文件均存在。
6. 使用 DevTools CLI 执行 `cache --clean compile --project C:\Users\宋\Documents\桌球小程序`，再 `open --project C:\Users\宋\Documents\桌球小程序`。
7. 使用 WeChat DevTools 本地截图验收工具尝试路由首页和预约页；automator 端口可启动，但页面路由超时，退回 GUI 截图。

## 通过项

- 本地结构检查通过：`pagesChecked=18`，`ok=true`，`failures=[]`。
- `miniprogram/app.json` 中存在 `pages/home/index` 和 `pages/reservation/index`。
- 首页和预约页 `.js/.wxml/.wxss/.json` 文件齐全。
- `miniprogram/services/api.js`、`miniprogram/utils/mock.js`、`miniprogram/utils/constants.js` 文件存在。
- JS 语法检查通过，无输出。
- 定向启动检查输出：`TARGET_STARTUP_CHECK_OK pages=2 targets=3 absoluteRequires=4`。
- DevTools CLI 登录状态正常，返回 `{"login":true}`。
- DevTools CLI 清理编译缓存成功，返回 `cleancache`。
- DevTools CLI 打开项目成功，返回 `open`。

## 失败项

- DevTools 真实窗口截图显示首页仍未正常启动，模拟器停在首页空白态。
- Console 仍有启动级模块解析错误：
  - `Error: module 'pages/home/services/api.js' is not defined, require args is '/services/api.js'`
  - `Error: SystemError (appServiceSDKScriptError) module 'pages/home/services/api.js' is not defined, require args is '/services/api.js'`
  - `Error: MiniProgramError module 'pages/home/services/api.js' is not defined, require args is '/services/api.js'`
- 本轮没有看到新的 `Page "pages/reservation/index" has not been registered yet` 明确报错；但因为首页顶层 require 仍失败，且 DevTools 自动路由 `pageRoutingOk=false`，预约页启动未能被真实确认。

## 证据

- DevTools 截图：`docs/testing/screenshots/首页预约页启动QA复测-20260621/02-reservation.png`
- 截图说明：文件名来自预约用例，但工具实际 `pageRoutingOk=false`，截图中底部页面路径仍为 `pages/home/index`，因此不能当作预约页通过证据；它能证明当前 DevTools 编译后首页仍报 `/services/api.js` 解析失败。

## 未实测范围

- 未真实打开并确认预约页渲染成功。
- 未验证真实预约提交链路、我的预约、预约详情、我已到店。
- 未真机、体验版、多账号、真实手机号授权验证。
- 未执行上传、预览、部署、真实云数据库写入。

## 结论

本轮 QA 不通过，不能进入验收官收口。

本地静态证据显示文件存在、页面注册存在、语法无误；但微信开发者工具真实编译截图仍报 `services/api.js` 模块解析失败。当前修复方向需要回前端工程师 Agent 继续处理小程序 DevTools 对 `require("/services/api.js")` 的解析问题。

## 建议下一步

- 下一步负责 agent：前端工程师 Agent
- lane：frontend
- 建议任务：将首页/预约页启动级 require 调整为微信开发者工具可识别的写法，并重新由 QA 清编译缓存后复测。
- 是否触发暂停条件：否。当前是代码级启动问题，应回前端修复；不需要用户提供截图或授权。
