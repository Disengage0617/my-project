# P0 预约 mock 状态闭环修复 - 2026-06-20

message_type: handoff
request_id: REQ-20260620-P0预约mock状态闭环修复
from_lane: frontend
to_lane: coordinator
created_at: 2026-06-20T23:51:38+08:00
recorded_in: 当前对话 / docs/worklog.md / docs/agent-dispatch-state.md / docs/testing/P0预约提交链路QA-20260620.md / docs/frontend/P0预约mock状态闭环修复-20260620.md

## 修复内容

- 修复 `createReservation` mock 只返回对象、不写回 `mock.reservations` 的问题。
- 新增本地 mock 预约构造逻辑：兼容 `tableId/table_id`、`assistantId/assistant_id`、`startTime/start_time`、`endTime/end_time`、`estimatedAmount/estimated_amount` 等字段。
- 新创建预约会写入 `mock.reservations`，并补齐 `id/_id/reservation_id`、桌台摘要、助教摘要、时间展示、预计金额、备注和状态。
- `submitArrival/confirmArrival/markNoShow/openTable/closeTable` 改为更新同一份 `mock.reservations` 状态，详情和列表刷新可读到新状态。
- 未改页面 UI、后端、Web 后台、真实数据库或部署配置。

## 改动文件

- `miniprogram/services/api.js`
- `docs/frontend/P0预约mock状态闭环修复-20260620.md`
- `docs/worklog.md`

## 接口字段变化

- mock 本地返回体新增/补齐 `id/_id/reservation_id`，便于详情、列表和状态动作共用同一 ID。
- mock 新预约同时保留 camel/snake 字段：`storeId/store_id`、`tableId/table_id`、`assistantId/assistant_id`、`durationHours/duration_hours`、`peopleCount/people_count`、`estimatedAmount/estimated_amount`。
- mock 状态动作继续兼容 `reservationId/reservation_id`。

## 验证结果

- `node --check miniprogram/services/api.js`：通过。
- `node --check miniprogram/utils/mock.js`：通过。
- `node --check miniprogram/pages/reservation-detail/index.js`：通过。
- `node --check miniprogram/pages/my-reservations/index.js`：通过。
- 复跑 QA 失败的 mock action chain：通过。

关键输出：

```json
{
  "duyaCount": 8,
  "createdInDetail": true,
  "createdInMyReservations": true,
  "arrivalStatus": "pending_verify",
  "detailStatusAfter": "pending_verify",
  "listStatusRefreshed": true,
  "ok": true
}
```

- `git diff --check -- miniprogram/services/api.js miniprogram/utils/mock.js miniprogram/pages/reservation-detail miniprogram/pages/my-reservations docs/frontend docs/worklog.md`：通过，无空白错误；仅有既有 LF/CRLF 换行提示。

## 未实测范围

- 未在微信开发者工具真实页面点击创建预约。
- 未真机/体验版。
- 未调用真实云端 action。
- 未写真实数据库。
- 未验证真实账号、手机号授权、多账号/多角色权限。

## 剩余问题

- mock 闭环已通过，但不能替代微信开发者工具页面点击和真实云端预约写入验证。
- 后续 QA 需要复测预约页创建预约、我的预约出现新预约、预约详情到店提交、状态刷新。
