# 独牙重复失败停止堆代码诊断 - 2026-06-20

message_type: handoff
request_id: REQ-20260620-独牙重复失败停止堆代码诊断
from_lane: backend
to_lane: coordinator
created_at: 2026-06-20T22:22:00+08:00
recorded_in: 当前对话 / docs/worklog.md / docs/backend/独牙重复失败停止堆代码诊断-20260620.md / docs/复盘与回档机制-20260620.md

消息内容：
- 失败层级判断：
  - 当前不得写“已修复/通过/放行”。用户 2026-06-20 22:17 截图仍显示预约页选择“独牙”为 0 张，优先级高于本地测试、部署包 smoke 和静态核对。
  - 最高概率层级：真实云数据库 `billiard_table` 数据层。原因是云端 `api` 代码已部署到包含 `duya` 和 `getAvailableTables` 过滤的版本，但此前所有操作都明确没有写真实 DB；若真实库仍无 C01-C08、`table_type=duya` 或 `store_id` 不匹配，页面仍会显示 0。
  - 次高概率层级：真实 `getAvailableTables` action 返回体。当前 CLI/本地 SDK 未能直接调用真实 action，无法确认真实返回是空数组、字段不匹配，还是仍有错误码。
  - 中等概率层级：调用环境/缓存不一致。用户当前 DevTools 可能仍在旧编译、旧云环境、旧函数实例或未刷新状态；但当前 `miniprogram/app.js` 指向 `cloud1-d2gp2ayiwab8f8a94`，项目 AppID 为 `wx6a196f6ebfb27596`，云函数 `api` 已多次查询为 Active。
  - 较低概率层级：前端实际 payload/response 映射。只读核对显示预约页会向 `getAvailableTables` 同时传 `tableType` 和 `table_type`；页面 `getList` 兼容 `list/items/tables`；`normalizeTable` 兼容 `table_type/type/table_type_name`；`matchSelectedTableType` 兼容 `duya/独牙/Cxx`。除非真实返回体字段完全异常，否则前端映射不像首要根因。
- 只读证据：
  - `docs/复盘与回档机制-20260620.md` 已要求独牙重复失败进入 `stop_coding_diagnose`，不得继续堆代码。
  - `docs/testing/api运行时修复QA复测-20260620.md` 明确：部署包结构层可说明 `Cannot find module './src/handlers'` 类问题已处理，但不能写成用户页面已通过。
  - `docs/agent-thread-map.md` 当前状态为 `blocked_user_auth / runtime_not_verified`，要求停止继续靠本地代码判断。
  - 前端调用链只读核对：
    - `miniprogram/services/api.js` 在 `wx.cloud.callFunction` 存在时调用云函数 `api`，`getAvailableTables` 不在 mock fallback 列表。
    - `miniprogram/pages/reservation/index.js` 组装 `tablePayload` 时同时包含 `storeId/store_id`、`tableType/table_type`、`startTime/start_time`、`endTime/end_time`。
    - 页面取数兼容 `res.list || res.items || res.tables || []`，并二次按 `table.typeKey/table.type` 过滤。
  - 云端/工具只读边界：
    - 微信开发者工具 CLI `cloud` 只暴露 `env`、`functions`；`functions` 只暴露 `list/info/deploy/inc-deploy/download`，没有真实 action invoke 或 DB query 子命令。
    - 本地 `wx-server-sdk` 直调 `cloud.callFunction({ name:"api", action:"getDebugContext" })` 失败在鉴权层：`INVALID_PARAM: callFunction:fail missing secretId or secretKey of tencent cloud`。
  - 回档点证据：
    - `.tmp_cloud_download_current_api_20260620`：2026-06-20 22:04:25，部署新版前的云端 `api` 下载包，根级 `handlers.js` 约 36,943 字节，旧逻辑只初始化 A01/A02/B01，不支持独牙目标。
    - `.tmp_cloud_download_after_deploy_api_20260620`：2026-06-20 22:08:57，第一次部署后的包，根级 `handlers.js` 仅 44 字节转发文件，曾触发 `./src/handlers` 风险。
    - `.tmp_cloud_download_runtime_fix_api_20260620`：2026-06-20 22:14:20，运行时入口修复后的下载包，根级 `handlers.js` 约 84,583 字节，包含 `duya`、`bootstrapMvp`、`getAvailableTables`。
- 是否建立/已有回档点：
  - 已有云函数回档点，见上述三个隔离下载目录。
  - 目前不建议为“独牙 0 张”回滚云函数：回滚到 `.tmp_cloud_download_current_api_20260620` 会恢复旧函数逻辑，仍无 C01-C08/`duya` 目标能力；回滚到 `.tmp_cloud_download_after_deploy_api_20260620` 可能恢复根级转发结构并增加运行时模块缺失风险。
  - 只有当用户页面出现更大范围云函数启动失败、且确认当前 `.tmp_cloud_download_runtime_fix_api_20260620` 包导致广泛不可用时，才考虑回滚；回滚也必须由中枢申请明确“回滚云函数授权”。
  - 数据库尚未建立 before 快照，因为本轮工具无法只读查询真实 DB；写库前必须先建立 DB before 摘要。
- 如果要修复真实 DB，需要的精确授权与 before/after 方案：
  - 第一步只读授权：允许在 `cloud1-d2gp2ayiwab8f8a94` 只读查看 `billiard_table` 集合，不写入。查询条件至少包括 `store_id=store_demo_001`，并统计总数、`table_type` 分布、`enabled` 分布、`status` 分布，逐条核对 C01-C08。
  - before 摘要必须记录：
    - `billiard_table` 中 `store_id=store_demo_001` 总数。
    - 是否存在 `_id=store_demo_001_table_C01` 到 `store_demo_001_table_C08` 或等价 `code/table_no=C01-C08`。
    - 每条 C 组记录的 `_id/code/table_no/store_id/table_type/table_type_name/enabled/status/area_name/price_cent`。
    - 当前页面实际使用的 `storeId`，如果不是 `store_demo_001`，必须按实际 store 再查一遍。
  - 第二步写入授权：仅在 before 证明缺 C01-C08 或字段错误时申请。建议最小 after：
    - 只对测试门店或用户确认门店执行。
    - upsert 缺失的 C01-C08，`store_id=store_demo_001`，`code/table_no=C01-C08`，`table_type=duya`，`table_type_name=独牙`，`area_name=独牙区`，`enabled=true`，`status=idle`，`price_cent=5800`，`sort_order=23..30`。
    - 不删除、不重置、不覆盖已有非 C 组数据；已有 C 组若字段不一致，只修正必要字段并记录 before/after。
  - 写入后验证：只读查询 C01-C08，再让用户或测试工程师复测预约页；仍不得仅凭写库命令成功写“页面通过”。
- 如果建议回档，回档到哪个云函数包/本地文件状态，影响是什么：
  - 当前不建议回档。原因：用户当前失败是“独牙 0 张”，更像数据/真实 action 返回问题；回滚云函数会丢失已部署的 `duya` 过滤和运行时入口修复，不能解决数据缺失。
  - 若必须回档云函数，优先选择 `.tmp_cloud_download_current_api_20260620` 作为“部署前旧云函数包”回档参考，但影响是恢复旧逻辑：只支持历史 3 桌初始化、不支持 C01-C08 独牙目标，用户独牙仍不可放行。
  - 本地文件回档如需执行，可将 `cloudfunctions/api/handlers.js/constants.js/rules.js` 恢复为根级转发文件；但这会重新引入 `./src/handlers` 运行时风险，不建议。
- 下一步最小动作：
  - 不写代码、不部署、不写库。
  - 由中枢获取 DB 只读入口：微信云开发控制台人工只读查看，或提供腾讯云只读密钥/工具，或用户在 DevTools 云开发数据库界面截图/导出 `billiard_table` 的只读摘要。
  - 最小只读查询目标：`billiard_table`，`store_id=store_demo_001`，C01-C08，字段 `table_type/enabled/status`。
  - 若 DB 有 C01-C08 且字段正确，再转前端工程师 Agent 要求抓真实页面 `getAvailableTables` payload/response 摘要，核对是否 storeId、响应字段或缓存异常。
- 改动文件：
  - `docs/backend/独牙重复失败停止堆代码诊断-20260620.md`
  - `docs/worklog.md`
- 验证结果：
  - 已只读核对 `docs/复盘与回档机制-20260620.md`、`docs/testing/api运行时修复QA复测-20260620.md`、`docs/agent-thread-map.md`。
  - 已只读核对 `miniprogram/services/api.js` 与 `miniprogram/pages/reservation/index.js` 的 payload/response 映射。
  - 已确认微信开发者工具 CLI 不提供真实 action invoke 或 DB query。
  - 已尝试本地 SDK 只读 callFunction，失败于缺少腾讯云 `secretId/secretKey`，未触达业务 action。
  - 待执行：`git diff --check -- docs/backend/独牙重复失败停止堆代码诊断-20260620.md docs/worklog.md`。
- 未实测范围：
  - 未查真实云数据库。
  - 未调用真实云函数 action。
  - 未抓用户 DevTools 的真实 payload/response。
  - 未执行任何修复动作，未写库、未部署、未改前端。
- 建议下一步负责 agent：
  - Codex 中枢：向用户申请 DB 只读检查入口或让用户提供 `billiard_table` 只读截图/导出。
  - 后端工程师 Agent：拿到 DB 只读入口后只读核对 C01-C08 和字段。
  - 前端工程师 Agent：仅当 DB 确认正确后，核对真实 payload/response 映射和页面缓存。
- 是否触发暂停条件：
  - 是。继续定位需要真实 DB 只读入口或真实 action 返回体；任何写库、初始化、迁移、部署或回滚都必须另行授权。

## Handoff 回传

message_type: handoff
request_id: REQ-20260620-独牙重复失败停止堆代码诊断
from_lane: backend
to_lane: coordinator
created_at: 2026-06-20T22:22:00+08:00
recorded_in: 当前对话 / docs/worklog.md / docs/backend/独牙重复失败停止堆代码诊断-20260620.md / docs/复盘与回档机制-20260620.md

消息内容：
- 失败层级判断：最高概率是真实 `billiard_table` 数据缺 C01-C08/`table_type=duya`/可用状态或 `store_id` 不匹配；次高是真实 `getAvailableTables` 返回体未确认；调用环境/缓存中等概率；前端 payload/response 映射较低概率但需真实返回体确认。
- 只读证据：前端会传 `storeId/store_id` 和 `tableType/table_type`；页面兼容 `list/items/tables` 和 `duya/独牙/Cxx`；云端函数包已有回档点；CLI 无 DB/action 只读入口；本地 SDK 直调缺 `secretId/secretKey`。
- 是否建立/已有回档点：已有 `.tmp_cloud_download_current_api_20260620`、`.tmp_cloud_download_after_deploy_api_20260620`、`.tmp_cloud_download_runtime_fix_api_20260620` 三个云函数包回档点；数据库 before 尚未建立。
- 如果要修复真实 DB，需要的精确授权与 before/after 方案：先申请 DB 只读授权并记录 `billiard_table` before 摘要；若缺 C01-C08，再申请 DB 写入授权，最小 upsert C01-C08 为 `store_demo_001` 的 `duya/独牙/enabled=true/status=idle`，不删除、不重置、不覆盖非目标数据。
- 如果建议回档，回档到哪个云函数包/本地文件状态，影响是什么：当前不建议回档；回到旧包会丢失独牙过滤和入口修复，无法解决 0 张问题。仅广泛启动失败时再申请回滚授权。
- 下一步最小动作：停止写代码，获取 DB 只读入口或用户提供 `billiard_table` 截图/导出；核对 C01-C08、`table_type=duya`、`enabled=true`、`status=idle`、`store_id`。
- 改动文件：`docs/backend/独牙重复失败停止堆代码诊断-20260620.md`、`docs/worklog.md`。
- 验证结果：只读文件核对、CLI 能力核对、SDK 只读调用边界核对完成；`git diff --check` 待最终命令确认。
- 未实测范围：未查真实 DB、未真实调用 action、未抓页面 payload/response、未写库、未部署、未改业务代码。
- 建议下一步负责 agent：Codex 中枢申请 DB 只读入口；后端工程师 Agent 做 DB 只读核对；DB 正确后再交前端工程师 Agent 查真实 payload/response。
- 是否触发暂停条件：是。继续需要真实 DB 只读入口；任何写库/迁移/部署/回滚必须另行授权。
