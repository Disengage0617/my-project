# 底部 banner 与助教交互 QA 复测 - 2026-06-21

message_type: handoff
request_id: REQ-20260621-底部banner与助教交互协作修复
from_lane: qa
to_lane: coordinator
created_at: 2026-06-21T12:03:00+08:00
recorded_in: 当前对话 / docs/worklog.md / docs/testing/底部banner与助教交互QA复测-20260621.md

## 测试环境

- 项目路径：`C:\Users\宋\Documents\桌球小程序`
- 本轮边界：只读复测；未改业务代码；未上传、未预览、未部署；未写真实云数据库。
- 微信开发者工具截图规则：只截用户当前已打开窗口，不自行用 MCP/CLI 新开或重开项目，不用静态检查替代真实页面验收。

## 输入材料

- `docs/frontend/底部banner与助教交互修复-20260621.md`
- `miniprogram/custom-tab-bar/index.js`
- `miniprogram/pages/assistant-list/index.js`
- `miniprogram/pages/assistant-list/index.wxml`
- `miniprogram/pages/assistant-list/index.wxss`
- `miniprogram/pages/assistant-booking/index.js`
- `miniprogram/pages/mine/index.js`
- `miniprogram/app.json`

## 测试步骤

1. 复核前端工程师 Agent handoff。
2. 静态复核 `app.json` 是否启用 `tabBar.custom=true`、5 个 tab 页路径和图标资源是否存在。
3. 静态复核 `custom-tab-bar/index.js` 的路由 selected 同步、点击切 tab 和失败回滚逻辑。
4. 静态复核助教页顶部筛选 tab 的绑定、选中态、筛选函数、空态和助教卡片预约跳转。
5. 静态复核助教预约页 query id 安全解码。
6. 静态复核我的页 tabBar selected 同步。
7. 跑小程序结构检查、JS 语法、JSON parse、WXML 标签计数、定向交互断言、残留 require 检查。
8. 跑本地 mock 预约/已到店链路，确认未明显回退。
9. 截取当前已打开 DevTools 窗口并目检实际证据可信度。

## 通过项

- 小程序结构检查通过：`pagesChecked=18`、`ok=true`、`failures=[]`。
- JS 语法检查通过，覆盖 `custom-tab-bar/index.js`、首页、预约页、订单页、助教列表、助教预约、我的页、公共 `services/api.js`、`utils/mock.js`、`utils/constants.js`。
- JSON parse 通过：`JSON_OK 21`。
- WXML 标签检查通过：`WXML_TAGS_OK 7`。
- tabBar 和助教交互定向断言通过：`TABBAR_ASSISTANT_ASSERT_OK tabs=5`。
- 残留 require 检查通过：`RESIDUAL_REQUIRE_OK`，未检出 `pages/home/services`、`pages/reservation/services`、`require("/services`、`require("./services` 等旧路径残留。
- `app.json` 已启用 `tabBar.custom=true`，5 个 tab 页均注册，10 个 tabBar 图标资源存在。
- `custom-tab-bar/index.js` 包含 5 个 tab 页路径，包含 `attached`、`pageLifetimes.show`、`updateSelectedByRoute`、`switchTab`、失败回滚。
- 助教页顶部标签已是可交互筛选：WXML 有 `bindtap="switchCoachTab"`，JS 有 `filterAssistants` 和 `filteredAssistants`。
- 助教卡片预约跳转具备 id 校验、`encodeURIComponent(id)` 和 `navigateTo.fail` 提示。
- 助教预约页读取 query id 时使用 `safeDecode` / `decodeURIComponent`。
- 我的页 `onShow` 包含 `setTabBarSelected(this, 4)`。
- 本地 mock 预约/已到店链路通过：输出 `{"ok":true,"tableCode":"C01","detailFound":true,"listFound":true,"arrivalStatus":"pending_verify"}`。

## 失败项

- 未发现明确代码级失败项。
- 当前 DevTools 截图不能作为真实页面验收通过证据：截图中 DevTools 被外部视频/直播窗口大面积遮挡，只露出右侧一小段模拟器；实际可辨认为 `pages/home/index` 的局部画面，不是助教页，也看不到完整底部 tabBar 交互过程和 Console。

## 未实测范围

- 底部 custom tabBar 的 5 个 tab 在真实微信开发者工具内逐个点击切换未实测。
- 5 个 tab 的真实选中态同步未实测。
- 助教页顶部 `职业认证 / 擅长打法 / 可约时间` 在真实微信开发者工具内点击、选中态、筛选/排序/空态未实测。
- 助教卡片预约按钮真实跳转到 `assistant-booking` 并携带正确 id 未实测。
- 真实微信页面的预约功能和已到店链路本轮未复测；仅本地 mock 链路验证未回退。
- Console 是否仍有业务 JS 报错或 `SystemError timeout` 本轮未能可靠观察，因为当前 DevTools 被遮挡且 Console 不可见。
- 未真机、体验版、多账号验证。

## 截图和证据来源

- 当前窗口截图：`docs/testing/screenshots/current-open-devtools-tabbar-assistant-qa-20260621.png`
- 证据可信度：低。该截图可证明当前确有 DevTools 窗口，但无法证明底部 tabBar/助教页交互通过。

## 结论

本轮 QA 静态与本地 mock 复测通过，未发现需要回派前端工程师 Agent 的明确代码级问题。

真实微信开发者工具页面验收未通过确认：当前窗口不是可用于验收的目标业务页状态，且被外部窗口遮挡。不能把本地静态检查、mock 链路或被遮挡截图冒充真实页面通过。

## 建议下一步

- 下一步负责 agent：测试工程师 Agent / 用户配合
- lane：qa
- 需要用户手动操作：
  1. 关闭或移开遮挡 DevTools 的外部窗口。
  2. 在当前已打开的微信开发者工具中进入首页，截图底部 custom tabBar。
  3. 依次点击 5 个底部 tab，至少截图“助教”页和“我的”页，带 Console。
  4. 在助教页点击 `职业认证 / 擅长打法 / 可约时间`，截图选中态和列表变化。
  5. 点击一个助教卡片的“预约”，截图跳转后的 `assistant-booking` 页面和页面路径。
- 是否触发暂停条件：是。需要用户把当前 DevTools 切到目标业务页并提供无遮挡截图；本轮不建议进入验收官。
