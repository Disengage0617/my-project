# 独牙运行时数据源只读定位 - 2026-06-20

message_type: handoff
request_id: REQ-20260620-独牙运行时数据源只读定位
from_lane: backend
to_lane: coordinator
created_at: 2026-06-20T当前时间+08:00
recorded_in: 当前对话 / docs/backend/独牙运行时数据源只读定位-20260620.md / docs/worklog.md

消息内容：
- 定位结论：用户当前微信开发者工具运行时最不像本地 mock；更可能在读 `cloud1-d2gp2ayiwab8f8a94` 上的真实云函数 `api` 与真实云数据库旧数据，或读到旧版云函数/编译缓存。当前只读文件证据无法区分“云函数未部署最新版”和“云数据库仍是 3 桌旧数据”，但两者都能解释独牙为 0 张。
- 证据：
  - `miniprogram/app.js` 启动时执行 `wx.cloud.init({ env: "cloud1-d2gp2ayiwab8f8a94", traceUser: true })`，运行环境默认指向真实云环境。
  - `miniprogram/services/api.js` 在 `wx.cloud.callFunction` 存在时优先调用云函数 `api`，payload 为 `{ action, payload }`；只有 `getMineCenter` 在 `ACTION_NOT_FOUND` 时会 fallback 到 mock。
  - `getAvailableTables` 不在 `READONLY_MOCK_FALLBACK_ACTIONS` 中，预约页独牙数据不会因云端缺失自动降级到 mock。
  - `miniprogram/pages/reservation/index.js` 选择桌型时会把 `tableType/table_type` 传给 `getAvailableTables`，之后还会做页面二次过滤；如果真实云端只返回 A/B 组或空数组，页面会显示“当前筛选暂无桌台”。
  - `miniprogram/utils/mock.js` 已包含 40 张桌台和 C01-C08 独牙；若当前运行时读的是 mock，独牙不应为 0 张。
  - 当前本地 `cloudfunctions/api/src/handlers.js` 已包含 C01-C08 初始化和 `getAvailableTables` 桌型过滤，但用户运行时仍为 0 张，说明本地代码层变化尚未在 DevTools 当前数据源生效。
  - `cloudfunctions/api/scripts/seed.js` 已具备 40 张完整种子数据，说明完整初始化方案存在；但是否已写入真实云库，本轮未授权检查。
  - `docs/testing/预约闭环MVP阶段验收报告.md` 明确历史真实云端验收采用“最小 3 桌口径”，并提示进入完整试运营需切换到 `seed.js` 的 40 桌完整口径。
  - `docs/backend-cloud-deploy-plan.md` 和 `docs/backend/真实云端联调部署前清单-20260620.md` 均说明部署云函数、执行真实云端 smoke、执行 `seed/bootstrapMvp` 写入测试数据都需要用户授权；目前没有本轮授权执行这些动作。
- 当前最可能原因：真实云数据库 `billiard_table` 仍保留历史 MVP 最小 3 桌数据，或云函数 `api` 仍是旧版代码；用户 DevTools 因 `wx.cloud.callFunction` 走真实云端，不走包含 40 桌的本地 mock。若开发者工具未清缓存或未重新编译，也可能继续加载旧编译产物，但该可能性低于真实云端旧函数/旧数据。
- 仍不能确定的项：
  - 当前云端 `api` 函数是否已经部署到包含 40 桌初始化和 `getAvailableTables` 筛选的新版本。
  - 当前 `cloud1-d2gp2ayiwab8f8a94` 的 `billiard_table` 集合中是否只有 A01/A02/B01，还是已有 40 张但字段不一致。
  - 用户 DevTools 当前是否打开了正确项目、正确 AppID、正确云环境。
  - 开发者工具是否存在旧编译缓存、未重新编译、未清缓存。
  - 用户当前页面请求 `getAvailableTables` 返回的真实 payload/result 内容。
- 需要用户授权：
  - 只读云端检查授权：需要用户允许在 `cloud1-d2gp2ayiwab8f8a94` 上只读调用 `getDebugContext`、`getAvailableTables({ store_id:"store_demo_001", table_type:"duya" })`、`getAvailableTables({ store_id:"store_demo_001" })`，或只读查看 `billiard_table` 集合数量和 C01-C08 是否存在。
  - 部署云函数授权：若只读检查发现云函数旧版或 `getAvailableTables` 不支持桌型筛选，需要用户明确允许上传并覆盖云函数 `api`，选择云端安装依赖。
  - 初始化或迁移测试数据授权：若只读检查发现真实库缺 C01-C08 或仍是 3 桌，需要用户明确允许执行受控初始化/迁移，把 `store_demo_001` 的桌台补齐到 40 张；该动作会写真实云数据库，不能默认执行。
- 改动文件：`docs/backend/独牙运行时数据源只读定位-20260620.md`、`docs/worklog.md`。
- 验证结果：本轮只读核对 `miniprogram/services/api.js`、`miniprogram/pages/reservation/index.js`、`miniprogram/utils/mock.js`、`miniprogram/app.js`、`project.config.json`、`project.private.config.json`、`cloudfunctions/api/src/handlers.js`、`cloudfunctions/api/scripts/seed.js`、`docs/backend-cloud-deploy-plan.md`、`docs/backend/真实云端联调部署前清单-20260620.md`、`docs/agent-thread-map.md`、`docs/worklog.md` 和相关 QA 报告；未部署、未连云、未写库。
- 剩余问题：没有真实云端只读结果前，不能判断到底是旧函数、旧数据还是 DevTools 缓存；也不能声称独牙问题已修复。
- 建议下一步负责 agent：Codex 中枢先向用户申请“只读云端检查授权”；授权后由后端工程师 Agent 做只读云端定位。若确认旧函数，再申请部署授权；若确认旧数据，再申请初始化/迁移测试数据授权。
- 是否触发暂停条件：是。继续定位必须进入真实云端只读检查；部署或补齐云数据库更是高风险授权项。本轮已暂停在授权边界内，未执行。

## 分层判断

| 可能数据源 | 当前判断 | 依据 |
|---|---|---|
| 本地 mock | 低概率 | mock 已包含 C01-C08；若读 mock，独牙应返回 8 张。 |
| 本地云函数代码 | 低到中概率 | 本地代码已补 40 张和桌型筛选，但 DevTools 是否使用本地代码未确认。 |
| 真实云端旧函数 | 中高概率 | `api.call` 优先 `wx.cloud.callFunction`，部署计划仍要求授权上传 `api`。 |
| 真实云数据库旧数据 | 最高概率之一 | 历史真实云端 MVP 验收采用 3 桌最小口径；用户现象与缺 C 组高度一致。 |
| 缓存/编译产物 | 中概率 | DevTools 多次手动复核仍失败，可能未部署/未重新编译/缓存旧包；但文件证据无法确认。 |

## 下一步只读检查建议

获得用户“只读云端检查授权”后，只做读取，不写库：

1. 确认当前小程序环境：`getDebugContext` 返回 `env/appid/openid`。
2. 调用 `getAvailableTables` 全量：确认总数、桌型分布、是否存在 C01-C08。
3. 调用 `getAvailableTables({ table_type: "duya" })`：确认是否返回 C01-C08。
4. 如允许查看云数据库，只读查询 `billiard_table`：统计 `store_id=store_demo_001` 总数和 `table_type=duya` 数量。
5. 只记录结果，不执行 `bootstrapMvp`、`seed`、`manageTables` 或任何写 action。

## Handoff 回传

message_type: handoff
request_id: REQ-20260620-独牙运行时数据源只读定位
from_lane: backend
to_lane: coordinator
created_at: 2026-06-20T当前时间+08:00
recorded_in: 当前对话 / docs/backend/独牙运行时数据源只读定位-20260620.md / docs/worklog.md

消息内容：
- 定位结论：用户当前运行时更可能读真实云端旧函数或真实云数据库旧数据，不像读本地 mock；目前不能在无云端只读授权下进一步区分。
- 证据：见本文“证据”和“分层判断”。
- 当前最可能原因：`cloud1-d2gp2ayiwab8f8a94` 的真实云端仍是历史 MVP 3 桌数据或旧版 `api` 云函数，导致 `getAvailableTables` 返回中无 C01-C08。
- 仍不能确定的项：云函数版本、真实库桌台数量、DevTools 当前环境/缓存、真实接口返回体。
- 需要用户授权：需要，分三档：只读云端检查授权、部署云函数授权、初始化或迁移测试数据授权。
- 改动文件：`docs/backend/独牙运行时数据源只读定位-20260620.md`、`docs/worklog.md`。
- 验证结果：只读文件核对完成；未部署、未连云、未写库；`git diff --check` 待最终命令确认。
- 剩余问题：必须拿到真实云端只读证据后才能进入修复判断；不得再用本地测试通过关闭问题。
- 建议下一步负责 agent：Codex 中枢申请只读云端检查授权，授权后交后端工程师 Agent 执行。
- 是否触发暂停条件：是，继续定位需要真实云端只读授权；部署和数据修复需要更高等级授权。
