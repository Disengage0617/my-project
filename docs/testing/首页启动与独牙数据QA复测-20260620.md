# 首页启动与独牙数据 QA 复测 - 2026-06-20

message_type: handoff
request_id: REQ-20260620-首页启动与独牙数据QA复测
from_lane: qa
to_lane: coordinator
created_at: 2026-06-20T23:08:02+08:00
recorded_in: 当前对话 / docs/worklog.md / docs/testing/首页启动与独牙数据QA复测-20260620.md / docs/agent-dispatch-state.md

## QA 结论

本地静态与只读自动化层通过；微信开发者工具真实业务页未可靠验收，不能放行给验收官。

- 首页启动错误：本地静态层确认 `getDefaultHeroSlides()` 已定义，`data.heroSlides` / `data.activeHeroSlide` 引用可解析，`normalizePageConfig(config)` 已有空对象兜底；未发现继续触发 `getDefaultHeroSlides is not defined` 的静态风险。
- 独牙数据链路：用户已提供真实 DB 控制台证据 `[C_RESULT] { count: 8 }`，说明 C01-C08 已写入；本地静态层确认预约页同时传 `tableType/table_type`，兼容 `duya/独牙/Cxx`，并按 `enabled !== false` 与 `idle/free/available/空闲` 判断可约；后端 `getAvailableTables` 查询 `enabled=true` 并支持 `table_type/tableType` 过滤。
- DevTools 页面验收：已尝试 MCP automator 只读路由截图，但 `pageRoutingOk=false`，不能证明首页不空白，也不能证明预约页独牙显示 C01-C08。

因此，本轮只能结论为“本地静态口径通过，真实页面仍需用户重新编译后手动复测并回传截图/控制台结果”。

## 已验证层级

1. 文档复核：
   - `docs/frontend/首页getDefaultHeroSlides运行时错误修复-20260620.md` 已记录前端修复内容、验证命令和剩余实机确认点。
   - `docs/agent-dispatch-state.md` 已记录当前 active request、用户 C01-C08 写入证据和首页启动错误背景。
2. 代码静态复核：
   - `miniprogram/pages/home/index.js` 存在 `function getDefaultHeroSlides()`。
   - `miniprogram/pages/home/index.js` 的 `data.heroSlides`、`data.activeHeroSlide` 调用目标已定义。
   - `normalizePageConfig(config)` 已补 `config = config || {};`。
   - `miniprogram/pages/reservation/index.js` 请求 `getAvailableTables` 时同时传 `tableType` 和 `table_type`。
   - 预约页桌型归一包含 `duya`、`独牙`、C 组编号前缀。
   - 预约页可约判断兼容 `enabled !== false` 与 `idle/free/available/空闲`。
   - `cloudfunctions/api/handlers.js` 包含 C 组 `table_type: "duya"`、`table_type_name: "独牙"`，并按 `{ store_id: storeId, enabled: true }` 查询桌台。
3. 本地命令：
   - `node --check miniprogram/pages/home/index.js`：通过。
   - `node --check miniprogram/pages/reservation/index.js`：通过。
   - `node --check cloudfunctions/api/handlers.js`：通过。
   - 定向静态断言输出：`STATIC_ASSERT_OK homeDefaultHero=defined reservationDuyaIdleEnabled=compatible backendDuya=present`。
   - WXML 基础标签计数：
     - `miniprogram/pages/home/index.wxml`：`WXML_VIEW_OK`。
     - `miniprogram/pages/reservation/index.wxml`：`WXML_VIEW_OK`。
4. 微信开发者工具 MCP 尝试：
   - 工具可读取正确项目：`projectRoot=C:\Users\宋\Documents\桌球小程序`，AppID `wx6a196f6ebfb27596`，`miniprogramRoot=miniprogram/`。
   - 页面清单包含 `pages/home/index` 和 `pages/reservation/index`。
   - automator 启动成功，但批量页面路由超时，返回 `ok=false`、`pageRoutingOk=false`。
   - 本轮未采信 MCP 截图为业务页通过证据。

## 未实测范围

- 未可靠完成微信开发者工具业务页截图验收。
- 未确认首页重新编译后是否不再空白。
- 未确认预约页选择“独牙”后是否显示 C01-C08。
- 未真机复测。
- 未真实页面点击提交预约。
- 未写真实数据库。
- 未部署。
- 未做 UI 风格验收。
- 未给最终上线验收结论。

## 是否需要用户手动重新编译/截图

需要。

最小复测步骤：

1. 在微信开发者工具中清缓存并重新编译。
2. 确认首页能正常显示，不再空白，控制台不再出现 `getDefaultHeroSlides is not defined`。
3. 从首页进入预约页。
4. 选择桌型“独牙”。
5. 预期显示 C01-C08，共 8 张；不混入普台 A 组、银腿 B 组。
6. 截图回传：
   - 首页正常显示截图。
   - 预约页选择“独牙”后的桌台列表截图。
   - 如失败，回传控制台错误和 `getAvailableTables` 返回体摘要。

## 失败回传要求

若仍失败，请回传以下信息：

1. 首页是否空白，以及控制台完整错误文本。
2. 是否仍出现 `getDefaultHeroSlides is not defined`。
3. 预约页选择“独牙”后页面文案、桌台数量和截图。
4. `getAvailableTables` 返回体摘要：`ok`、`code/message`、`total`、`items/list/tables` 前几项的 `table_no/table_type/enabled/status`。
5. 是否已清缓存并重新编译。

## 建议下一步负责 agent

下一步负责 agent：用户手动复测 / 测试工程师 Agent / 验收官 Agent  
lane：qa / reviewer

- 用户回传首页和独牙截图后，测试工程师 Agent 记录真实页面结果。
- 若截图显示首页正常且独牙 C01-C08 可见，再交验收官 Agent 做阶段复核。
- 若首页仍空白，回派前端工程师 Agent。
- 若首页正常但独牙仍 0 张，回派后端工程师 Agent 核对真实云函数返回与真实 DB 数据。

是否触发暂停条件：是。真实页面复测必须用户在微信开发者工具中重新编译并截图，当前不能写页面通过。

