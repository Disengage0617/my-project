# 独牙云端 api 隔离下载核对 - 2026-06-20

message_type: handoff
request_id: REQ-20260620-独牙云端api隔离下载核对
from_lane: backend
to_lane: coordinator
created_at: 2026-06-20T当前时间+08:00
recorded_in: 当前对话 / docs/backend/独牙云端api隔离下载核对-20260620.md / docs/worklog.md

消息内容：
- 下载命令/结果：
  - 已先查看 `cloud functions download --help`，确认支持 `--env`、`--name`、`--path`。
  - 首次使用相对路径 `.tmp_cloud_download_current_api_20260620` 下载失败，CLI 将相对路径解析到 `E:\微信web开发者工具\` 安装目录并因 EPERM 拒绝；未写云端、未覆盖本地业务代码。
  - 使用绝对路径重新执行成功：

```text
& 'E:\微信web开发者工具\cli.bat' cloud functions download --project 'C:\Users\宋\Documents\桌球小程序' --appid wx6a196f6ebfb27596 --env cloud1-d2gp2ayiwab8f8a94 --name api --path 'C:\Users\宋\Documents\桌球小程序\.tmp_cloud_download_current_api_20260620' --lang zh
结果：√ 下载云函数
```

- 下载目录：
  - `C:\Users\宋\Documents\桌球小程序\.tmp_cloud_download_current_api_20260620`
  - 顶层文件/目录包括：`index.js`、`handlers.js`、`constants.js`、`rules.js`、`package.json`、`src/`、`scripts/`、`test/`、`node_modules/`。
- 云端源码核对结论：
  - 当前云端 `api` 源码是旧版。
  - 下载后的 `.tmp_cloud_download_current_api_20260620/src/handlers.js` 中，`bootstrapMvp` 只初始化 3 张桌台：`A01`、`A02`、`B01`。
  - 下载后的 `.tmp_cloud_download_current_api_20260620/src/handlers.js` 中，`bootstrapMvp` 未包含 C01-C08、未包含 `duya`、未写入 `table_type: "duya"`。
  - 下载后的 `.tmp_cloud_download_current_api_20260620/src/handlers.js` 中，`getAvailableTables` 只查询 `{ store_id: storeId, enabled: true }` 并返回 `{ list }`，未读取 `payload.table_type || payload.tableType`，未做服务端桌型过滤，也未返回 `tables/items/total`。
  - 下载包的 `scripts/seed.js` 虽包含 40 张桌台和 `duya` 种子配置，但这不等于云端 `api` handler 当前已具备预约页所需的 40 桌初始化和桌型过滤能力。
- 和本地源码差异：
  - 本地 `cloudfunctions/api/src/handlers.js` 已包含 `TABLE_TYPES = ["standard", "silver", "duya", "gold", "rose_gold"]`。
  - 本地 `bootstrapMvp` 已按 A/B/C/D/E 五组初始化 40 张桌台，其中 C 组 8 张为 `table_type: "duya"`、`table_type_name: "独牙"`。
  - 本地 `getAvailableTables` 已兼容 `payload.table_type || payload.tableType`，并按 `table.table_type` 过滤，返回 `{ list, tables, items, total }`。
  - 因此，用户当前 DevTools 选择“独牙”仍 0 张的后端侧主因已确认：真实云端 `api` 尚未部署当前本地新版 handler。
- 需要部署授权：
  - 需要。若要让用户当前真实云端运行时读取到 C01-C08 和 `table_type/tableType` 过滤，必须获得明确“部署云函数授权”，将本地 `cloudfunctions/api` 当前版本上传并覆盖云端 `api`。
  - 本轮未部署、未上传、未覆盖云函数。
- 需要 DB 只读/写入授权：
  - 仍需要 DB 只读授权：部署新版函数后，仍需只读确认真实 `billiard_table` 是否已有 40 张、C01-C08、`table_type=duya`、`status=idle/free`、`enabled=true`。旧函数已确认，但旧数据仍未排除。
  - 可能需要 DB 写入授权：如果只读查询确认真实库仍只有 A01/A02/B01 或缺 C01-C08，则需要单独申请“初始化或迁移测试数据授权”，受控补齐 40 张桌台。
  - 本轮未查真实 DB、未写真实 DB、未执行 `bootstrapMvp` 或 `seed.js`。
- 改动文件：
  - `docs/backend/独牙云端api隔离下载核对-20260620.md`
  - `docs/worklog.md`
  - 新增隔离下载目录：`.tmp_cloud_download_current_api_20260620/`
- 验证结果：
  - `cloud functions download` 到绝对隔离目录：通过。
  - `rg`/`Select-String` 核对下载源码关键字：下载的 handler 仅命中 `bootstrapMvp`、`getAvailableTables`、`table_type_name`；未命中 `duya`、`C01`、`C08`、`tableType`。
  - 核对下载源码片段：`bootstrapMvp` 只含 A01/A02/B01；`getAvailableTables` 未按桌型过滤。
  - 核对本地源码片段：本地 handler 已含 C 组 `duya` 和 `getAvailableTables` 桌型过滤。
  - `node --check .tmp_cloud_download_current_api_20260620\src\handlers.js`：通过，无输出。
  - `node --check cloudfunctions\api\src\handlers.js`：通过，无输出。
  - 待执行：`git diff --check -- docs/backend/独牙云端api隔离下载核对-20260620.md docs/worklog.md`。
- 未实测范围：
  - 未部署新版云函数。
  - 未调用真实 `getAvailableTables({ table_type: "duya" })`。
  - 未查询真实云数据库 `billiard_table`。
  - 未验证微信开发者工具页面返回体、缓存和真机表现。
- 建议下一步负责 agent：
  - Codex 中枢：向用户申请“部署云函数授权”，说明会上传并覆盖云环境 `cloud1-d2gp2ayiwab8f8a94` 的 `api` 云函数，但不写数据库。
  - 后端工程师 Agent：获授权后部署 `cloudfunctions/api`，部署后做只读 smoke；若 smoke 仍缺 C01-C08，再申请 DB 只读/迁移授权。
  - 测试工程师 Agent：部署和必要数据补齐后，复测 DevTools 预约页“独牙”筛选。
- 是否触发暂停条件：
  - 是。当前已确认需要部署云函数才能让真实云端运行本地新版 handler；部署是高风险操作，必须另行授权。本轮没有执行部署或任何写库动作。

## Handoff 回传

message_type: handoff
request_id: REQ-20260620-独牙云端api隔离下载核对
from_lane: backend
to_lane: coordinator
created_at: 2026-06-20T当前时间+08:00
recorded_in: 当前对话 / docs/backend/独牙云端api隔离下载核对-20260620.md / docs/worklog.md

消息内容：
- 下载命令/结果：相对路径下载因 CLI 解析到安装目录被 EPERM 拒绝；绝对路径下载成功，结果 `√ 下载云函数`。
- 下载目录：`.tmp_cloud_download_current_api_20260620/`。
- 云端源码核对结论：当前云端 `api` 是旧版；handler 只初始化 A01/A02/B01，未包含 C01-C08/`duya`，`getAvailableTables` 不按 `table_type/tableType` 过滤。
- 和本地源码差异：本地新版已含 40 桌、C01-C08 `duya`、`getAvailableTables` 服务端过滤和 `list/tables/items/total` 返回；云端下载版缺这些关键逻辑。
- 需要部署授权：需要，下一步若要修复真实云端运行时，必须授权上传并覆盖云函数 `api`。
- 需要 DB 只读/写入授权：仍需要 DB 只读确认真实桌台数据；如真实库缺 C01-C08，再单独申请初始化或迁移测试数据写入授权。
- 改动文件：`docs/backend/独牙云端api隔离下载核对-20260620.md`、`docs/worklog.md`、`.tmp_cloud_download_current_api_20260620/`。
- 验证结果：下载成功；关键字/源码片段核对完成；下载版和本地版 `node --check` 均通过；`git diff --check` 待最终命令确认。
- 未实测范围：未部署、未写库、未查真实 DB、未调用真实 `getAvailableTables`、未跑 DevTools 页面复测。
- 建议下一步负责 agent：Codex 中枢申请部署云函数授权；获授权后后端工程师 Agent 执行部署和只读 smoke；再由测试工程师 Agent 复测。
- 是否触发暂停条件：是，部署云函数和任何数据修复都必须另行授权。
