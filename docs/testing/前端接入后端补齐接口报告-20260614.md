# 前端接入后端补齐接口报告

日期：2026-06-14

## 本轮执行 agent

- 前端工程师 Agent

## 接入页面

- `miniprogram/pages/merchant-dashboard/`
- `miniprogram/pages/merchant-config/`
- `miniprogram/pages/table-grid/`
- `miniprogram/pages/no-show-locks/`
- `miniprogram/pages/assistant-booking/`
- `miniprogram/pages/campaign-registration/`
- `miniprogram/pages/reservation-detail/`

## 接口调用点

- 商家后台概览：`getStoreOverview`、`getCustomers`、`getCampaignRegistrations`、`getNoShowRecords`
- 配置中心只读检查：`getStoreDetail`、`manageTables(action=list)`、`getPriceRules`、`getAssistants`、`getCampaigns`、`getCustomers`、`getCampaignRegistrations`
- 桌台状态：`getAvailableTables`、`updateTableStatus`
- 活动报名：`createCampaignRegistration`
- 助教预约：`getAssistantDetail`、`createAssistantReservation`
- 客户沉淀：`getCustomers`、`getCustomerProfile` 已补前端 api 降级支持
- 爽约限制：`getNoShowRecords`、`liftNoShowBan`
- 预约详情：`getReservationDetail`

## 关键处理

- `miniprogram/services/api.js` 补齐新增后端 action 的本地降级返回，保证无云函数环境也能跑前端联调。
- 桌台状态切换向后端发送 `idle/cleaning/repairing` 枚举，页面继续展示中文状态。
- 爽约限制列表改为真实接口调用，并在解除中、成功、失败状态给出反馈。
- 配置中心按当前禁改真实数据边界，仅做只读接口联通检查，不在页面触发商家配置写入。
- 预约详情从列表筛选改为调用 `getReservationDetail`。
- 助教预约详情从列表筛选改为调用 `getAssistantDetail`。

## 验证结果

- 前端 JS/JSON 解析：通过。
- WXML 标签结构检查：通过。
- 新增前端 mock action 调用：通过。
- 乱码扫描：未发现明显乱码命中。
- 后端基线测试：`npm test`，21/21 通过。

## 剩余问题

- 微信开发者工具内真实页面点击联调未在当前 shell 环境执行。
- 商家配置写入类接口涉及真实数据修改，当前页面未开放保存动作；需要中枢授权真实配置写入后再进入保存表单与实机验收。
