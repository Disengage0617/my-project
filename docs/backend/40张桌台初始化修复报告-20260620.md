# 40 张桌台初始化修复报告 - 2026-06-20

message_type: handoff
request_id: REQ-20260620-后端补齐40张桌台初始化
from_lane: backend
to_lane: coordinator
created_at: 2026-06-20T当前时间+08:00
recorded_in: 当前对话 / docs/backend/40张桌台初始化修复报告-20260620.md / docs/worklog.md

消息内容：
- 缺陷原因：`cloudfunctions/api/src/handlers.js` 的 `bootstrapMvp` 只初始化 A01、A02、B01 三张桌台，缺少项目规则要求的 C01-C08 独牙桌台；同时后端 `getAvailableTables` 未按 `table_type/tableType` 做服务端筛选，真实/DevTools 数据源选择独牙时可能返回 0 张或依赖前端兜底。
- 完成内容：补齐 `bootstrapMvp` 初始化 40 张桌台；补 `getAvailableTables` 的 `table_type/tableType` 筛选；补测试断言 `bootstrapMvp` 后总桌台为 40，且 `getAvailableTables({ table_type: "duya" })` 返回 C01-C08。
- 改动文件：`cloudfunctions/api/src/handlers.js`、`cloudfunctions/api/test/handler-flow.test.js`、`docs/backend/40张桌台初始化修复报告-20260620.md`、`docs/worklog.md`。
- 接口/数据变化：
  - `bootstrapMvp` 初始化桌台从 3 张改为 40 张。
  - A01-A12：`table_type="standard"`，`table_type_name="普台"`，`area_name="普台区"`。
  - B01-B10：`table_type="silver"`，`table_type_name="乔氏银腿"`，`area_name="银腿区"`。
  - C01-C08：`table_type="duya"`，`table_type_name="独牙"`，`area_name="独牙区"`。
  - D01-D06：`table_type="gold"`，`table_type_name="乔氏金腿"`，`area_name="金腿区"`。
  - E01-E04：`table_type="rose_gold"`，`table_type_name="乔氏玫瑰金"`，`area_name="玫瑰金区"`。
  - 所有桌台默认 `status="idle"`、`enabled=true`，并带 `sort_order`。
  - `getAvailableTables` 返回增加 `items`、`total`，并支持 `table_type/tableType` 服务端过滤。
- 验证结果：
  - `node --check cloudfunctions/api/src/handlers.js` 通过。
  - `node --check cloudfunctions/api/test/handler-flow.test.js` 通过。
  - `cd cloudfunctions/api && npm test` 通过，29/29。
  - `git diff --check -- cloudfunctions/api/src/handlers.js cloudfunctions/api/test docs/backend/40张桌台初始化修复报告-20260620.md docs/worklog.md` 退出码 0；仅有 Git LF/CRLF 换行提示，无 whitespace 错误。
- 未实测范围：未部署云函数，未连接/写真实云数据库，未跑微信开发者工具 MCP，未做真机/截图/扫码/真实手机号授权。
- 剩余问题：用户当前 DevTools 若已连接旧云端数据，仍需在获得授权后重新部署/重新初始化或迁移真实云端测试数据；本轮只修本地代码和测试。
- 建议下一步负责 agent：测试工程师 Agent 复跑本地后端测试和独牙筛选 QA；若用户授权，再由后端工程师 Agent 进入真实云端部署/数据修复流程。
- 是否触发暂停条件：否。本轮未部署、未写真实库。
