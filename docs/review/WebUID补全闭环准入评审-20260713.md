# WebUID 补全闭环准入评审（2026-07-13）

```text
message_type: review
request_id: REQ-20260713-WebUID补全闭环准入评审
from_lane: reviewer
to_lane: coordinator
created_at: 2026-07-13T当前时间+08:00
recorded_in: 当前对话 / docs/review/WebUID补全闭环准入评审-20260713.md / docs/worklog.md
```

## 评审结论

- 统一结论：`P0=0 / P1=1 / P2=3`，当前**不准入后端候选构建、部署授权、托管上传或真实探针**。
- 后端身份补全实现与 QA 本身为 `P0=0 / P1=0`；前端 probe-only 实现与 QA 本身为 `P0=0 / P1=0`。唯一 P1 是 mandatory canonical 与调度状态没有同步 2026-07-12 至 2026-07-13 的真实部署、托管、探针、日志和新实现事实，无法以唯一权威状态证明下一次高风险操作不是重复执行。
- P1 解除不需要改业务代码：由 canonical 唯一 owner（coordinator）只写同步 `docs/product/closures/CL-1A-web-admin-real-readonly.md` 与 `docs/agent-dispatch-state.md`，reviewer 只读复核后即可准入后端构建全新 9 文件候选。
- 现有线上 `api` 仍是 2026-07-12 的旧运行时 v2 包；当前 `uid -> auth.getUserInfoForAdmin -> openId` 补全只在本地，未构建、未部署、未真实调用。
- 现有线上托管 `cl-1a-web-admin-readonly-20260712-v1` 是旧 probe-only 包，必须保持不动；20260713 前端 manifest 只代表本地候选，不得上传旧目录。
- manager 尚未正向通过，`real_cloud_readonly` 继续 fail/blocked；owner 单字段升级继续冻结，`real_write=blocked`。

## P1：canonical 与调度真源漂移

交叉读取发现：

1. canonical 顶部仍写 `getAdminSession` 本地 45/45，最新进展止于 2026-07-11 `v2` 托管；没有记录 2026-07-12 运行时 v2 的单次部署、远端 exact 9 文件、两次真实 probe 失败、20260712-v1 probe-only 托管、2026-07-13 HTTPS 200、`IDENTITY_MISSING_OPENID`、单次精确日志空结果与当前 enrichment 本地修正。
2. `docs/agent-dispatch-state.md` 顶部仍保留旧 `evidence_fingerprint`、`retry_count_total=3`、旧 `changed_since_last_attempt`；文件尾部最新状态仍停留在 2026-07-12 二维码等待，未登记上述后续真实操作、当前无写锁、当前 blocker 与解除条件。
3. `docs/worklog.md` 已有这些事实，但项目规则要求 active closure 的唯一用户结果必须由 canonical 控制，并要求 dispatch 继承所有重试预算；worklog 不能替代 canonical/dispatch。

在此状态下若直接构建并向用户请求部署/上传/探针授权，会存在复用旧 manifest、遗漏已消耗次数或误判当前线上版本的风险，因此按任务暂停条件计为 P1。

### P1 最小解除清单

coordinator 必须同步并由 reviewer 再核对：

- active closure、canonical owner 与当前 `write_lock_owner=none`；
- 2026-07-12 运行时 v2 单次部署：9 files / 155577 bytes / manifest `9174250adb8824cb958859f676d78bd267e27a2f4ee59267080a29d52bad7f17`，env before/after 保持单值，远端 exact；
- 该包的唯一探针失败事实以及后续 20260712-v1 probe-only 页面命中 `IDENTITY_MISSING_OPENID` 的事实；不得把失败写成 manager 通过；
- 20260712-v1 远端精确 15 文件与 2026-07-13 首页 HTTPS 200；该旧目录禁止覆盖；
- `IDENTITY_MISSING_OPENID` 精确 RequestId 日志查询 1 次、结果 0，具体日志/字段仍 unknown；
- 当前本地 backend 85/85、frontend 28/28 与新代码/前端 manifest；明确两者都未部署/上传；
- `evidence_fingerprint`、`changed_since_last_attempt`、累计 retry/probe/deploy/hosting 次数与 blocker key 不得因换 Agent、换日期或新 request_id 重置；
- 当前 blocker 为本地新候选尚未构建且高风险操作尚无新 manifest 绑定授权；manager 三项正向之前 owner 冻结。

## 后端实现与 SDK 真源

### SDK 只读 Auth 调用

- 锁定 `@cloudbase/node-sdk@3.17.2`。
- `src/auth/index.ts` 直接证明：`getEndUserInfo(uid,{timeout})` 先校验 uid，然后单次提交 `POST auth.getUserInfoForAdmin`，参数为 `uuid: uid`；成功结果按 `{ ...runtimeDefault, ...result.data }` 合并并提供 `uid/openId/appId`。
- `POST` 是 Auth 查询 API 的传输方法，不是 DB 写入。实现复用函数运行时的临时凭据，没有新增长期 secret。
- 当前 app config 没有配置 `retryOptions` 或 `retries`；本实现显式 timeout，应用层不会自行重试。
- 目标环境函数身份是否有权调用 `auth.getUserInfoForAdmin` 仍为 `unverified`，只能由后续唯一真实 probe 证明；无权限时应归一化为 `IDENTITY_ENRICHMENT_SDK_FAILURE` 并在 DB 前 fail closed。

### 身份边界

- 原生 `wxContext.OPENID` 最高优先级，不初始化 Web Auth adapter。
- 仅 runtime `uid/appId` 可信且 `openId` 为空时，以该 runtime uid 补全最多一次。
- 补全前要求 uid 合法、runtime appId 命中唯一 allowlist；补全后要求 uid 一致、appId 与 runtime/allowlist 一致、openId 非空。
- payload 中的 uid/openid/openId/role/storeId 不参与可信身份解析。
- 身份失败发生在 `getDb()` 与 handler 前；QA 已证明 DB/handler 不执行，未新增 DB 写 primitive。
- resolver surface 精确为 `getAdminSession`、`getAdminPriceRules`、`getAdminStoreDetail`；其他/写 action 的 `resolverCalls=0`。

### diagnostic 16 安全性

- 后端当前 15 个可产生的非敏感诊断码与前端显式 16 码集合一致；第 16 个是旧兼容码 `IDENTITY_MISSING_OPENID`。
- 前端使用 exact `Set.has`；未知码、只满足正则外形的未来码、空白/大小写/HTML 变体均丢弃。
- UI 只保留本地固定 message、exact diagnosticCode 与格式校验后的 RequestId；raw/upstream/uid/openId/appId/token 不进入结果或 DOM。
- probe-only 成功和失败均为 `getAdminSession=1 / continuation=0 / other transport=0`；API guard 在 transport 前，页面没有 retry、刷新、sign-out、六模块或写入口。

## 后端新候选范围（P1 解除后）

必须从当前 `cloudfunctions/api/` 机械复制到全新、空的候选目录，根部只允许以下 9 个普通文件：

1. `actionGuard.js`
2. `adminIdentity.js`
3. `apiMain.js`
4. `constants.js`
5. `handlers.js`
6. `index.js`
7. `package-lock.json`
8. `package.json`
9. `rules.js`

必须排除 `node_modules/`、`test/`、`scripts/`、`src/`、docs、evidence、旧候选、远端下载目录、日志、缓存、`.env` 与任何子目录。依赖必须继续精确锁定 `@cloudbase/node-sdk=3.17.2`、`wx-server-sdk=4.0.2`。

旧 manifest `9174250a...f17` 对应旧 `adminIdentity.js`，已经因 enrichment 实现变化而失效；不得用于新候选或新授权。新候选必须重新列出 9 文件逐字节 SHA-256、bytes、总 bytes 与新 manifest，并由 QA 独立位置重算一致。

## 唯一推荐路线

1. coordinator 先同步 canonical/dispatch；reviewer 只读确认 P1 关闭。
2. backend 构建全新精确 9 文件候选，不做远端操作；QA 独立复算范围、源匹配、manifest、85/85、依赖版本、audit 与语法。
3. reviewer 确认候选未漂移后，coordinator 申请绑定**新后端 manifest**的单次部署授权。
4. backend 在部署前只读核对完整 env，必须保持 `ADMIN_WEB_AUTH_ALLOWED_APP_IDS=wx6a196f6ebfb27596` 单值且不写 env；一次部署；部署后一次 info/env after；预建空目录后一次远端包下载并 exact 9 文件/manifest。任一步失败停止，不探针。
5. 只有后端远端 exact 通过，才只读确认 `cl-1a-web-admin-readonly-20260713-v1` 不存在；存在则停止，不覆盖。不存在后申请绑定前端 15 files / 15,887,367 bytes / manifest `8f7e7d0970be229eda0792623af856d377ad11ccd4f9df76b070ef1784822b3e` 的单次上传授权。
6. frontend 仅上传到该全新前缀一次；远端 exact 15、总 bytes、manifest 与首页 HTTPS 200 全部通过后停止。
7. 最后单独申请唯一 probe 授权；建立监听后单次访问新页面。登录/扫码只由用户本人完成；页面加载或回跳只允许一个 `getAdminSession`，成功或失败立即停止。失败不得刷新、重试、换 AppID、扩大 allowlist、调用其他 action 或查宽泛日志。
8. manager session 成功后，另行授权并验证同一可信会话的 `getAdminPriceRules` 与 `getAdminStoreDetail`；三项均成功且 store scope 一致前，不得升级 owner。

## 授权拆分与止损

高风险授权必须分为三段，不能合并为一份跨阶段总授权：

1. **后端部署授权**：可把 env before/after 只读核对、单次部署、单次 info、单次远端包下载 exact 合在同一份条件授权中；必须绑定新 9 文件 manifest 与受限接受 `1 moderate / 5 high / 0 critical`。
2. **前端托管上传授权**：可把前缀不存在核对、单次上传、remote exact、首页一次 HTTPS GET 合在同一份条件授权中；必须绑定 20260713-v1 manifest，且绝不能覆盖 20260712-v1 或其他目录。
3. **唯一真实 probe 授权**：必须在前两段全部成功后单独取得，不能与部署或上传预授权合并。该分离保证任一前置失败不会隐式保留一个仍可被消费的 action 授权。

所有阶段首次失败即停止；不得第二次部署、第二次上传、第二次下载、第二次 probe、修 env、扩大 allowlist、换 AppID、写 DB/role、seed、删除、迁移、上传小程序、体验版、审核或 git push。

## 严重度

### P0

- 0。

### P1

1. canonical 与 dispatch 未同步当前真实状态、证据指纹和累计预算；关闭前不得构建候选或申请高风险授权。

### P2

1. 目标环境函数角色对 `auth.getUserInfoForAdmin` 的真实权限未验证；唯一 probe 失败时必须 fail closed，不能改权限或重试。
2. 后端依赖审计仍为 `1 moderate / 5 high / 0 critical`，只允许用户重新受限接受一次隔离只读验证，不代表生产长期安全。
3. 前端主 JS 约 1.009 MB 的既有 Vite chunk warning；不影响当前唯一 action 边界，但正式化前应拆分。

## 六层状态

| 层级 | 状态 | 结论 |
| --- | --- | --- |
| `local_static` | pass | backend enrichment 与 frontend exact 16/probe-only 静态边界通过 |
| `local_mock` | pass | backend 85/85、frontend 28/28 |
| `local_cloud_function` | pass | 锁定 SDK 受控 transport 与入口/身份测试通过；真实权限不在此层 |
| `gui_devtools` | fail | 旧线上包的真实 manager probe 失败；新包/新页面未上线 |
| `real_cloud_readonly` | fail | manager 三项与 owner 正向均未通过 |
| `real_write` | blocked | manager 三项正向前 owner 升级冻结 |

## 标准回传

- 完成内容：交叉审阅后端实现/QA、前端实现/QA、SDK 真源、diagnostic 16、action 零扩张与 canonical/止损。
- 改动文件：本评审报告；`docs/worklog.md` reviewer 条目。
- 验证结果：实现本身 P0/P1=0；统一 closure 为 `P0=0 / P1=1 / P2=3`，暂不准入候选构建或授权。
- 剩余问题：coordinator 必须先同步 canonical/dispatch；新后端候选与 manifest 尚未生成；真实 Auth 权限、manager/owner 和真实只读 transport 尚未验证。
- 建议下一步负责 agent：Codex 中枢（coordinator）同步 canonical/dispatch，随后回派验收官 Agent 只读确认 P1 关闭。
- 是否触发暂停条件：是；P1 未关闭前不得候选构建或高风险授权。
- 无需用户转发，由 Codex 中枢继续分派。

---

## 2026-07-13 固定 Node 本地 preflight 准入复核

```text
message_type: review
request_id: REQ-20260713-uid-enrichment-fixed-node-preflight-review
from_lane: reviewer
to_lane: coordinator
created_at: 2026-07-13T13:06:00+08:00
recorded_in: 当前对话 / docs/review/WebUID补全闭环准入评审-20260713.md / docs/worklog.md
```

### 唯一执行结果

- 固定本地命令仅执行 **1 次**：`E:\node.exe -e <fixed-js-body>`；exit code=`0`，未触发失败止损。
- 实际唯一 stdout：

```json
{"schema":"webuid-enrichment-fixed-node-preflight-v1","ok":true,"entrances":{"node":true,"tcb":true,"wechatCli":true},"cloudbaseCli":{"name":"@cloudbase/cli","version":"3.6.1","tcbBin":"bin/tcb","valid":true},"candidate":{"exists":true,"rootItems":9,"files":9,"dirs":0,"totalBytes":157533,"exactNames":true},"staging":{"exists":true,"rootItems":9,"files":9,"dirs":0,"totalBytes":157533,"exactNames":true},"rawEqualFiles":9,"remoteExecuted":false,"writes":0,"failures":[]}
```

- 固定 JS body 的权威摘要：只使用 `fs.existsSync`、`JSON.parse(package.json)`、`readdirSync(...,{withFileTypes:true})`、`statSync`、`readFileSync(...).equals(...)`；规定文件名为 `actionGuard.js/adminIdentity.js/apiMain.js/constants.js/handlers.js/index.js/package-lock.json/package.json/rules.js`；不调用 `sort`，不计算 manifest/SHA，不写文件、不复制、不启动子进程。
- 可复制调用骨架：

```powershell
& 'E:\node.exe' -e "<将本节上述固定 JS body 原样内联；入口、package、candidate、staging 均使用本轮报告中的绝对路径>"
```

本命令已消耗唯一预检次数；该骨架仅用于审计留档，**不得再次执行**。

### 治理复核

- dispatch 与 canonical 两个顶部权威 YAML 均通过严格 `yaml.safe_load`；request/status 对齐。
- 两处 `retry_count_total=16`，14 个 blocker key 的值求和均为 16，`blocked_agent_failure:cli_version_verifier_parser=1`。
- 两处 fingerprint 字面值均为 `44ee79daeb2bc379483ed3041bb4a472daa9e6ae3cefd225b7814e5bdaa405d7`；但按治理规范从 canonical 公开的 11 个稳定字段（键排序、数组排序、UTF-8、无空白 JSON）独立复算为 `6565ae54cd90cb767ba864763fb366dab21ab658f653eca0af3a7ed47a872621`，**未命中**。
- `current_authorization=terminated_after_v3_local_version_parser_failure_no_remote_operation_consumed`、`write_lock_owner=none`、`canonical_owner=coordinator`；v3 报告 env/deploy/status/download/action/log/DB/role 均为 0；远端核验目录仍不存在。
- owner 冻结规则仍在：manager 同会话三项真实正向通过前不得执行 role 写。
- `git diff --check` exit 0；仅既有 CRLF warning。

### 结论与分级

- `P0=0 / P1=1 / P2=2`。
- 唯一 P1：canonical 发布的 11 个稳定字段无法复现两处登记 fingerprint；在中枢把 `blocker_key/解除条件` 与 fingerprint 原文统一并重新生成、再经一次纯治理复核前，**不准入新的部署授权或执行**。
- P2：缓存 `_npx` 绝对路径仍是机器局部路径；当前 CloudBase/微信 CLI 登录态未在本地 preflight 中探测。两项均应由未来新授权的首次实际步骤决定，首次失败立即停止，不得换入口或重试。

### 标准回传

- 完成内容：唯一固定 Node preflight 一次通过；完成 YAML、retry、fingerprint、授权终止、远端 0、写锁与 owner 冻结复核。
- 改动文件：本评审报告；`docs/worklog.md` 追加 reviewer 条目。
- 验证结果：本地入口/版本/candidate-staging 不变性均通过；治理 fingerprint 独立复算不通过。
- 剩余问题：仅上述 fingerprint 可复现性 P1；不得用两处字面相同替代独立复算。
- 建议下一步负责 Agent：Codex 中枢修正 canonical/dispatch 的稳定字段与 fingerprint 后，回派验收官 Agent 做纯治理复核。
- 是否触发暂停条件：是；当前不准入新部署授权。
- 无需用户转发，由 Codex 中枢继续分派。

---

## 2026-07-13 WebUID enrichment 治理 P1 快速复核

```text
message_type: review
request_id: REQ-20260713-uid-enrichment-governance-p1-recheck
from_lane: reviewer
to_lane: coordinator
created_at: 2026-07-13T12:25:00+08:00
recorded_in: 当前对话 / docs/review/WebUID补全闭环准入评审-20260713.md / docs/worklog.md
```

### 复核结论

- **未通过**；当前 closure 保持 `P0=0 / P1=1 / P2=3`，不准入中枢重新请求部署授权。
- canonical YAML 通过严格 `yaml.safe_load`；`retry_count_total=14`、12 个 blocker key 值求和为 14、新增 `blocked_agent_failure:manifest_verifier_sort_semantics=1`，`write_lock_owner=none`、action set 三项、授权终止、远端操作 0、owner 冻结和 blocker release 均与事实报告一致。
- dispatch 与 canonical 的 fingerprint 字面值均为 `1ac04a713e9d513c05e8f12f3d4495c77a38b07b36a27c8ecd96a0f11d665591`。
- reviewer 按规范从 canonical 取 11 个稳定字段，字段名按字典序、数组排序、UTF-8 无空白 canonical JSON 后独立计算 SHA-256，精确命中同一 fingerprint。复算输入包含 closure/source/changed-files/contract/runtime/environment/evidence-layers/actor roles/business fixture/action set/blocker key，不包含时间、Agent 或 retry。
- 唯一阻塞仍是 dispatch 的 YAML 代码块不能严格解析。首个确定错误位于 `docs/agent-dispatch-state.md:133`：未引用 plain scalar 内嵌 ``message_type: status``，PyYAML 报 `mapping values are not allowed here`。同一长 YAML block 静态扫描另有多处类似第二个 `: ` 的未引用 scalar，不能在只修首行前宣称整块可解析。

### P1 最小解除条件

1. coordinator 不改变当前事实字段，只修复 dispatch YAML 的结构有效性。
2. 推荐把顶部当前 active-state 截成独立、可解析的 YAML block；庞大的 legacy `dispatch:` 历史若继续保留在同一 YAML，必须对所有含 `: ` 的 plain scalar 正确引用/转义并通过整块严格解析。不得只修首个报错后停止。
3. 修复后必须再次证明：dispatch 与 canonical 两个完整 YAML block 均可 strict parse；retry `14=14`；两处 fingerprint 一致且复算命中；授权终止、远端 0、write lock none、owner 冻结和 blocker release 不变。
4. 以上通过后，治理 P1 才可关闭，届时才准入中枢重新请求绑定同一候选、唯一 Node verifier、全新 staging/api、env before/after 零写、单次部署、单次下载和首次失败即停的精确授权。

### 标准回传

- 完成内容：严格解析两份 YAML，复算 retry 与稳定 evidence fingerprint，交叉核对授权、远端计数、写锁、owner 与 release。
- 改动文件：本评审报告；`docs/worklog.md` reviewer 条目。
- 验证结果：canonical parse pass；retry `14=14`；fingerprint recompute pass；dispatch parse fail at line 133。
- 剩余问题：仅治理 P1——dispatch 整个 YAML block 必须严格可解析。
- 下一步负责 Agent：Codex 中枢修复 dispatch 文档结构后回派验收官 Agent。
- 是否触发暂停条件：是；当前不得请求或执行新的部署授权。
- 无需用户转发，由 Codex 中枢继续分派。

---

## 2026-07-13 WebUID enrichment 治理 P1 最终复核

```text
message_type: review
request_id: REQ-20260713-dispatch-active-yaml-split-recheck
from_lane: reviewer
to_lane: coordinator
created_at: 2026-07-13T12:26:00+08:00
recorded_in: 当前对话 / docs/review/WebUID补全闭环准入评审-20260713.md / docs/worklog.md
```

### 最终结论

- 治理 P1 已关闭；当前为 `P0=0 / P1=0 / P2=3`。
- **准入 Codex 中枢重新向用户请求一次精确的单次后端部署授权**；本结论不等于已授权，也不准入后端直接执行。
- dispatch 顶部权威 Active Request YAML 与 canonical YAML 均通过严格 `yaml.safe_load`；两文件各只有一个 YAML block。历史 `dispatch:` 已完整保留在明确标注的 `text` block，不再参与权威 YAML 解析。
- 两份 YAML 的 request/status 一致；retry total 均为 14，明细和均为 14，新 blocker 为 1；`write_lock_owner=none`。
- 两处 fingerprint 均为 `1ac04a713e9d513c05e8f12f3d4495c77a38b07b36a27c8ecd96a0f11d665591`；按 11 个稳定字段 canonical JSON 独立复算精确命中。
- 授权终止、staging/env/info/deploy/download/action/log/DB/role 均为 0、候选未漂移、唯一 Node verifier、owner 冻结及 blocker release 在 dispatch/canonical/worklog/backend/review 间一致。
- `git diff --check` 对 dispatch、canonical、review 和 worklog 无错误，仅有 Windows CRLF 提示。

### 新授权必须绑定的边界

- 候选：`.tmp_web_uid_enrichment_candidate_20260713_v1`；精确 9 files / 157533 bytes / manifest `a0520bb158c30e6c07d4eb0220205ff8834ccd337bf8e5433a24ef3d2208a2cb`。
- verifier：仅 Node.js 默认 `Array.sort()`；禁止 PowerShell culture sort、`localeCompare` 或任何区域性排序。
- staging：先确认 `.tmp_web_uid_enrichment_deploy_staging_20260713_v1` 不存在，再创建全新 `staging/api`，机械复制 9 文件并用唯一 Node verifier 一次复算；不一致立即停止。
- env：部署前后只读完整核对，必须保留单值 `ADMIN_WEB_AUTH_ALLOWED_APP_IDS=wx6a196f6ebfb27596` 及全部既有键；不得编辑、保存或重排。
- deploy：仅对环境 `cloud1-d2gp2ayiwab8f8a94` 中现有 `api` 单次代码部署；不得创建其他函数或重试。部署后一次 status/detail 必须为 `Active + Available`。
- download：确认 `.tmp_web_uid_enrichment_remote_verify_20260713_v1` 不存在后创建全新空目录，单次下载远端 `api`；排除平台 `node_modules` 后必须 exact 9 / 157533 / 同 manifest / 9/9 raw bytes。
- 止损：任一步失败或结果不明立即停止，不得第二次预检、部署、下载或修配置；不得夹带前端托管、页面、登录/扫码、action/probe、日志、DB/role、seed、删除、迁移、小程序、审核或 git push。
- P2：依赖审计仍为 `1 moderate / 5 high / 0 critical`，必须由用户在新授权中重新受限接受，不代表生产长期安全通过。

### 标准回传

- 完成内容：复核拆分后的两份权威 YAML、retry、fingerprint、授权/远端计数、写锁、owner 与 release。
- 改动文件：本评审报告；`docs/worklog.md` reviewer 最终条目。
- 验证结果：两份 YAML parse pass；retry `14=14`；fingerprint recompute pass；diff-check pass。
- 剩余问题：等待用户重新给出精确部署授权；真实部署、Auth 权限、manager/owner 和 frontend transport 仍未验证。
- 下一步负责 Agent：Codex 中枢请求精确授权；获得授权后再分派后端工程师 Agent 单次执行。
- 是否触发暂停条件：是；等待新的精确用户授权。
- 无需用户转发，由 Codex 中枢继续分派。

---

## 2026-07-13 WebUID enrichment CLI 绝对入口与 staging 复用评审

```text
message_type: review
request_id: REQ-20260713-uid-enrichment-cli-entry-review
from_lane: reviewer
to_lane: coordinator
created_at: 2026-07-13T12:51:00+08:00
recorded_in: 当前对话 / docs/review/WebUID补全闭环准入评审-20260713.md / docs/worklog.md
```

### 结论

- 当前治理与下一授权门为 `P0=0 / P1=0 / P2=3`；**准入中枢重新请求一次精确单次部署授权**，但不准入直接执行。
- v2 授权已因 bare `tcb` 无法解析而终止；CLI 进程、远端请求、env/info/deploy/download/action/log/DB/role 均为 0，线上未触碰，写锁已释放。
- CloudBase CLI 的历史成功调用形式和当前唯一缓存入口均可证明；不需要猜测 PATH，也不得再次使用 bare `tcb`。
- 已通过的 `.tmp_web_uid_enrichment_deploy_staging_20260713_v1/api` 当前仍可安全复用；不建议创建 v3 staging，也不应在下一轮再次执行 Node manifest verifier。

### 唯一 CloudBase CLI 入口

历史成功调用记录统一为：

```text
cmd.exe /c E:\npx.cmd --yes --package @cloudbase/cli tcb ...
```

既有原始证据中，该形式的 `tcb env:list --json` 和 `tcb fn detail api ... --json` 均 exit 0；前者看到目标环境 `cloud1-d2gp2ayiwab8f8a94` 为 `NORMAL`，后者成功返回 `api` detail。成功输出明确标识 `CloudBase CLI 3.6.1`。

本地只读路径证明：

- launcher：`E:\npx.cmd`，存在；其脚本最终使用 `E:\node.exe` 与 npm `npx-cli.js`。
- Node：`E:\node.exe`，存在。
- 唯一缓存包根：`C:\Users\宋\AppData\Local\npm-cache\_npx\9a8789722ddc2fbe\node_modules\@cloudbase\cli`。
- package 与 cache lock 均锁定 `@cloudbase/cli=3.6.1`；lock 的 resolved 为官方 npm 包 `@cloudbase/cli/-/cli-3.6.1.tgz`。
- bin：`C:\Users\宋\AppData\Local\npm-cache\_npx\9a8789722ddc2fbe\node_modules\@cloudbase\cli\bin\tcb`，存在，入口为 `../dist/standalone/cli.js`。

为消除 PATH、npx 重新解析和版本漂移，下一授权应绑定下列**直接绝对调用**，仅用于 env before/after：

```powershell
& 'E:\node.exe' 'C:\Users\宋\AppData\Local\npm-cache\_npx\9a8789722ddc2fbe\node_modules\@cloudbase\cli\bin\tcb' fn detail api -e cloud1-d2gp2ayiwab8f8a94 --json
```

执行前只允许 `Test-Path` 与读取 package/cache lock，确认上述三个绝对文件仍存在且版本仍精确为 3.6.1；任一不符立即停止，不得回退到 bare `tcb`、`npx`、PATH、GUI 或其它缓存。

### 部署与下载入口

- 既有成功部署、function info 和 download 使用的绝对入口为 `E:\微信web开发者工具\cli.bat`，该文件当前存在。
- 既有部署报告证明该入口曾对同一项目、AppID `wx6a196f6ebfb27596`、环境 `cloud1-d2gp2ayiwab8f8a94` 的现有 `api` 返回 `success=true`；历史 `islogin` 原始证据为 exit 0 / `login=true`。
- 当前登录状态没有在本评审中重新探测；新授权下首次实际 CLI 若因登录、版本、路径或其它原因失败/unknown，立即停止，不得换工具或重试。
- 新授权必须把 `E:\微信web开发者工具\cli.bat` 固定为部署、postdeploy function info 和单次 remote download 的唯一入口。

### staging 复用策略

reviewer 本轮只读确认当前状态：

- candidate 与 staging 各为 exact 9 files / 0 dirs / 157533 bytes。
- 两者当前 manifest 均为 `a0520bb158c30e6c07d4eb0220205ff8834ccd337bf8e5433a24ef3d2208a2cb`，逐文件 raw bytes 9/9 相同，mtime 也逐项相同。
- `.tmp_web_uid_enrichment_remote_verify_20260713_v1` 仍不存在。

下一轮不得新建 v3，也不得覆盖、复制或再次计算 staging manifest。只允许在取得新授权并拿到唯一 write lock 后执行一次**不变性守卫**：

1. 只读确认既有 staging `api/` 仍为 exact 9 个规定文件、0 目录、157533 bytes。
2. 只读 `Buffer.equals` 比较 staging 与授权 candidate 9/9 raw bytes；不排序生成 manifest、不计算 SHA、不写文件。
3. 一致则直接进入唯一绝对 CloudBase CLI 的 env before；不一致则立即停止，不得重建 staging、重新复制或改用 v3。

该守卫不重复上一轮已通过的 manifest precheck，只证明被授权复用的 staging 自上次通过后没有普通工作区漂移。

### 治理复核

- dispatch 与 canonical 两个权威 YAML strict parse 通过。
- 两处 retry total 均为 15，明细和均为 15，新 `blocked_agent_failure:cloudbase_cli_command_unresolved=1`。
- 两处 fingerprint 均为 `a64646267cc5d51ce71e480cd5d74f2f0388f73a95e1e2f8da321ff72f250e45`；按 11 个稳定字段 canonical JSON 独立复算精确命中。
- 授权终止、远端操作 0、write lock none、owner 冻结与 blocker release 一致。
- diff-check 无错误，仅 Windows CRLF warning。

### 新授权必须保留的止损边界

- 先做 staging 不变性守卫；随后仅用固定缓存 CloudBase CLI 绝对入口执行一次 env before。
- env before 必须证明完整环境变量仍保留单值 `ADMIN_WEB_AUTH_ALLOWED_APP_IDS=wx6a196f6ebfb27596`；不得编辑、保存或重排。
- 仅用 `E:\微信web开发者工具\cli.bat` 对现有 `api` 单次部署，再做一次 Active+Available/info、一次绝对 CloudBase CLI env after 和一次全新目录 remote download exact。
- 任一步失败或 unknown 立即停止；不得第二次不变性守卫、env、部署、info、download，不得换入口、修配置或创建 v3 staging。
- 不得夹带前端托管、页面、登录/扫码、action/probe、日志、DB/role、seed、删除、迁移、小程序、审核或 git push。
- 依赖风险仍为 `1 moderate / 5 high / 0 critical`，需在新授权中重新受限接受。

### 标准回传

- 完成内容：只读定位唯一缓存 CLI、版本和既有身份上下文证据；评估 staging 复用；复核治理状态。
- 改动文件：本评审报告；`docs/worklog.md` reviewer 条目。
- 验证结果：绝对入口与 3.6.1 cache 可证明；staging 当前 exact 且可复用；YAML/retry/fingerprint/diff-check 通过。
- 剩余问题：当前 CloudBase/微信 CLI 登录状态仅有历史成功证据，需由新授权中的首次 env before/部署实际结果决定；失败即停。
- 下一步负责 Agent：Codex 中枢申请新精确授权；获授权后由后端工程师 Agent 只执行一次。
- 是否触发暂停条件：是；等待新的精确用户授权。
- 无需用户转发，由 Codex 中枢继续分派。

---

## 2026-07-13 WebUID enrichment 部署前止损独立复核

```text
message_type: review
request_id: REQ-20260713-uid-enrichment-precheck-failure-review
from_lane: reviewer
to_lane: coordinator
created_at: 2026-07-13T17:25:00+08:00
recorded_in: 当前对话 / docs/review/WebUID补全闭环准入评审-20260713.md / docs/worklog.md
```

### 结论

- 根因已明确为**校验工具排序口径错误**，不是候选变化，也不是未知状态。
- reviewer 使用 Node.js 按 JavaScript 默认 `Array.prototype.sort()` 独立复算：候选仍为精确 9 个根文件、0 个目录、157,533 bytes，9/9 与 `cloudfunctions/api/` 同名源文件 raw bytes 一致，manifest 仍为 `a0520bb158c30e6c07d4eb0220205ff8834ccd337bf8e5433a24ef3d2208a2cb`。
- 将唯一相邻顺序差异改为 `package.json` 在 `package-lock.json` 前，可精确复现失败值 `eb8ce3bec6fa0b0735cc84224330e21ad205617ce3dc5048eb5c5b980c1c46d4`。这与失败报告中的 PowerShell culture sort 顺序完全吻合，形成充分因果证据。
- 原用户授权已因“任一步失败或结果不明立即停止、不得继续后续步骤”而终止。虽然云端部署、env、download、action、log、DB、role 次数均为 0，但再次启动完整流程仍属于新的执行尝试，不能沿用原授权；必须重新取得绑定同一候选和修正后唯一 Node 核验器的精确授权。
- 技术根因与候选本身：`P0=0 / P1=0 / P2=1`。P2 仍为已受限接受的依赖审计 `1 moderate / 5 high / 0 critical`。
- 当前 closure 治理状态：`P0=0 / P1=1 / P2=3`。唯一新增 P1 是 canonical/dispatch 尚未登记本次“用户已授权 -> 本地 verifier 首次失败 -> 授权终止 -> 云端操作 0”的事实，顶部仍写候选尚未构建、当前无新授权且 retry=13。该真源冲突关闭前，不准入新的授权请求或直接重试。

### 唯一权威 manifest 算法

1. 只枚举目标目录根部普通文件；根项必须精确为 9 个普通文件且目录为 0。
2. basename 使用 JavaScript 默认 `Array.prototype.sort()`；不得使用 `localeCompare`、`Intl.Collator`、PowerShell `Sort-Object`、系统区域性排序或大小写区域排序。
3. 每个文件读取 raw bytes，计算 `bytes` 和 raw-byte SHA-256。
4. 每行严格为 `basename + U+0009 + bytes + U+0009 + sha256`。
5. 各行按上述 JS 默认顺序以 U+000A 连接，末行无换行；整体以 UTF-8 编码后计算 SHA-256。
6. 本候选权威顺序固定为：`actionGuard.js`、`adminIdentity.js`、`apiMain.js`、`constants.js`、`handlers.js`、`index.js`、`package-lock.json`、`package.json`、`rules.js`。

唯一脚本入口为 Node.js，执行时把 `ROOT` 指向候选或 staging 的 `api/`：

```powershell
node -e "const fs=require('fs'),path=require('path'),crypto=require('crypto');const root=path.resolve(process.env.ROOT);const entries=fs.readdirSync(root,{withFileTypes:true});const files=entries.filter(e=>e.isFile()).map(e=>e.name).sort();const rows=files.map(name=>{const b=fs.readFileSync(path.join(root,name));return{name,bytes:b.length,sha256:crypto.createHash('sha256').update(b).digest('hex')}});const body=rows.map(x=>x.name+'\t'+x.bytes+'\t'+x.sha256).join('\n');console.log(JSON.stringify({rootItems:entries.length,files:files.length,dirs:entries.filter(e=>e.isDirectory()).length,totalBytes:rows.reduce((n,x)=>n+x.bytes,0),order:files,manifest:crypto.createHash('sha256').update(Buffer.from(body,'utf8')).digest('hex'),rows},null,2));"
```

### staging 复制后的精确核验步骤

1. 新授权后，先确认 `.tmp_web_uid_enrichment_deploy_staging_20260713_v1` 仍不存在；存在即停止。
2. 创建全新 `.tmp_web_uid_enrichment_deploy_staging_20260713_v1/api`，只机械复制授权的 9 个文件，不运行 npm、不产生任何子目录。
3. 设置 `ROOT=.tmp_web_uid_enrichment_deploy_staging_20260713_v1/api`，只运行上面的唯一 Node 脚本一次。
4. 必须同时满足：`rootItems=9`、`files=9`、`dirs=0`、`totalBytes=157533`、顺序精确等于上述 9 项、manifest 精确等于 `a0520bb158c30e6c07d4eb0220205ff8834ccd337bf8e5433a24ef3d2208a2cb`。
5. 再逐文件与候选 raw bytes 比较 9/9 一致；任一条件不满足立即停止，不得进入 env before 或部署。
6. PowerShell 只可承担路径存在性与机械复制，不得参与文件排序或 manifest 生成。

### 授权状态与下一步

- 旧授权：已终止、不可复用；不是“部署次数已消耗”，而是“整份条件式执行授权已因首次失败失效”。
- 新准入：当前为**条件准入**。coordinator 必须先更新 canonical/dispatch：登记本次授权已终止、部署/env/download/action/log/DB/role 均为 0、候选未漂移、根因是 verifier sort semantics，并把累计 retry 从 13 增至 14（建议 blocker key：`blocked_agent_failure:manifest_verifier_sort_semantics`），同时生成新的 evidence fingerprint。reviewer 快速复核两份真源一致后，才允许重新请求一次精确授权。
- 新授权需明确这是失败止损后的全新尝试，并绑定同一候选、同一 manifest、唯一 Node 算法、单次 staging 核验、单次部署和首次失败即停。
- 新授权不得增加前端托管、页面访问、登录/扫码、action/probe、日志、DB/role、seed、删除、迁移、小程序、审核或 git push。
- 下一步负责 Agent：Codex 中枢先同步 canonical/dispatch 并回派 reviewer 快速复核；P1 关闭后再申请新授权。用户明确授权后，由后端工程师 Agent 从全新 staging 开始单次执行。
- 是否触发暂停条件：是；先等待治理 P1 关闭，随后等待新的精确用户授权。
- 无需用户转发，由 Codex 中枢继续分派。

## 2026-07-13 WebUID enrichment 候选最终部署准入

```text
message_type: review
request_id: REQ-20260713-WebUID-enrichment候选最终部署准入
from_lane: reviewer
to_lane: coordinator
created_at: 2026-07-13T当前时间+08:00
recorded_in: 当前对话 / docs/review/WebUID补全闭环准入评审-20260713.md / docs/worklog.md
```

### 准入结论

- 候选/部署门阶段：`P0=0 / P1=0 / P2=1`；完整 closure 累计仍为 `P0=0 / P1=0 / P2=3`。
- **可以由 coordinator 向用户申请仅针对该候选的单次 `api` 代码部署授权。**
- 该准入只覆盖 staging、环境变量只读 before/after、单次部署、一次状态核对和一次远端包 exact；不得包含前端托管上传、浏览器页面访问、登录/扫码、任何 action/probe、日志查询、DB/role 写。
- owner 继续冻结；manager 同一可信会话三个只读 action 全部正向通过前不得执行 role 写。

### reviewer 独立复算

- 候选：`.tmp_web_uid_enrichment_candidate_20260713_v1`。
- 当前精确 9 个根部普通文件、0 目录、0 hidden/other；157,533 bytes。
- reviewer 按 U+0009/U+000A 权威算法独立重算 manifest：`a0520bb158c30e6c07d4eb0220205ff8834ccd337bf8e5433a24ef3d2208a2cb`。
- 9/9 与当前 `cloudfunctions/api/` 同名源文件 raw bytes 一致；backend evidence、QA evidence 与 reviewer 三方一致。
- backend/QA：源 85/85、7/7 JS syntax、package/lock JSON、锁定依赖 `@cloudbase/node-sdk@3.17.2` 与 `wx-server-sdk@4.0.2` 均通过。
- 候选最终无 `node_modules` 或任何子目录；QA 在独立镜像执行 `npm ci --ignore-scripts` 后已清理镜像，候选未漂移。
- `npm audit` 为 `1 moderate / 5 high / 0 critical`；仅能由用户重新受限接受本次隔离只读闭环部署，不代表生产长期安全。
- 预定 staging `.tmp_web_uid_enrichment_deploy_staging_20260713_v1` 与远端核验目录 `.tmp_web_uid_enrichment_remote_verify_20260713_v1` 当前均不存在。
- 相关候选/报告/evidence 的 `git diff --check` exit 0。

### staging/api 强制路径

候选目录名不是 `api`，不得把候选根直接传给按目录名识别函数的部署工具。授权后 backend 必须：

1. 再次证明 `.tmp_web_uid_enrichment_deploy_staging_20260713_v1` 不存在；存在则停止。
2. 创建唯一全新父目录，并在其中创建唯一 `api/` 子目录。
3. 仅从已授权候选机械复制 9 个文件到该 `api/`；不得运行 npm、产生子目录或改文件。
4. 从 staging `api/` 再次复算 9 files / 157,533 bytes / 同一 manifest；任一不一致在部署前停止。
5. 部署工具只能从该 staging 父目录识别既有函数名 `api`，不得以候选目录名创建新函数。

### 单次部署顺序

1. staging exact 通过后，只读取得目标环境现有 `api` 的完整环境变量 before；必须恰有并保留 `ADMIN_WEB_AUTH_ALLOWED_APP_IDS=wx6a196f6ebfb27596` 单值，且不得写、保存或重排环境变量。
2. 仅对环境 `cloud1-d2gp2ayiwab8f8a94` 中已存在的 `api` 执行一次代码部署；不得创建其他函数，不得重试。
3. 部署成功后只允许一次函数状态/detail 核对和一次环境变量 after；必须 `Active + Available`，完整 env before/after 完全一致。
4. 再次证明 `.tmp_web_uid_enrichment_remote_verify_20260713_v1` 不存在，创建全新空目录后仅下载远端 `api` 包一次。
5. 排除平台安装的 `node_modules/` 后，远端函数根必须精确为授权 9 文件、157,533 bytes、同一 manifest，且 9/9 与候选 raw bytes 一致。
6. 到此立即停止。本授权不得访问前端页面、调用 `getAdminSession` 或其他 action，也不得查询日志。

### 首次失败止损

- 候选/staging/manifest、env before、目标函数名或环境不一致：部署次数保持 0，立即停止。
- 单次部署返回失败/不明：停止，不得第二次部署或修配置。
- 部署后非 `Active + Available`、env 漂移、远端下载失败或 remote exact 不一致：停止，不得补部署、再次下载、探针或上传前端。
- 不得修改 allowlist、换 AppID、执行 `audit fix/--force`、写 DB/role、seed、删除、迁移、覆盖托管、上传小程序、体验版、审核或 git push。

### 推荐用户精确授权原文

> 我授权后端工程师 Agent 仅针对 CloudBase 环境 `cloud1-d2gp2ayiwab8f8a94` 中现有的 `api` 云函数，使用本地干净候选 `.tmp_web_uid_enrichment_candidate_20260713_v1`（精确 9 个文件、157533 bytes、manifest `a0520bb158c30e6c07d4eb0220205ff8834ccd337bf8e5433a24ef3d2208a2cb`）执行一次受限代码部署闭环。先确认 `.tmp_web_uid_enrichment_deploy_staging_20260713_v1` 不存在，再创建全新 `staging/api`，仅机械复制这 9 个文件并复算同一 manifest；不一致立即停止。部署前只读取得 `api` 的完整环境变量，必须保留单值 `ADMIN_WEB_AUTH_ALLOWED_APP_IDS=wx6a196f6ebfb27596` 及全部既有键，不得编辑、保存或重排环境变量。随后仅部署现有 `api` 一次，不得创建其他函数或重试。部署成功后只读核对一次函数状态/detail 和一次环境变量 after，必须为 `Active + Available` 且 before/after 完全一致；再确认 `.tmp_web_uid_enrichment_remote_verify_20260713_v1` 不存在，创建全新空目录并单次下载远端 `api` 包，排除平台安装的 `node_modules` 后核对根部精确 9 文件、157533 bytes、同一 manifest 且逐字节等于候选。任一步失败或结果不明立即停止，不得第二次部署、第二次下载、修配置、换 AppID、扩大 allowlist 或继续后续步骤。我受限接受本候选仅用于本次隔离只读闭环的 `1 moderate / 5 high / 0 critical` 依赖风险；这不代表生产长期安全通过。本授权不包含前端托管上传、页面访问、登录/扫码、任何 action/probe、日志查询、DB 或角色写、seed、删除、迁移、覆盖其他资源、上传小程序、体验版、审核或 git push。

### 标准回传

- 完成内容：独立复算候选范围、bytes、manifest、源匹配，交叉核对 backend/QA 证据，并定义 staging/api、env 保全、单次部署与 remote exact 止损。
- 改动文件：本评审报告；`docs/worklog.md` reviewer 条目。
- 验证结果：候选阶段 `P0=0 / P1=0 / P2=1`；9 files / 157,533 bytes / manifest `a0520bb1...a2cb`；三方一致。
- 剩余问题：等待用户绑定新 manifest 的精确授权；真实部署、Auth 权限、manager/owner 和前端 transport 均未验证。
- 建议下一步负责 agent：coordinator 向用户投递上述精确授权；授权后由后端工程师 Agent 单次执行。
- 是否触发暂停条件：是；必须等待新的候选绑定部署授权。
- 无需用户转发，由 Codex 中枢继续分派。

---

## 2026-07-13 CL-1A 治理 P1 最终复核 v2

```text
message_type: review
request_id: REQ-20260713-CL-1A治理P1复核-v2
from_lane: reviewer
to_lane: coordinator
created_at: 2026-07-13T当前时间+08:00
recorded_in: 当前对话 / docs/review/WebUID补全闭环准入评审-20260713.md / docs/worklog.md
```

### 最终结论

- 治理 P1 已关闭；当前累计为 `P0=0 / P1=0 / P2=3`。
- **准入 backend 仅在本地构建全新、干净、精确 9 文件 enrichment 候选并交 QA。**
- 本结论不准入任何远端部署、环境变量操作、托管上传、action/probe、日志查询、DB/role 写或其他线上行为；新候选必须生成新 manifest，QA/reviewer 复核后再向用户申请绑定该 manifest 的新部署授权。

### 最终复核证据

- dispatch 与 canonical 的 active request 均为 `REQ-20260713-WebUID-openid-enrichment-candidate`。
- 两份 YAML 均可解析；`retry_count_total=13`，明细求和均为 13。
- fingerprint 均为 `e0fc48e9b1930ebea9d5589d74998654b068493378ce4b04dce71bd94311e9ff`；`write_lock_owner=none`。
- dispatch `local_deploy_preflight_result` 已明确为 backend QA 85/85、候选与新 manifest 尚未构建、无新云授权。
- canonical YAML 已同步 backend 85/85；`gui_devtools=pass`、`real_cloud_readonly=fail` 与顶部当前状态一致；`retry_history` 明确为历史节选；旧通用“最新进展”已改为截至 2026-07-10 的历史标题。
- 当前线上 api、20260712-v1 托管、MISSING_OPENID/RequestId/日志 0、前端本地 20260713-v1、当前无远端授权和 owner 冻结在两份真源一致。
- canonical、dispatch、worklog 的 `git diff --check` exit 0。

### 候选构建边界

候选根部只允许：`actionGuard.js`、`adminIdentity.js`、`apiMain.js`、`constants.js`、`handlers.js`、`index.js`、`package-lock.json`、`package.json`、`rules.js`。禁止任何子目录、`node_modules/test/scripts/src/docs/evidence/.env`、旧候选或远端下载产物。

旧线上 manifest `9174250a...f17` 已失效于当前 enrichment 源码，不得复用。backend 必须从当前源机械复制、生成逐文件 bytes/SHA-256/总 bytes/新 manifest；QA 独立复算并确认 9/9 源匹配、85/85、依赖锁定与 audit。

### 标准回传

- 完成内容：最终只读复核 canonical/dispatch/worklog，确认首次复核列出的内部旧活跃字段已全部清理。
- 改动文件：本评审报告；`docs/worklog.md` reviewer v2 条目。
- 验证结果：`P0=0 / P1=0 / P2=3`；YAML parse、retry 13=13、fingerprint、write lock、状态层级与 diff-check 均通过。
- 剩余问题：新后端候选与 manifest 尚未生成；真实 Auth 权限、manager/owner 和 transport 仍未验证。
- 建议下一步负责 agent：后端工程师 Agent 本地构建精确 9 文件候选，随后测试工程师 Agent 独立 QA。
- 是否触发暂停条件：否（仅限本地候选构建）；任何远端步骤仍必须暂停等待新授权。
- 无需用户转发，由 Codex 中枢继续分派。

---

## 2026-07-13 CL-1A 治理 P1 首次复核

```text
message_type: review
request_id: REQ-20260713-CL-1A治理P1复核
from_lane: reviewer
to_lane: coordinator
created_at: 2026-07-13T当前时间+08:00
recorded_in: 当前对话 / docs/review/WebUID补全闭环准入评审-20260713.md / docs/worklog.md
```

### 复核结论

- 结论：**未通过**；累计仍为 `P0=0 / P1=1 / P2=3`，不准入 backend 候选构建。
- 已通过部分：dispatch 与 canonical 顶部均记录线上 api `9174250a...f17`、hosting `03110c...40eb`、`IDENTITY_MISSING_OPENID`、RequestId、日志 0、本地 backend 85/85、frontend 28/28、前端新 manifest、当前无远端授权、`write_lock_owner=none` 与 owner 冻结。
- 两份 YAML 均可解析；`retry_count_total=13`，11 个 blocker key 的值求和精确为 13；两份 fingerprint 均为 `e0fc48e9b1930ebea9d5589d74998654b068493378ce4b04dce71bd94311e9ff`。
- `git diff --check` 对 canonical、dispatch、worklog 为 exit 0。

### 唯一剩余 P1

唯一缺口类别是：**两份权威真源内部仍残留旧的“当前/活跃”字段，导致同一文件内自相矛盾。**具体需一次性清理：

1. dispatch 顶部 `local_deploy_preflight_result` 仍写旧 `f52177...`、45/45、9-file ready；当前事实应为 enrichment 后端 QA 85/85，但新 9 文件候选尚未构建、无新 manifest。
2. canonical YAML 的 `request_id` 仍为 20260711 旧请求；应指向当前 enrichment candidate 请求。
3. canonical YAML `local_cloud_function.evidence/note` 仍为 `npm_test_45_passed`；应同步 85/85 与锁定 SDK enrichment 证据。
4. canonical 顶部写 `gui_devtools=pass`、`real_cloud_readonly=fail`，但 YAML 仍分别为 `blocked/blocked`；必须统一为当前层级状态并更新 evidence/note。
5. canonical 中无日期的通用标题 `## 最新进展` 仍把 2026-07-10 包写成“当前远端”；应改成明确的历史标题，或同步为当前状态，避免与顶部“当前真源”冲突。
6. canonical YAML `retry_history` 只列 3 条而 total 为 13；应补齐，或增加明确字段/说明表示它只是 legacy initial excerpt，完整明细以 dispatch 顶部计数为准。

这些项同属一个治理 P1，不涉及业务代码或线上事实变化。coordinator 清理后再次回派 reviewer；通过前不得构建候选或申请任何新授权。

### 标准回传

- 完成内容：只读复核 canonical/dispatch/worklog 的当前版本、累计次数、fingerprint、授权、写锁、blocker release 与 owner 冻结。
- 改动文件：本评审报告；`docs/worklog.md` reviewer 复核条目。
- 验证结果：YAML parse pass；retry `13=13`；fingerprint/write lock/authorization 一致；diff-check pass；但内部旧活跃字段仍造成 P1。
- 剩余问题：仅需 coordinator 清理上述同一类真源内部冲突。
- 建议下一步负责 agent：Codex 中枢修正 canonical/dispatch 后回派验收官 Agent。
- 是否触发暂停条件：是；当前不准入候选构建。
- 无需用户转发，由 Codex 中枢继续分派。

---

## 2026-07-13 fixed preflight 新授权门治理复核

```text
message_type: review
request_id: REQ-20260713-fixed-preflight-authorization-gate-governance-recheck
from_lane: reviewer
to_lane: coordinator
created_at: 2026-07-13T13:10:00+08:00
recorded_in: 当前对话 / docs/review/WebUID补全闭环准入评审-20260713.md / docs/worklog.md
```

### 逐项核验

- dispatch/canonical 顶部权威 YAML 均严格 `yaml.safe_load` 通过；request/status 一致。
- retry 两处均为 `retry_count_total=16`，明细值求和均为 16；技术 parser 失败保留历史累计 1，未被重置。
- 当前 blocker 一致：dispatch 为 `blocked_authorization:fixed_node_preflight_single_deploy_reauthorization_required`，canonical 稳定指纹字段补充 `release=new_exact_authorization`。
- 当前授权为 `none_waiting_new_exact_authorization_after_fixed_node_preflight_pass`；`write_lock_owner=none`，`canonical_owner=coordinator`。
- dispatch/canonical 均明确唯一 preflight 已 exit0、`writes=0`、`remoteExecuted=false`，v1/v2/v3 授权均终止且线上未触碰；owner 三项真实正向前 role 写冻结不变。
- `git diff --check` exit 0，仅既有 CRLF warning。

### 指纹独立复算

实际 canonical JSON 单行：

```json
{"action_set":["getAdminPriceRules","getAdminSession","getAdminStoreDetail"],"actor_role_set":["manager","owner"],"blocker_key":"blocked_authorization:fixed_node_preflight_single_deploy_reauthorization_required:release=new_exact_authorization","business_fixture_revision":"store_demo_001:tables40_disabled3_price0_campaign0_manager1_owner0_admin0","changed_files_digest":"a0520bb158c30e6c07d4eb0220205ff8834ccd337bf8e5433a24ef3d2208a2cb","closure_id":"CL-1A-web-admin-real-readonly","contract_revision":"2fb6bc179a804901eea595906512733f37090861f8c5eb8befa1f4769ee1c858","environment_key":"cloud1-d2gp2ayiwab8f8a94","evidence_layers":["gui_devtools","local_cloud_function","local_mock","local_static","real_cloud_readonly","real_write"],"fingerprint_version":1,"runtime_revision":"online-api:9174250adb8824cb958859f676d78bd267e27a2f4ee59267080a29d52bad7f17;local-candidate:a0520bb158c30e6c07d4eb0220205ff8834ccd337bf8e5433a24ef3d2208a2cb","source_revision":"local-enrichment-candidate:a0520bb158c30e6c07d4eb0220205ff8834ccd337bf8e5433a24ef3d2208a2cb;online-api:9174250adb8824cb958859f676d78bd267e27a2f4ee59267080a29d52bad7f17"}
```

- SHA-256 独立复算为 `be37b79a17a257d87f62c929aae0d8da82a1218b8dbea65c4743b2acfcf6d5f1`，与 dispatch/canonical 两处精确一致。

### 分级与准入

- `P0=0 / P1=0 / P2=2`。
- P2：固定 `_npx` cache 是机器局部入口；当前 CLI 登录/远端服务状态未被本地 preflight 证明。新授权中的首次 env before/部署若失败或 unknown，必须立即停止，不能换入口或重试。
- **准入中枢申请新的精确授权**，但只准入“申请授权”，不准入当前直接执行。授权必须明确：禁止第二次 preflight 和 staging guard；授权后直接从固定绝对 Node+tcb 的唯一 env before 开始；既有 `api` 部署、状态核对、env after、全新目录单次 download 各一次；任何失败/unknown 即停；不包含前端、页面、登录、action/probe/log、DB/role 或其他写操作。

### 标准回传

- 完成内容：纯治理复核两份 YAML、retry、blocker、授权、写锁、owner、远端 0 和稳定指纹。
- 改动文件：本评审报告；`docs/worklog.md`。
- 验证结果：全部通过，P0/P1=0。
- 剩余问题：仅等待用户新的精确授权；真实部署与远端结果仍未发生。
- 建议下一步负责 Agent：Codex 中枢向用户申请授权；获得后由后端工程师 Agent 单次执行。
- 是否触发暂停条件：是；需要新的高风险部署授权。
- 无需用户转发，由 Codex 中枢继续分派。

---

## 2026-07-13 确定性部署运行器最终独立评审

```text
message_type: review
request_id: REQ-20260713-deterministic-deploy-runner-final-review
from_lane: reviewer
to_lane: coordinator
created_at: 2026-07-13T14:33:00+08:00
recorded_in: 当前对话 / docs/review/WebUID补全闭环准入评审-20260713.md / docs/worklog.md
```

### 已独立确认的通过项

- 三次止损根因确为临场 PowerShell 排序、PATH `tcb`、PowerShell JSON 解析；三次真实远端均为 0。
- runner 已移除 PowerShell、PATH/npx、locale sort、fallback 和隐式 retry；固定五步为 `environment_before -> deploy -> status -> environment_after -> download`。
- run token 在本地 guards 与首个 adapter 前 `wx` 创建；candidate/staging exact、remote 预不存在、CLI package name/version/bin、env Namespace/exact、Windows Unicode+空格 cmd quoting、deploy/status/env/download fail-closed 路径均可定位。
- reviewer 独立执行：runner/test `node --check` 通过；全量测试 `35/35`；`validate` 为 `remoteExecuted=false/writes=0` 且 contract SHA-256=`7c628b9dd77e5cf9a7a57e616b0518ae360500b3ee5d7533596fb6d4565efbcc`；`mock` 为 `remoteExecuted=false`；diff-check exit 0。
- 本轮未运行 CLI `execute`、tcb、微信 CLI、npx 或远端；未读取 production candidate/staging，也未重复此前 fixed preflight。

### 独立新增安全复现

仅使用 OS 临时目录、fixture 与 fake adapter；输出为：

```json
{"authorizationAliasReused":true,"first":{"code":"FAKE_STOP","calls":["environment_before"]},"second":{"code":"FAKE_STOP","calls":["environment_before"]},"toolContentMutationAccepted":true,"toolCode":"FAKE_STOP","preexistingSummaryReachedAdapter":true,"summaryCode":"EEXIST","junctionResult":{"created":true,"wroteOutside":true},"remoteExecuted":false}
```

### P0

1. **同一 authorization alias 可换 run id 重放。** 当前只以 run-id hash 生成 token；`authorizationAlias` 没有独占消费。复现使用同一 alias、同一授权文本与同一 scope，仅换两个 UUIDv4 run id 并更新 run digest，两次均到达首个 adapter。应以 authorization alias 作为主一次性 token（`wx`），内部绑定唯一 run alias；同一 alias 成功、失败、guard fail 或崩溃后都不得再次进入 adapter。
2. **授权没有绑定已审查代码和实际工具内容。** `contractSha256` 仅是 redacted config contract 的摘要，不是 runner 源文件摘要；scope 只绑定工具路径与 package metadata。复现在生成 authorization 后修改 fake 微信 CLI 内容，guard 仍接受并进入 adapter。修复后 scope 必须包含并核对 reviewer 审查过的 runner source SHA-256，以及 Node、tcb、cmd、微信 CLI 的实际文件摘要；runner contract SHA 必须把 runner source digest 纳入，不能只靠路径、版本和 package.json。

### P1

1. **stateDir 可经 junction 写出项目且未纳入授权。** `AtomicRunState.begin()` 直接递归创建/打开 stateDir，没有 direct-child/realpath/lstat 检查；临时 junction 实证 token 被写到项目外。stateDir、token/summary 规则必须进入 canonical scope；创建前后均拒绝 symlink/junction/reparse 越界。
2. **summary 未在首个 adapter 前预留。** 预置同 run alias 的 summary、token 不存在时，runner 仍进入 adapter，结束写 summary 才 `EEXIST`，并会遮蔽原失败。`begin()` 必须在任何 guard/adapter 前同时原子预留 authorization token、run state 和 summary fd；后续只能写已预留 fd，任何冲突在 adapter=0 时终止。

### P2

1. guards 与 deploy 之间隔着 env-before，candidate/staging/tool 仍存在 TOCTOU 窗口。修复 P0 后应在 deploy 进程启动前用已授权摘要再核对一次，或使用不可变快照/OS 锁；这属于 runner 内部同一次执行，不得退回临场 preflight。
2. `parseCliArgs()` 对重复 `--run-id/--authorization-file/--confirm` 采用 last-wins。应拒绝任何重复参数和未知参数，保持 fail closed。

### 结论

- `P0=2 / P1=2 / P2=2`。
- 根因方向已结构化改善，但“用户授权只能执行一次”和“已审查 runner/工具不可漂移”仍可绕过；**不准入中枢申请新部署授权，更不得 execute**。
- 回派 backend 修复上述 P0/P1，并由 QA 增加四项定向回归：alias 换 run-id 重放=adapter0、授权后工具/runner 内容漂移=adapter0、state junction=零外写、summary 预存在=adapter0且原始安全错误不被遮蔽；同时回归 35 项、Windows fake batch、parser/env/download、敏感扫描。

### canonical / dispatch 治理建议

- 当前 runner 已改变被测源与授权合同，现有 `be37...` 指纹必须失效。当前缺陷版本的精确摘要：runner=`eff9ceb48db56da59d351c368364e389cbf0b3700ddf5b5c32c1212bf92a7b02`、test=`c9c8aaef69140dd5ff8c276e8d9175de0eda125311660a6069d84e0fc71eb41f`、README=`e2889b7bd8a3cd8ffca12e8fc7bd501310c41f569fd8cc1e2252b108b1c4f3ed`。
- `changed_files_digest` 建议固定算法为“规范化相对路径排序后，每行 `path<TAB>fileSha256`，LF 拼接，再 SHA-256”；当前三文件结果=`5f4c037bce7eab60052c73a3d0c4391e4a1ee09d36ce2dc2c7eaaeaac25d40d6`。
- 当前 defect 合同建议 `contract_revision=a9d2508e0368ce3ee70367d2c36e750413caf997d43050311bb271056c887450`；blocker=`blocked_defect:deterministic_runner_authorization_and_state_integrity:release=backend_fix_and_qa_retest`；按既有其他稳定字段计算的临时 evidence fingerprint=`685884ba1e2a32bc86afbea79a317faed1cdcddb1e449e17e1fc08ee53051815`。
- 上述 fingerprint 只用于标识当前缺陷版本，不得写入未来授权；修复后必须用新 runner/test/README/contract 摘要重新计算。`retry_count_total` 建议保持 16：本轮是纯本地实现/QA/review，未发生新的授权执行或远端尝试；blocker 与指纹应更新，但不虚增真实尝试计数。

### 修复后新授权必须绑定的边界

- 用户授权文本与 schema v2 同时列出：唯一 `auth-UUIDv4`、唯一 `run-UUIDv4`、runner source SHA、runner contract SHA、scopeDigest、四个实际工具摘要、candidate/staging/remote/state 路径与 exact 文件合同、环境/函数/AppID/env-value digest、五步顺序和各一次预算。
- 只允许该 runner 单一入口；禁止 Agent 临场拼 PowerShell、PATH/npx、help/version、fallback 或替代 CLI。允许 runner 在主 authorization token 封存后执行一次内部 guards；不得另跑此前 fixed preflight。
- 精确允许本地写：一次 authorization/run token、append-only JSONL、预留 summary、一次全新 remote verify 目录；其余本地/远端写禁止。
- 任一 guard、env-before、deploy、status、env-after、download 失败/unknown/timeout，authorization alias 永久消费，后续步骤 0，不得换 run id、换 alias 复用同一授权、换工具或重试。
- 不包含 action/probe/log、DB/role、环境变量写、seed/delete/migration、hosting、小程序、审核或 git push；依赖风险需在新授权中重新受限接受。

### 标准回传

- 完成内容：完整规则/止损/runner/tests/reports 审查，独立 35 项与四类不可绕过复现，治理指纹和授权边界评审。
- 改动文件：本 review 报告、`docs/worklog.md`。
- 验证结果：基础矩阵通过，但新增 P0/P1 阻塞成立。
- 剩余问题：授权 alias、runner/tool 内容绑定、state 路径与 summary 预留。
- 建议下一步负责 Agent：后端工程师 Agent 修复；测试工程师 Agent复测后回派验收官 Agent。
- 是否触发暂停条件：`blocked_defect`；用户暂不需要操作，不得申请部署授权。
- 无需用户转发，由 Codex 中枢继续分派。
## 2026-07-13 22:27 +08:00 — deterministic runner 最终独立评审：授权文件严格解析阻断

```text
message_type: review
request_id: REQ-20260713-181800-runner-final-review
from_lane: reviewer
to_lane: coordinator
created_at: 2026-07-13T22:27:15+08:00
recorded_in: 当前对话 / docs/review/WebUID补全闭环准入评审-20260713.md / docs/worklog.md
```

### 结论

- **未通过，`P0=1 / P1=1 / P2=0`；不得 git push，不得申请部署授权，不得执行 runner `execute`。**
- 独立复跑本身全绿：主测试 `78/78`、独立 QA 完整性矩阵 `23/23`；三个 `node --check`、`validate`、`mock` 均通过。`validate` contract SHA-256 为 `c02ef65b8cdc34a0d5203ada7decb2c2dfc4305c6d84eb55b86c16bbe5f4b1e2`，`remoteExecuted=false / writes=0`。
- 全绿矩阵遗漏了生产 CLI 入口读取 `--authorization-file` 的严格 JSON 边界，因此不能据此签字放行。

### P0：authorization-file 重复键可被 last-wins 接受

- 生产入口 `main()` 对授权文件直接执行普通 `JSON.parse(...)`。含两个同名 `schemaVersion` 的原始授权 JSON 会被解析为最后一个值；随后 `validateAuthorizationEnvelope()` 接受该对象。
- 最小只读复现结果：`duplicateKeysInRaw=2`、`effectiveSchemaVersion=3`、`duplicateAcceptedByAuthorizationEnvelope=true`、`adapterCalls=0`、`remoteExecuted=false`、`writes=0`。
- 当重复键最后一项组成有效 canonical 授权时，现有生产路径不会因“原始授权文本存在重复键”而停止，可继续进入一次性状态账本和真实 adapter 路径。这违反 bug 真源“安全边界 JSON 必须拒绝重复键”的永久规则，也使人审授权文本与机器实际解释产生歧义，属于真实部署授权门的 P0。

### P1：授权根 schema 与固定工具 metadata 未统一严格解析

- `validateAuthorizationEnvelope()` 未拒绝授权根对象的额外字段；最小复现 `extraTopLevelFieldAcceptedByAuthorizationEnvelope=true`。授权根应固定为精确七字段：`schemaVersion`、`authorizationAlias`、`runIdSha256`、`userAuthorizationText`、`userAuthorizationTextSha256`、`scope`、`scopeDigest`，并校验字段类型、格式、版本、缺失和额外字段。
- `validateFixedTools()` 读取固定 CloudBase CLI 的 `package.json` 仍使用普通 `JSON.parse`。虽然原始 package bytes 已纳入工具内容摘要，当前不会造成未绑定内容漂移，但它仍是工具身份安全边界，应和授权文件一样拒绝重复键及非精确 contract metadata。
- 已使用严格解析的入口应保留：prior token JSONL、prior summary、CloudBase environment detail。不得弱化或删除其现有 permanent regression。

### 必须新增的永久回归

1. 生产 authorization-file 根级和嵌套任意对象重复键均在任何 state lock、adapter、远端操作前 fail closed。
2. authorization-file 根级额外字段、缺失字段、错误类型、错误版本、错误格式均 fail closed；adapter `0`。
3. 固定 CLI `package.json` 根级和 `bin` 内重复键均 fail closed；adapter `0`。
4. 保留并全量回归现有主测试 78 项、QA 23 项及所有历史 P0/P1 fixture，不得删除、跳过或改成弱断言。

### Git 与真实操作边界

- 当前 branch 为 `codex/v3-backend-capability`，upstream 为 `origin/codex/v3-backend-capability`，remote 为 `https://github.com/Disengage0617/my-project.git`，本地 HEAD 与 upstream 当时均为 `86ffb9c`。
- 工作区约有 538 个 porcelain 条目，混有大量 `.tmp*`、图片、`miniprogram/`、`web-admin/`、历史文档和生成物；整库 `git add`/commit/push 风险不可接受。
- 本轮真实 execute、tcb、微信 CLI、env、deploy、status、download、action、log、DB、role、hosting、miniprogram、git push 全部为 `0`。
- 下一步必须回派后端只修上述严格解析和测试，再由独立 QA 跑全矩阵、reviewer 重审、coordinator 更新 canonical/dispatch；只有新一轮 `P0/P1=0` 后才重新做精确 Git include/exclude 预检并消费用户的单次普通 push 授权。
## 2026-07-13 23:40 +08:00 — deterministic runner strict input 最终复审通过

```text
message_type: review
request_id: REQ-20260713-231900-runner-final-rereview
from_lane: reviewer
to_lane: coordinator
created_at: 2026-07-13T23:40:00+08:00
recorded_in: 当前对话 / docs/review/WebUID补全闭环准入评审-20260713.md / docs/worklog.md
```

### 最终结论

- `P0=0 / P1=0 / P2=0`，上轮 authorization-file 重复键/根 schema 与固定 CLI package strict parser 缺口均已关闭。
- reviewer 独立复跑主测试 `89/89`、独立 QA 永久矩阵 `34/34`；runner、主测试、QA fixture 三个 `node --check`，以及 `validate`、`mock` 全部通过。
- `validate` contract SHA-256=`1bb909bdb1e28723849cf2dcf59424f19294a1f058e767c135fa7ee50f2f0d61`，`remoteExecuted=false / writes=0`。
- production main 的 authorization raw input 七类负例和固定 package 三类边界均在 state/token/lock/adapter 前拒绝；源码直接 `JSON.parse` 仅剩 strict scanner 内部的字符串 token 与整文扫描后解析两处。
- 历史 P0/P1 fixture 未删除、未跳过、未弱化；89+34 的测试名仍覆盖 alias/scope/text replay、tool content、state/junction、summary/token/lock、最终 deploy gate、Windows argv、ledger schema/语义/外部锚及本轮 strict input。

### 当前证据摘要

- runner=`aa7262ed22c98e25e8d525c30b101dab828a8d09a434b7ae0021b7876af767f7`
- main test=`bad1fa79de8ecd243c4de93b22350de34d78efa417269f50ee50c024cac5accb`
- QA fixture=`e870e911cd039b9b191c068595cf334b661c0db6a71c0102643055bb5771b936`
- README=`149c23e87a9122802878eda27a753b61cb472b74d37ac17f1c183689454e6638`
- 四个 runner 核心文件按 `path<TAB>sha256`、路径排序、LF 拼接的 `changed_files_digest=631da33d8c508dc24e1f194ae30694fc94d7cc6f2537d45ff436407e3174828d`。
- `contract_revision=1bb909bdb1e28723849cf2dcf59424f19294a1f058e767c135fa7ee50f2f0d61`。
- blocker 更新为 `blocked_authorization:deterministic_runner_single_execute:release=new_exact_cloudbase_authorization_bound_to_current_contract` 后，按 12 个稳定字段 canonical JSON 复算的建议 `evidence_fingerprint=a49c500ad6f423046d624cdfba77bc2d900c7ce070798b2126d5f9601b2985ac`。

### Git 只读预检与精确提交范围

- branch=`codex/v3-backend-capability`；upstream=`origin/codex/v3-backend-capability`；remote=`https://github.com/Disengage0617/my-project.git`；本地 HEAD 与 upstream 均为 `86ffb9c77d8841f67e881fd4bbb5d08a24829aea`。
- 工作区约 538 个 porcelain 条目，禁止 `git add .`、`git add -A`、整库 commit 或任何 force push。
- 精确允许提交仅以下 8 个路径：
  1. `scripts/cloudbase/deterministic-api-deploy-runner.js`
  2. `scripts/cloudbase/test/deterministic-api-deploy-runner.test.js`
  3. `scripts/cloudbase/test/deterministic-api-deploy-runner.qa-integrity.test.js`
  4. `scripts/cloudbase/README.md`
  5. `docs/bugs/CloudBase确定性部署重复失败根因与防复发-20260713.md`
  6. `docs/backend/CloudBase-api确定性单次部署运行器-20260713.md`
  7. `docs/testing/CloudBase-api确定性单次部署运行器QA-20260713.md`
  8. `docs/review/WebUID补全闭环准入评审-20260713.md`
- 明确排除：`docs/agent-dispatch-state.md`、canonical、`docs/worklog.md`、`AGENTS.md`、index/registry/changelog/治理文档，以及所有 `.tmp*`、candidate/staging/remote verify、`cloudfunctions/`、`miniprogram/`、`web-admin/`、截图、图片、dist、node_modules、日志及其他未列路径。这些路径存在累计混合差异或不属于本次最小 runner 提交。
- 对更宽的 18 个候选治理/runner 文件执行脱敏模式扫描为 0 命中；其中一个被排除治理文档有单处尾随空白，因此不得扩大到该 18 文件集合。本次 8 文件最小集合无该卫生问题。
- 准入 coordinator 分派一次精确 path-scoped 普通 commit，再执行一次普通非强制 push；push 前必须核对 staged name-only 精确等于上述 8 路径、`git diff --cached --check` 通过、HEAD/upstream 仍未漂移。禁止 force、删除远端分支或夹带其他文件。

### 后续 CloudBase 边界

- 准入 coordinator 更新 canonical/dispatch 的 contract、hash、fingerprint 与 blocker；该治理更新不纳入上述最小 Git commit。
- 准入更新后向用户另行申请绑定当前 contract/fingerprint 的一次精确 CloudBase execute 授权；当前 Git 授权不得解释为 CloudBase 授权。
- 残余边界仍是同一 OS 用户整体删除/一致改写 trusted stateRoot，以及 `spawnSync` 后外部 CLI 按 pathname 打开 staging 前的 OS 级竞态；需要 ACL/运维保护或未来 native share-lock/授权不可变副本，不构成本轮 declared runner scope 内 P2。
- 本轮真实 execute、tcb、微信 CLI、env、deploy、status、download、action、log、DB、role、hosting、miniprogram、git push 全部为 0。
