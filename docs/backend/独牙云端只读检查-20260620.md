# 独牙云端只读检查 - 2026-06-20

message_type: handoff
request_id: REQ-20260620-独牙云端只读检查
from_lane: backend
to_lane: coordinator
created_at: 2026-06-20T当前时间+08:00
recorded_in: 当前对话 / docs/backend/独牙云端只读检查-20260620.md / docs/worklog.md

消息内容：
- 检查范围：
  - 项目 AppID：`wx6a196f6ebfb27596`。
  - 小程序云环境：`cloud1-d2gp2ayiwab8f8a94`。
  - 云函数：`api`。
  - 本地调用链：`miniprogram/app.js`、`miniprogram/services/api.js`、`cloudfunctions/api/index.js`、`cloudfunctions/api/src/handlers.js`。
  - 已有云函数下载快照：`.tmp_cloud_download/`、`.tmp_cloud_download_root/`。
  - 未执行范围：未部署、未上传、未写真实云数据库、未初始化/迁移数据、未删除/重置/覆盖数据、未跑微信开发者工具 MCP。
- 只读证据：
  - `miniprogram/app.js` 指向 `cloud1-d2gp2ayiwab8f8a94`，全局 `storeId` 为 `store_demo_001`。
  - `project.config.json` 的 `appid` 为 `wx6a196f6ebfb27596`，`cloudfunctionRoot` 为 `cloudfunctions/`。
  - 微信开发者工具 CLI 只读命令 `cloud env list` 返回 `cloud1-d2gp2ayiwab8f8a94`，确认当前账号/项目可见该云环境。
  - 微信开发者工具 CLI 只读命令 `cloud functions list --env cloud1-d2gp2ayiwab8f8a94` 返回云函数列表，包含 `api`。
  - 微信开发者工具 CLI 只读命令 `cloud functions info --env cloud1-d2gp2ayiwab8f8a94 --names api` 返回 `api` 状态为 `Active`，运行时为 `Nodejs16.13`，超时为 `3` 秒。
  - 当前本地 `cloudfunctions/api/src/handlers.js` 已包含 `getAvailableTables` 的 `table_type/tableType` 过滤，并返回 `list/tables/items/total`；`bootstrapMvp` 已包含 A/B/C/D/E 共 40 张桌台，其中 C01-C08 为 `table_type: "duya"`、`table_type_name: "独牙"`。
  - 已有 `.tmp_cloud_download/src/handlers.js` 快照中，`getAvailableTables` 只按 `{ store_id, enabled: true }` 查询并返回 `{ list }`，没有读取 `payload.table_type/tableType`，也没有 `bootstrapMvp`、`duya`、`C01` 相关代码；该快照目录时间为 `2026-06-14 13:12:58`，只能作为历史云函数下载快照旁证，不能等同于本轮实时云端源码。
  - 当前微信开发者工具 CLI 帮助只暴露 `env list`、`functions list/info/download/deploy/inc-deploy` 等能力；未发现可直接只读调用云函数 action 或查询云数据库集合的 CLI 子命令。本轮未执行 `download`，避免产生新的本地代码覆盖或混淆证据。
- 真实原因判断：
  - 已确认真实云环境存在，且 `api` 云函数存在并处于 `Active`。
  - 结合用户 DevTools 实测“独牙 0 张”、本地 mock/本地后端均已有 C01-C08，以及历史下载快照仍是旧版 `getAvailableTables`，当前最可能原因仍是：用户运行时调用的真实云端 `api` 或真实云数据库不是当前本地 P0 修复后的 40 桌口径。
  - 更细分地看，存在两类高概率原因：一是云函数 `api` 尚未部署当前本地新版，导致桌型过滤/初始化 action 不一致；二是真实云数据库 `billiard_table` 仍是历史 3 桌或缺 C01-C08/`table_type=duya` 数据。当前 CLI 只读能力不足以直接区分这两者。
  - 不能把本轮结果写成“已修复”。本轮只确认了云环境/云函数存在，并收窄了问题范围。
- 需要部署授权：
  - 需要，但仅在下一步确认 `api` 云函数云端代码确为旧版或缺少 `getAvailableTables` 桌型过滤时才执行。
  - 本轮没有部署授权，也未执行 `deploy/inc-deploy/upload`。
- 需要初始化/迁移数据授权：
  - 需要，但仅在下一步确认真实云数据库 `billiard_table` 缺 C01-C08、`table_type=duya` 或状态/启用字段不符合口径时才执行。
  - 本轮没有写库授权，也未执行 `bootstrapMvp`、`seed.js`、`manageTables` 或任何写 action。
- 改动文件：
  - `docs/backend/独牙云端只读检查-20260620.md`
  - `docs/worklog.md`
- 验证结果：
  - 已执行只读云环境查询：通过，返回 `cloud1-d2gp2ayiwab8f8a94`。
  - 已执行只读云函数列表查询：通过，返回列表包含 `api`。
  - 已执行只读 `api` 云函数信息查询：通过，`api` 为 `Active / Nodejs16.13 / timeout 3`。
  - 已核对本地项目配置和调用链：小程序运行时默认走 `wx.cloud.callFunction({ name: "api", data: { action, payload } })`。
  - 已核对本地新版后端代码：本地已具备 40 桌和 `duya` 筛选逻辑。
  - 已核对已有云函数下载快照：历史快照缺少 `duya` 筛选和 `bootstrapMvp` 40 桌逻辑。
  - 待执行：`git diff --check -- docs/backend/独牙云端只读检查-20260620.md docs/worklog.md`。
- 未实测范围：
  - 未实时下载当前云端 `api` 源码；因此不能最终确认云端代码是否仍旧。
  - 未只读调用 `getAvailableTables({ table_type: "duya" })`；当前 CLI 未提供 action 调用入口。
  - 未只读查询真实云数据库 `billiard_table`；当前 CLI 未提供数据库集合查询入口。
  - 未验证用户 DevTools 页面网络返回体、缓存状态、编译产物状态。
- 建议下一步负责 agent：
  - 后端工程师 Agent：在获得更明确的只读工具入口后，优先实时读取/下载当前云端 `api` 代码或只读调用 `getAvailableTables`。
  - 测试工程师 Agent：若后端拿到只读返回体，再对照用户页面现象做复核。
  - Codex 中枢：若确认旧函数，申请“部署云函数授权”；若确认旧数据，申请“初始化或迁移测试数据授权”。
- 是否触发暂停条件：
  - 是。当前已到只读 CLI 能力边界；继续区分“旧函数”与“旧数据”需要以下任一项：可只读调用云函数 action 的工具/会话、可只读查看云数据库集合的控制台/工具、或允许下载当前云端 `api` 到隔离目录进行版本比对。
  - 写入修复必须另行授权：部署云函数属于部署授权；补齐 C01-C08 属于真实云数据库初始化/迁移授权。

## 本轮命令摘要

```text
& 'E:\微信web开发者工具\cli.bat' cloud env list --project 'C:\Users\宋\Documents\桌球小程序' --appid wx6a196f6ebfb27596 --lang zh
结果：返回 cloud1-d2gp2ayiwab8f8a94

& 'E:\微信web开发者工具\cli.bat' cloud functions list --project 'C:\Users\宋\Documents\桌球小程序' --appid wx6a196f6ebfb27596 --env cloud1-d2gp2ayiwab8f8a94 --lang zh
结果：返回云函数列表，包含 api

& 'E:\微信web开发者工具\cli.bat' cloud functions info --project 'C:\Users\宋\Documents\桌球小程序' --appid wx6a196f6ebfb27596 --env cloud1-d2gp2ayiwab8f8a94 --names api --lang zh
结果：api Active，runtime Nodejs16.13，timeout 3
```

## Handoff 回传

message_type: handoff
request_id: REQ-20260620-独牙云端只读检查
from_lane: backend
to_lane: coordinator
created_at: 2026-06-20T当前时间+08:00
recorded_in: 当前对话 / docs/backend/独牙云端只读检查-20260620.md / docs/worklog.md

消息内容：
- 检查范围：`wx6a196f6ebfb27596` / `cloud1-d2gp2ayiwab8f8a94` / 云函数 `api` / 本地调用链 / 历史云函数下载快照；未部署、未写库、未初始化/迁移。
- 只读证据：云环境列表确认 `cloud1-d2gp2ayiwab8f8a94`；云函数列表确认存在 `api`；`api` 元信息为 `Active / Nodejs16.13 / timeout 3`；本地新版已具备 40 桌和 `duya` 过滤；历史下载快照缺少 `duya` 过滤和 40 桌 `bootstrapMvp`。
- 真实原因判断：真实云端旧函数或真实云数据库旧数据仍是最高概率原因；本轮不能最终区分二者，也不能声称修复完成。
- 需要部署授权：需要，前提是下一步确认云端 `api` 仍为旧版。
- 需要初始化/迁移数据授权：需要，前提是下一步确认真实 `billiard_table` 缺 C01-C08 或字段/状态不符合口径。
- 改动文件：`docs/backend/独牙云端只读检查-20260620.md`、`docs/worklog.md`。
- 验证结果：只读 CLI 环境/云函数查询通过；本地文件和历史下载快照核对完成；`git diff --check` 待最终命令确认。
- 未实测范围：未只读调用 `getAvailableTables`，未查真实 DB 集合，未实时下载当前云端源码，未验证 DevTools 页面返回体/缓存。
- 建议下一步负责 agent：Codex 中枢先决定是否申请更高一档只读工具入口或隔离下载当前云端 `api`；确认旧函数后交后端部署，确认旧数据后交后端做受控数据迁移方案。
- 是否触发暂停条件：是。继续定位需要额外只读工具/会话；任何修复动作都需要部署或真实数据库写入授权。
