# 独牙真实 DB 只读核对 - 2026-06-20

message_type: handoff
request_id: REQ-20260620-独牙真实DB只读核对
from_lane: backend
to_lane: coordinator
created_at: 2026-06-20T22:30:00+08:00
recorded_in: 当前对话 / docs/worklog.md / docs/backend/独牙真实DB只读核对-20260620.md

消息内容：
- 只读查询方式：
  - 已尝试确认本机可用的真实云数据库只读入口，但当前未能直接读取真实 DB。
  - 微信开发者工具 CLI 只暴露 `cloud env` 与 `cloud functions`；`cloud functions` 仅支持 `list/info/deploy/inc-deploy/download`，没有 DB query，也没有 action invoke。
  - 本地 `wx-server-sdk` 直调真实云函数需要腾讯云 `secretId/secretKey`；当前环境变量未配置相关密钥。
  - 因此，本轮没有拿到真实 `billiard_table` 集合返回体，也没有拿到真实 `getAvailableTables` action 返回体。
- 查询命令或工具：
  - CLI 能力核对：

```text
& 'E:\微信web开发者工具\cli.bat' cloud --help
结果：仅 env / functions。

& 'E:\微信web开发者工具\cli.bat' cloud functions --help
结果：仅 list / info / deploy / inc-deploy / download。
```

  - 本机命令与凭据核对：

```text
Get-Command tcb,cloudbase,tccli -ErrorAction SilentlyContinue
结果：未发现可用命令。

TENCENTCLOUD_SECRETID / TENCENTCLOUD_SECRETKEY / TCB_SECRETID / TCB_SECRETKEY
结果：环境变量均未配置。
```

  - SDK 只读调用尝试：

```text
node -e "... cloud.callFunction({ name:'api', data:{ action:'getDebugContext', payload:{ storeId:'store_demo_001' } } }) ..."
结果：INVALID_PARAM: callFunction:fail missing secretId or secretKey of tencent cloud
```

  - DevTools 本地 HTTP 探测：

```text
http://127.0.0.1:37660/
http://127.0.0.1:37660/status
http://127.0.0.1:37660/cloud
结果：均为 Cannot GET ...，未发现可用 DB/action 只读端点。
```

- 返回摘要：
  - 真实 DB：未取得返回体。没有真实 `billiard_table` 数据证据。
  - 真实 action：未取得返回体。没有真实 `getAvailableTables` 返回摘要。
  - 本地文件/页面调用链：已只读核对，预约页会传 `storeId/store_id=store_demo_001` 与 `tableType/table_type=duya`，页面兼容 `list/items/tables` 和 `duya/独牙/Cxx`。
- C01-C08 是否存在：
  - 未能通过真实 DB 只读查询确认。
  - 当前不能断言存在或不存在；只能维持上一轮判断：真实 DB 缺 C01-C08 或字段不符合是最高概率根因。
- 字段是否符合：
  - 未能通过真实 DB 只读查询确认 `store_id/storeId`、`table_type/tableType/typeKey/type`、`enabled`、`status`。
  - 待核对目标字段：
    - `store_id` 或 `storeId` 是否为用户当前页面实际 store，默认先查 `store_demo_001`。
    - C01-C08 是否存在。
    - `table_type/tableType/typeKey/type` 是否为 `duya` 或 `独牙`。
    - `enabled` 是否为 `true`。
    - `status` 是否为空闲/可预约：`idle/free/available/空闲`。
- 失败根因判断：
  - 用户截图失败仍是 P0，不能放行。
  - 在云端 `api` 已部署新版、前端 payload/映射只读核对未发现高概率问题、且本轮仍无法证明真实 DB 已有 C01-C08 的情况下，当前最可能根因仍是真实 `billiard_table` 数据缺失、字段不符合或 `store_id` 不匹配。
  - 但因为没有真实 DB/action 返回体，本轮只能给出概率判断，不能给出最终数据结论。
- 是否需要写库授权：
  - 当前还不能直接申请执行写库，因为 before 证据尚未建立。
  - 下一步必须先取得真实 DB 只读结果；若确认缺 C01-C08 或字段不符合，再由中枢申请单独 DB 写入/初始化/迁移授权。
- 写库前 before/after 方案草案：
  - before 必须记录：
    - `billiard_table` 在目标 store 下总数。
    - A/B/C/D/E 各桌型数量。
    - C01-C08 的 `_id/code/table_no/store_id/table_type/table_type_name/enabled/status/area_name/price_cent/sort_order`。
    - 如果当前页面实际 `storeId` 不是 `store_demo_001`，必须按实际 store 再记录一份。
  - after 最小方案草案：
    - 只补目标 store 缺失或字段错误的 C01-C08。
    - C01-C08 字段目标：`store_id=store_demo_001`，`code/table_no=C01-C08`，`table_type=duya`，`table_type_name=独牙`，`area_name=独牙区`，`enabled=true`，`status=idle`，`price_cent=5800`，`sort_order=23..30`。
    - 不删除、不重置、不覆盖非目标数据；已有 C 组记录仅修必要字段。
- 下一步负责 agent：
  - Codex 中枢：需要用户提供可执行的真实 DB 只读入口。可选方式：
    1. 在微信云开发控制台人工只读查看 `billiard_table`，提供 C01-C08 和字段截图/导出。
    2. 提供腾讯云/CloudBase 只读凭据或已登录可用的 `tcb/cloudbase/tccli` 工具。
    3. 允许使用微信开发者工具数据库面板人工只读核对并回传字段摘要。
  - 后端工程师 Agent：拿到只读入口后只读核对 `billiard_table`；如果数据正确，再转前端工程师 Agent 抓真实 payload/response。
  - 前端工程师 Agent：仅在 DB 正确但页面仍 0 张时接手，核对真实页面返回体与页面过滤。
- 改动文件：
  - `docs/backend/独牙真实DB只读核对-20260620.md`
  - `docs/worklog.md`
- 验证结果：
  - 已只读核对 CLI 能力：无 DB query/action invoke。
  - 已只读核对本机命令：无 `tcb/cloudbase/tccli`。
  - 已只读核对密钥环境变量：无腾讯云/CloudBase 密钥。
  - 已尝试 SDK 只读调用：失败于缺少 `secretId/secretKey`，未触达业务 action。
  - 已只读探测 DevTools 本地 HTTP 常见端点：未发现 DB/action 入口。
  - 待执行：`git diff --check -- docs/backend/独牙真实DB只读核对-20260620.md docs/worklog.md`。
- 未实测范围：
  - 未读取真实 `billiard_table`。
  - 未真实调用 `getAvailableTables`。
  - 未写库、未初始化、未迁移、未部署、未回滚、未改业务代码。
- 是否触发暂停条件：
  - 是。当前授权范围是 DB 只读，但本机缺少可执行的 DB 只读工具/凭据。继续需要用户提供 DB 只读入口；任何写库、初始化、迁移、删除、重置、部署或回滚仍需另行明确授权。

## Handoff 回传

message_type: handoff
request_id: REQ-20260620-独牙真实DB只读核对
from_lane: backend
to_lane: coordinator
created_at: 2026-06-20T22:30:00+08:00
recorded_in: 当前对话 / docs/worklog.md / docs/backend/独牙真实DB只读核对-20260620.md

消息内容：
- 只读查询方式：尝试使用 DevTools CLI、本机 CloudBase/Tencent CLI、本地 `wx-server-sdk` 与 DevTools HTTP；均未形成可用真实 DB 只读查询。
- 查询命令或工具：DevTools CLI 只有 env/functions，functions 无 DB/query/invoke；本机无 `tcb/cloudbase/tccli`；环境变量无腾讯云密钥；SDK 调用失败 `missing secretId or secretKey`。
- 返回摘要：未取得真实 DB/action 返回体；只取得工具能力与鉴权失败证据。
- C01-C08 是否存在：未能确认。
- 字段是否符合：未能确认。
- 失败根因判断：仍以真实 `billiard_table` 数据缺失、字段不符或 `store_id` 不匹配为最高概率，但缺真实 DB/action 证据，不能下最终结论。
- 是否需要写库授权：暂不能执行写库；必须先建立 DB before 证据。若只读确认缺 C01-C08 或字段不符，再申请写库/初始化/迁移授权。
- 写库前 before/after 方案草案：before 记录目标 store 的桌台总数、C01-C08 全字段、桌型/status/enabled 分布；after 最小补齐或修正 C01-C08 为 `duya/独牙/enabled=true/status=idle`，不删不重置不覆盖非目标数据。
- 下一步负责 agent：Codex 中枢提供 DB 只读入口或用户控制台截图/导出；后端工程师 Agent 再做真实 DB 只读核对；DB 正确后转前端工程师 Agent 抓真实 payload/response。
- 改动文件：`docs/backend/独牙真实DB只读核对-20260620.md`、`docs/worklog.md`。
- 验证结果：工具能力核对和 SDK 只读调用边界核对完成；`git diff --check` 待最终命令确认。
- 未实测范围：未读取真实 DB、未调用真实 action、未写库、未部署、未改业务代码。
- 是否触发暂停条件：是。继续需要真实 DB 只读入口；写库/初始化/迁移需另行授权。
