# P0 功能模块实施任务包 - 2026-06-20

message_type: status
request_id: REQ-20260620-P0功能模块实施任务包
from_lane: coordinator
to_lane: backend / frontend / qa / reviewer
created_at: 2026-06-20T当前时间+08:00
recorded_in: 当前对话 / docs/P0功能模块实施任务包-20260620.md / docs/worklog.md

## 背景

本任务包由 Codex 中枢合并以下三份回传生成：

- `docs/product/功能模块补齐计划-20260620.md`
- `docs/backend/后端功能模块缺口盘点-20260620.md`
- `docs/frontend/前端功能模块缺口盘点-20260620.md`

当前阶段只补功能模块，不进入前后端 UI 风格调整。若后续需要视觉重做或样式模板选择，中枢暂停提醒用户提供新的 UI 样式调整模板。

## P0 后端任务

下一步负责 agent：后端工程师 Agent  
lane：backend  
线程：`019ee328-dcbe-70d3-aec7-28259a637b45`

### 目标

把后端从“基础 action 可用”收口到 P0 生产风险可控：

- 统一接口真源文档与当前 `{ ok, data/code/message }` 响应协议。
- 补活动报名审核 action 与名额并发保护。
- 补手机号授权/登录角色 action 的本地 mock 或可测试入口。
- 补商家配置字段级校验与 `manageTables:disable` 有效预约保护。
- 补分页/筛选基础参数。
- 补对应本地测试。

### 允许改动范围

- `cloudfunctions/api/`
- `docs/backend/`
- 后端测试文件

### 禁止改动范围

- 不改 `miniprogram/`
- 不改 `web-admin/`
- 不部署云函数
- 不写真实数据库
- 不做 UI 样式调整

## P0 前端任务

下一步负责 agent：前端工程师 Agent  
lane：frontend  
线程：`019ee329-50c7-7040-b035-84a269e9f310`

### 目标

补齐商业模板第一版最影响用户体验的前端功能，不做视觉风格重做：

- 预约页增加日期选择。
- 统一 `selectedDate + startTime + durationHours` 生成起止时间。
- `getAvailableTables` 随日期/时间/时长/桌型刷新，并兼容 `list/items/tables` 返回。
- 桌台卡展示可用/不可用/维修/停用/使用中状态，不可用禁选。
- 增加预计价格摘要。
- 我的预约增加状态筛选 tabs、详情入口、状态文案统一、差异化按钮。

### 允许改动范围

- `miniprogram/pages/reservation/`
- `miniprogram/pages/my-reservations/`
- `miniprogram/pages/reservation-detail/` 必要的小范围入口兼容
- `miniprogram/services/api.js`
- `miniprogram/utils/constants.js`
- `miniprogram/utils/mock.js`
- `docs/frontend/` 前端实现报告

### 禁止改动范围

- 不改 `cloudfunctions/`
- 不改 `web-admin/`
- 不做 UI 风格重做
- 不部署
- 不写真实数据库

## QA 预备口径

后端和前端完成后交测试工程师 Agent：

- 后端：`cloudfunctions/api npm test` 必须通过。
- 前端：小程序 JS、JSON、WXML 静态检查必须通过。
- 回归范围：预约页日期/时间/时长/桌型切换、预计价格、不可用桌台禁选、预约提交、我的预约筛选、详情入口、到店按钮状态。
- 明确区分：静态检查、本地 mock、云函数本地测试、微信开发者工具/真机实测、真实云端联调。

## 暂停条件

- 需要部署云函数。
- 需要写真实云数据库。
- 需要真实价格、真实桌台编号、真实助教资料。
- 需要微信开发者工具截图或真机确认。
- 进入 UI 风格调整或视觉重做。
