# P0 功能模块 QA 回归报告 - 2026-06-20

message_type: handoff
request_id: REQ-20260620-P0功能模块QA回归
from_lane: qa
to_lane: coordinator
created_at: 2026-06-20T12:11:41+08:00
recorded_in: 当前对话 / docs/worklog.md / docs/testing/P0功能模块QA回归报告-20260620.md

## 1. 本轮边界

- 本轮执行 agent：测试工程师 Agent。
- 本轮范围：P0 功能模块本地分层 QA 回归。
- 本轮不做：不改业务代码、不部署、不写真实数据库、不做 UI 风格调整、不创建/管理线程、不做最终上线验收。
- 输入依据：`docs/testing/功能模块补齐QA口径-20260620.md`、`docs/backend/P0后端功能补齐报告-20260620.md`、`docs/frontend/P0前端预约功能补齐报告-20260620.md`、`docs/P0功能模块实施任务包-20260620.md`。

## 2. 通过项

| 分层 | 检查项 | 结果 |
|---|---|---|
| 后端本地测试 | 在 `cloudfunctions/api` 执行 `npm test` | 通过，29/29，fail 0 |
| 后端语法 | `node --check src/handlers.js`、`node --check test/handler-flow.test.js` | 通过 |
| 小程序 JS | `node --check` 覆盖预约页、我的预约、预约详情、`services/api.js`、`mock.js`、`constants.js` | 通过 |
| 小程序 JSON | 解析 `app.json`、预约页、我的预约、预约详情 JSON | 通过 |
| 小程序 WXML | 检查预约页、我的预约、预约详情 WXML 标签闭合 | 通过 |
| 页面入口 | 检查 `app.json` 已登记预约页、我的预约、预约详情，且 `.js/.wxml/.wxss/.json` 文件齐全 | 通过 |
| mock action | `getAvailableTables` 返回 `list/items/tables`，支持 `tableType/table_type` 筛选，包含不可用桌台 | 通过 |
| mock action | `getMyReservations` 支持状态筛选和分页字段 | 通过 |
| mock action | `loginByWechat`、`authorizePhone`、`getCurrentRole`、`reviewCampaignRegistration` 本地 mock 可调用 | 通过 |
| 错误码文案 | 检查 `TABLE_HAS_ACTIVE_RESERVATION`、`CAMPAIGN_FULL`、`DUPLICATE_REGISTRATION`、`LOCK_BUSY`、`UNAUTHORIZED`、`FORBIDDEN`、`RESOURCE_UNAVAILABLE`、`REASON_REQUIRED`、`PHONE_REQUIRED` | 通过 |
| diff 检查 | `git diff --check -- cloudfunctions/api miniprogram docs/backend docs/frontend docs/testing` | 通过，退出码 0；仅输出 LF/CRLF 换行提示 |

## 3. 后端回归明细

`npm test` 覆盖并通过以下 P0 点：

- 预约到店和桌台生命周期闭环。
- 预约时间冲突、短锁、30 分钟提前量、非法时间范围。
- 普通用户、员工权限、跨门店操作。
- 禁用/维修桌台资源拦截。
- 爽约 3 天限制与 manager 解除审计。
- 商家配置、Web 后台基础 action、我的中心、客户脱敏。
- `loginByWechat`、`authorizePhone`、`getCurrentRole`。
- 配置字段级校验、`manageTables:disable` 有效预约保护。
- `reviewCampaignRegistration` 审核、拒绝释放名额、活动报名短锁、后台列表分页。

## 4. 小程序和 mock 回归明细

- 预约页已静态确认：日期/时间/时长生成起止时间，传 `storeId/store_id`、`startTime/start_time`、`endTime/end_time`、`tableType/table_type`，消费 `list/items/tables`。
- 预约页已静态确认：桌台可用状态、不可用禁选、下一可约提示、预计价格计算、备注字段。
- 我的预约已静态确认：状态 tabs、`getMyReservations` 状态和分页入参、`list/items/reservations` 兼容、详情入口、待到店按钮。
- 预约详情已静态确认：状态文案映射和详情字段兼容。
- mock action 已运行确认：桌型筛选、不可用桌台样本、预约状态筛选、分页字段、新增登录/手机号/角色/活动审核 action、关键错误码。

## 5. 失败项和严重等级

| 严重等级 | 问题 |
|---|---|
| P0 | 未发现本地可验证 P0 失败项 |
| P1 | 未发现本地可验证 P1 失败项 |
| P2 | 未发现本地可验证 P2 失败项 |

说明：本结论只覆盖本地云函数测试、小程序静态检查和本地 mock action 检查，不代表微信开发者工具、真机或真实云端通过。

## 6. 复现步骤

1. 后端测试：
   - `cd cloudfunctions/api`
   - `npm test`
   - 结果：29/29 通过。
2. 后端语法：
   - `node --check src/handlers.js`
   - `node --check test/handler-flow.test.js`
   - 结果：通过。
3. 小程序 JS：
   - 对 `miniprogram/pages/reservation/index.js`、`miniprogram/pages/my-reservations/index.js`、`miniprogram/pages/reservation-detail/index.js`、`miniprogram/services/api.js`、`miniprogram/utils/mock.js`、`miniprogram/utils/constants.js` 执行 `node --check`
   - 结果：通过。
4. 小程序 JSON：
   - 用 Node `JSON.parse` 解析 `miniprogram/app.json` 和三个相关页面 JSON
   - 结果：通过。
5. WXML：
   - 用 Node 读取三页 WXML 并做基础标签闭合检查
   - 结果：通过。
6. mock action：
   - 用 Node 调用 `miniprogram/services/api.js` 的本地 mock 分支，断言 `getAvailableTables`、`getMyReservations`、新增登录/手机号/角色/活动审核 action 和关键错误码
   - 结果：通过。
7. diff 检查：
   - `git diff --check -- cloudfunctions/api miniprogram docs/backend docs/frontend docs/testing`
   - 结果：退出码 0；仅有 Git 提示 LF 将在下次触碰时替换为 CRLF。

## 7. 未实测范围

- 未使用微信开发者工具 GUI。
- 未做真机或体验版截图。
- 未部署云函数。
- 未连接真实云环境。
- 未写真实数据库。
- 未测试真实微信手机号解密授权。
- 未用真实价格、真实桌台编号、真实助教资料替换演示数据。
- 未做真实多账号联调。
- 未做真实云端高并发活动报名验证。
- 未做最终上线验收。

## 8. 是否需要用户打开微信开发者工具或真机截图

- 当前本地 P0 回归不需要用户打开微信开发者工具或提供真机截图。
- 若要进入微信 GUI/真机层验收，需要用户打开微信开发者工具或提供真机/体验版条件，并把该层单独作为后续 QA 范围。

## 9. Handoff 回传

message_type: handoff
request_id: REQ-20260620-P0功能模块QA回归
from_lane: qa
to_lane: coordinator
created_at: 2026-06-20T12:11:41+08:00
recorded_in: 当前对话 / docs/worklog.md / docs/testing/P0功能模块QA回归报告-20260620.md

消息内容：
- 完成内容：完成 P0 功能模块本地分层 QA 回归，覆盖后端本地测试、小程序静态检查、mock action 检查、错误码文案和 diff 检查；未做 UI 风格调整，未做最终上线验收。
- 改动文件：`docs/testing/P0功能模块QA回归报告-20260620.md`；`docs/worklog.md`。
- 验证结果：`cloudfunctions/api npm test` 29/29 通过；后端语法、小程序 JS/JSON/WXML/页面入口、mock action、错误码文案检查通过；`git diff --check -- cloudfunctions/api miniprogram docs/backend docs/frontend docs/testing` 退出码 0，仅有 LF/CRLF 换行提示。
- 失败项：未发现本地可验证 P0/P1/P2 失败项。
- 严重等级：无阻塞问题。
- 复现步骤：见本文档第 6 节。
- 未实测范围：微信开发者工具 GUI/真机、真实云端、真实数据库、真实手机号授权、真实价格/桌台/助教资料、多账号真实联调、高并发云端验证、最终上线验收。
- 是否需要用户打开微信开发者工具或真机截图：本地 P0 回归不需要；进入 GUI/真机层验收时需要。
- 建议下一步负责 agent：验收官 Agent 对照 P0 任务包、后端/前端报告和本 QA 报告做阶段验收，不得写最终上线验收结论。
- 是否触发暂停条件：否。
