# 写入测试桌台数据 - 2026-06-20

message_type: handoff
request_id: REQ-20260620-写入测试桌台数据
from_lane: backend
to_lane: coordinator
created_at: 2026-06-20T22:48:00+08:00
recorded_in: 当前对话 / docs/worklog.md / docs/backend/写入测试桌台数据-20260620.md

消息内容：
- 写入方式：
  - 未执行真实 DB 写入。
  - 原因：当前本机没有可用的真实云数据库写入通道。`tcb/cloudbase/tccli` 命令不存在；`TENCENTCLOUD_SECRETID/TENCENTCLOUD_SECRETKEY/TCB_SECRETID/TCB_SECRETKEY` 环境变量均未配置；微信开发者工具 CLI 只支持 `cloud env` 与 `cloud functions list/info/deploy/inc-deploy/download`，没有 DB 写入命令。
  - 本轮没有使用 `bootstrapMvp` 或 `seed.js`，因为它们会写 `store/staff_user/user/assistant/campaign/page_config/asset/audit_log` 等多个集合，超出“仅写 `billiard_table` 测试桌台数据”的授权范围。
- 写入数量：
  - 实际写入：0。
  - 目标写入：40 张测试桌台。
- 每类桌台数量：
  - 实际写入：0。
  - 目标 after：
    - A01-A12：12 张，`table_type=standard`，`table_type_name=普台`。
    - B01-B10：10 张，`table_type=silver`，`table_type_name=乔氏银腿`。
    - C01-C08：8 张，`table_type=duya`，`table_type_name=独牙`。
    - D01-D06：6 张，`table_type=gold`，`table_type_name=乔氏金腿`。
    - E01-E04：4 张，`table_type=rose_gold`，`table_type_name=乔氏玫瑰金`。
- C01-C08 摘要：
  - before：用户控制台只读结果已显示 `[C01-C08] []`、`cCount=0`、`hasAllC01ToC08=false`。
  - after 目标：`store_id=store_demo_001`，`code/table_no=C01-C08`，`table_type=duya`，`table_type_name=独牙`，`area_name=独牙区`，`enabled=true`，`status=idle`，`price_cent=5800`，`sort_order=23..30`。
  - 实际 after：未写入，暂无 after。
- 是否有失败记录：
  - 有，失败类型为“本机无可执行真实 DB 写入通道/凭据”，不是业务脚本失败。
  - 工具核对结果：

```text
Get-Command tcb,cloudbase,tccli -ErrorAction SilentlyContinue
结果：未发现可用命令。

TENCENTCLOUD_SECRETID / TENCENTCLOUD_SECRETKEY / TCB_SECRETID / TCB_SECRETKEY
结果：均未配置。

& 'E:\微信web开发者工具\cli.bat' cloud --help
结果：仅 env / functions。

& 'E:\微信web开发者工具\cli.bat' cloud functions --help
结果：仅 list / info / deploy / inc-deploy / download，无 DB 写入能力。
```

- before 记录：
  - 使用用户已完成的真实 DB 只读结果作为 before：
    - `[store_id=store_demo_001] count=0`
    - `[all_first_100] count=0`
    - `[C01-C08] []`
    - `[RESULT] totalRead:0,cCount:0,hasAllC01ToC08:false`
  - 判断：当前 `billiard_table` 为空，未发现非测试数据、`store_id` 冲突或覆盖风险；但本机无法直接复读确认。
- after 方案：
  - 只允许写 `billiard_table`。
  - 只写 `store_id=store_demo_001` 的 40 张测试桌台。
  - 不删除、不重置、不覆盖其它集合、不执行 `bootstrapMvp`、不执行 `seed.js`。
  - 建议使用云开发控制台数据库面板或具备 DB 权限的 CloudBase/Tencent CLI 执行一次性插入/设定。
- 可交给中枢/用户执行的字段清单：

| 区域 | 编号 | 数量 | table_type | table_type_name | area_name | price_cent | sort_order |
| --- | --- | ---: | --- | --- | --- | ---: | ---: |
| 普台区 | A01-A12 | 12 | standard | 普台 | 普台区 | 3800 | 1-12 |
| 银腿区 | B01-B10 | 10 | silver | 乔氏银腿 | 银腿区 | 4800 | 13-22 |
| 独牙区 | C01-C08 | 8 | duya | 独牙 | 独牙区 | 5800 | 23-30 |
| 金腿区 | D01-D06 | 6 | gold | 乔氏金腿 | 金腿区 | 6800 | 31-36 |
| 玫瑰金区 | E01-E04 | 4 | rose_gold | 乔氏玫瑰金 | 玫瑰金区 | 7800 | 37-40 |

  - 每条记录还应包含：`_id=${storeId}_table_${code}`、`store_id=store_demo_001`、`code`、`table_no=code`、`status=idle`、`enabled=true`、`created_at`、`updated_at`。
- 验证结果：
  - 已建立 before：使用用户真实 DB 控制台只读结果，集合为空。
  - 已建立目标 after 方案：40 张测试桌台字段与数量。
  - 已核对本机工具：无 DB 写入命令/凭据。
  - 未执行任何真实 DB 写入。
  - 待执行：`git diff --check -- docs/backend/写入测试桌台数据-20260620.md docs/worklog.md`。
- 剩余问题：
  - 当前无法由本机完成写库；需要中枢换用用户微信云开发控制台人工写入，或提供具备 DB 写入能力的工具/凭据。
  - 写入后仍需真实 DB 只读核对 after，再交测试工程师/用户复测页面；不得直接写“页面已修复”。
- 下一步负责 agent：
  - Codex 中枢：提供可执行 DB 写入通道，或指导用户在微信云开发控制台仅写入 `billiard_table` 40 张测试桌台。
  - 后端工程师 Agent：拿到可执行 DB 写入通道后再执行写入并记录 after。
  - 测试工程师 Agent：写入完成并只读验证后，复测预约页“独牙”是否显示 C01-C08。
- 是否触发暂停条件：
  - 是。触发“权限不足/缺少可执行真实 DB 写入通道”。本轮未写库、未部署、未改代码。

## Handoff 回传

message_type: handoff
request_id: REQ-20260620-写入测试桌台数据
from_lane: backend
to_lane: coordinator
created_at: 2026-06-20T22:48:00+08:00
recorded_in: 当前对话 / docs/worklog.md / docs/backend/写入测试桌台数据-20260620.md

消息内容：
- 写入方式：未执行写入；本机无真实 DB 写入工具/凭据，DevTools CLI 无 DB 写入命令。未使用 `bootstrapMvp/seed`，避免越权写其它集合。
- 写入数量：实际 0；目标 40。
- 每类桌台数量：实际 0；目标 A12/B10/C8/D6/E4。
- C01-C08 摘要：before 为用户真实 DB 只读结果 `[]/cCount=0/hasAllC01ToC08=false`；after 未产生，目标为 C01-C08 `duya/独牙/enabled=true/status=idle`。
- 是否有失败记录：有，失败在工具/权限层：无 `tcb/cloudbase/tccli`，无腾讯云密钥，CLI 不支持 DB 写入。
- 验证结果：before 与 after 方案已记录；工具能力核对完成；`git diff --check` 待最终命令确认。
- 剩余问题：需要中枢提供可执行 DB 写入通道，或让用户在微信云开发控制台按字段清单人工写入。
- 下一步负责 agent：Codex 中枢提供 DB 写入通道；后端工程师 Agent 执行或复核 after；测试工程师 Agent 页面复测。
- 是否触发暂停条件：是。缺少可执行写库通道，本轮未写真实数据库。
